<?xml version="1.0" encoding="UTF-8" ?>
<!--
    Part of the Shopify Connector (S1 guided setup).

    One decision per step. Every status carries text, never colour alone. Every
    control is a real focusable element with a visible focus ring, and the step
    heading is focusable so focus can land on it after each advance.

    EVERY BRANCH COMPARES A STEP KEY, NEVER A POSITION. `state.stepKey === 'credential'`
    reads as what it is; `state.step === 3` reads as nothing and silently means
    something different the moment a step is inserted — which Wave 5 did. The
    ordinal in the heading comes from the server's own ordered step list.

    No decoration on the credential or readiness steps: those are the two
    moments an operator most needs to read carefully, and an animation there
    would be a distraction dressed as delight.
-->
<templates xml:space="preserve">

    <t t-name="shopify_connector_core.SetupWizard">
        <div class="o_sc_setup" t-att-dir="direction">
            <div class="o_sc_setup__inner">

                <!-- ============ LOADING ============ -->
                <t t-if="state.status === 'loading'">
                    <div class="sc-skeleton sc-skeleton--band" aria-hidden="true"/>
                    <p role="status">Loading setup…</p>
                </t>

                <!-- ============ ERROR ============ -->
                <t t-elif="state.status === 'error'">
                    <div class="sc-band sc-band--danger" role="alert">
                        <i class="fa fa-exclamation-triangle sc-band__icon" aria-hidden="true"/>
                        <div class="sc-band__body">
                            <div class="sc-band__text">Setup could not be loaded</div>
                            <div class="sc-band__hint" t-esc="state.errorMessage"/>
                        </div>
                    </div>
                </t>

                <!-- ============ READY ============ -->
                <t t-else="">

                    <section t-if="state.showCompletion"
                             class="sc_setup__complete" aria-live="polite">
                        <span class="sc_setup__complete-icon" aria-hidden="true">
                            <i class="fa fa-check"/>
                        </span>
                        <p class="sc_setup__eyebrow">Setup complete</p>
                        <h1 class="sc_setup__complete-title" tabindex="-1" t-ref="heading">
                            <t t-esc="store.name || 'Your Shopify store'"/> is connected
                        </h1>
                        <p class="sc_setup__complete-copy">
                            The connector is activated and your twelve safety checkpoints are
                            saved. Activation did not start a sync or write anything to Shopify.
                        </p>
                        <div class="sc_setup__complete-actions" role="group"
                             aria-label="Completed setup actions">
                            <button class="sc-btn sc-btn--primary sc_setup_dashboard"
                                    t-on-click="() => this.openDashboard()">
                                Go to overview
                            </button>
                            <button class="sc-btn sc_setup_settings"
                                    t-on-click="() => this.openStoreSettings()">
                                Open store settings
                            </button>
                            <button class="sc-link sc_setup_rerun"
                                    t-att-disabled="state.busy"
                                    t-on-click="() => this.rerun()">
                                Review setup again
                            </button>
                        </div>
                        <p class="sc_setup__note">
                            Completed by <t t-esc="store.setup_completed_by"/>.
                        </p>
                    </section>

                    <!-- Four merchant phases over twelve guarded checkpoints. -->
                    <nav t-if="!state.showCompletion" class="sc_setup__steps"
                         aria-label="Setup progress">
                        <ol>
                            <t t-foreach="phases" t-as="phase" t-key="phase.key">
                                <li t-att-class="phaseClass(phase)"
                                    t-att-data-phase-key="phase.key"
                                    t-att-aria-current="phase.current ? 'step' : false">
                                    <span class="sc_setup_phase__marker" aria-hidden="true">
                                        <i t-if="phase.done" class="fa fa-check"/>
                                        <span t-else="" t-esc="phase_index + 1"/>
                                    </span>
                                    <span class="sc_setup_phase__copy">
                                        <span class="sc_setup_phase__label" t-esc="phase.label"/>
                                        <span class="sc_setup_phase__description" t-esc="phase.description"/>
                                    </span>
                                </li>
                            </t>
                        </ol>
                    </nav>

                    <!--
                        THE SCROLLING REGION.

                        The panel scrolls; the action row below it does not.
                        Splitting them is what lets Back / Continue / Save &amp;
                        Exit stay on screen through a long Permissions list or a
                        long readiness result, which is where an operator most
                        needs them and exactly where they used to disappear
                        below the fold.
                    -->
                    <section t-if="!state.showCompletion" class="sc_setup__panel" aria-live="polite">
                        <h1 class="sc_setup__heading" tabindex="-1" t-ref="heading">
                            <span class="sc_setup__heading-count"
                                  t-att-aria-label="'Setup checkpoint ' + currentStep.index + ' of ' + state.data.step_count">
                                <t t-esc="currentPhase.label"/> ·
                                <t t-esc="currentPhaseStepNumber"/> of
                                <t t-esc="currentPhase.ordered.length"/>
                            </span>
                            <span t-esc="currentStep.label"/>
                        </h1>

                        <!-- A refusal the server made. Never swallowed, never
                             turned into a silent no-op. -->
                        <div t-if="state.errorMessage"
                             class="sc-band sc-band--danger sc_setup__error"
                             role="alert" id="sc_setup_error">
                            <i class="fa fa-exclamation-triangle sc-band__icon" aria-hidden="true"/>
                            <div class="sc-band__body">
                                <div class="sc-band__text" t-esc="state.errorMessage"/>
                            </div>
                        </div>

                        <!-- ===== welcome ===== -->
                        <t t-if="state.stepKey === 'welcome'">
                            <p class="sc_setup__lead">
                                This connects one Shopify store to this Odoo database. It
                                takes about ten minutes, and <strong>nothing synchronises
                                and nothing is written to Shopify until you finish and
                                say so</strong>.
                            </p>
                            <h2 class="sc_setup__subheading">What you'll need</h2>
                            <ul class="sc_setup__list">
                                <li>Admin access to your Shopify organization, so you can create an app for this store in the Dev Dashboard and copy its Client ID and Client secret — or an Admin API access token you already hold from an app created the older way. Either path works; the credential step explains both.</li>
                                <li>About ten minutes. You can stop at any point and pick up where you left off.</li>
                            </ul>
                            <div class="sc-band sc-band--info" role="note">
                                <i class="fa fa-info-circle sc-band__icon" aria-hidden="true"/>
                                <div class="sc-band__body">
                                    <div class="sc-band__text">This connector needs Odoo.sh or on-premise hosting</div>
                                    <div class="sc-band__hint">
                                        Odoo Online (SaaS) cannot run it, because the connector
                                        needs background workers and outbound calls that Odoo
                                        Online does not provide. Better to know now than at the
                                        last step.
                                    </div>
                                </div>
                            </div>
                        </t>

                        <!-- ===== identity ===== -->
                        <t t-if="state.stepKey === 'identity'">
                            <p class="sc_setup__lead">
                                Name this store so you recognise it later, and point it at
                                the right Shopify shop.
                            </p>
                            <div class="sc_setup__field">
                                <label for="sc_setup_name">Store name</label>
                                <input id="sc_setup_name" type="text" class="o_input"
                                       t-model="state.form.name"
                                       aria-describedby="sc_setup_name_help"/>
                                <small id="sc_setup_name_help">
                                    Anything you'll recognise — "Main storefront", "EU shop".
                                </small>
                            </div>
                            <div class="sc_setup__field">
                                <label for="sc_setup_domain">Shopify domain</label>
                                <input id="sc_setup_domain" type="text" class="o_input"
                                       t-model="state.form.shopDomain"
                                       placeholder="acme-supplies.myshopify.com"
                                       aria-describedby="sc_setup_domain_help"/>
                                <small id="sc_setup_domain_help">
                                    Your store's permanent address, ending in .myshopify.com.
                                    Find it in Shopify under Settings → Domains. We confirm it
                                    against Shopify at the readiness step — we don't take it on
                                    trust here.
                                </small>
                            </div>
                            <p class="sc_setup__note">
                                One Shopify store per setup. Connecting a second shop means a
                                second store here.
                            </p>
                        </t>

                        <!-- ===== credential ===== -->
                        <t t-if="state.stepKey === 'credential'">
                            <p class="sc_setup__lead">
                                Choose how this store authenticates with Shopify, then
                                enter that path's credentials once.
                            </p>
                            <!--
                                TWO REAL PATHS, STATED AS WHAT THE MERCHANT HAS.

                                Shopify's current app-creation flow (the Dev
                                Dashboard) does not show a permanent Admin API
                                token to copy: the app exchanges its Client ID
                                and Client secret for a 24-hour token, and the
                                connector does that exchange and renewal itself.
                                A merchant who created their app the older way,
                                or was handed a long-lived token, still has that
                                token and it still works. The chooser asks which
                                of those two things the merchant actually holds,
                                because no error message downstream can tell
                                them which one they should have pasted.
                            -->
                            <div class="sc_setup__modes" role="radiogroup"
                                 aria-label="How this store authenticates">
                                <label class="sc_setup__mode"
                                       t-att-class="state.form.credentialMode === 'dev_dashboard_client_credentials' ? 'sc_setup__mode--selected' : ''">
                                    <input type="radio" name="sc_credential_mode"
                                           value="dev_dashboard_client_credentials"
                                           t-att-checked="state.form.credentialMode === 'dev_dashboard_client_credentials' ? 'checked' : undefined"
                                           t-on-change="() => this.setCredentialMode('dev_dashboard_client_credentials')"/>
                                    <span class="sc_setup__mode-title">Dev Dashboard app (Client ID and secret)</span>
                                    <span class="sc_setup__mode-hint">
                                        For an app created in Shopify's Dev Dashboard. Odoo
                                        exchanges these for an access token and renews it
                                        automatically — Shopify does not show you a token to
                                        copy on this path.
                                    </span>
                                </label>
                                <label class="sc_setup__mode"
                                       t-att-class="state.form.credentialMode === 'offline_access_token' ? 'sc_setup__mode--selected' : ''">
                                    <input type="radio" name="sc_credential_mode"
                                           value="offline_access_token"
                                           t-att-checked="state.form.credentialMode === 'offline_access_token' ? 'checked' : undefined"
                                           t-on-change="() => this.setCredentialMode('offline_access_token')"/>
                                    <span class="sc_setup__mode-title">Existing Admin API access token</span>
                                    <span class="sc_setup__mode-hint">
                                        For a store that already holds a long-lived Admin API
                                        access token from an app created the older way.
                                    </span>
                                </label>
                            </div>

                            <t t-if="state.form.credentialMode === 'dev_dashboard_client_credentials'">
                                <!--
                                    `role="status"`, not `role="note"`. This band
                                    is rendered conditionally on the selected
                                    authentication mode, so its content CHANGES
                                    while the step is open. `note` is a valid
                                    ARIA role but a document-structure one, not a
                                    live region (WAI-ARIA 1.2 lists alert, log,
                                    marquee, status and timer), so a screen
                                    reader announced nothing when the operator
                                    switched paths and the guidance silently
                                    became different guidance. `status` is the
                                    polite live region for exactly this: it is
                                    announced when it appears and does not
                                    interrupt.
                                -->
                                <div class="sc-band sc-band--info" role="status">
                                    <i class="fa fa-key sc-band__icon" aria-hidden="true"/>
                                    <div class="sc-band__body">
                                        <div class="sc-band__text">Both values are on your app's Overview page in the Dev Dashboard</div>
                                        <div class="sc-band__hint">
                                            This works only when the app and the store belong to
                                            the same Shopify organization. The token Shopify
                                            issues lasts 24 hours; Odoo requests a fresh one
                                            before it expires, so there is nothing to renew by
                                            hand. Shopify's guide:
                                            <a href="https://shopify.dev/docs/apps/build/dev-dashboard/get-api-access-tokens"
                                               target="_blank" rel="noopener noreferrer">Get API access tokens (Dev Dashboard)</a>.
                                        </div>
                                    </div>
                                </div>
                                <div class="sc_setup__field">
                                    <label for="sc_setup_client_id">Client ID</label>
                                    <input id="sc_setup_client_id" type="text"
                                           class="o_input sc_setup_client_id"
                                           autocomplete="off" spellcheck="false"
                                           aria-describedby="sc_setup_client_id_help"/>
                                    <small id="sc_setup_client_id_help">
                                        The app's public identifier. Shopify shows it beside
                                        the Client secret.
                                    </small>
                                </div>
                                <div class="sc_setup__field">
                                    <label for="sc_setup_client_secret">Client secret</label>
                                    <input id="sc_setup_client_secret" type="password"
                                           class="o_input sc_setup_client_secret"
                                           autocomplete="off" spellcheck="false"
                                           aria-describedby="sc_setup_client_secret_help"/>
                                    <small id="sc_setup_client_secret_help">
                                        Stored behind administrator-only access and never shown
                                        again — not here, not in the store form, not in a log.
                                        Rotate it in the Dev Dashboard if it is ever exposed.
                                    </small>
                                </div>
                            </t>

                            <t t-if="state.form.credentialMode === 'offline_access_token'">
                                <!--
                                    WHY THIS DISCLOSURE EXISTS, IN THESE WORDS.

                                    A Shopify custom app shows three secret-looking
                                    values, and only one of them is the one this path
                                    needs. Operators paste the Client ID or the
                                    Client Secret, get an authentication failure at
                                    the next step, and have no way to tell from the
                                    error which of the three was wrong. Naming all
                                    three, and saying plainly what the other two are
                                    for, is the whole fix.
                                -->
                                <!--
                                    `role="status"`, not `role="note"`. This band
                                    is rendered conditionally on the selected
                                    authentication mode, so its content CHANGES
                                    while the step is open. `note` is a valid
                                    ARIA role but a document-structure one, not a
                                    live region (WAI-ARIA 1.2 lists alert, log,
                                    marquee, status and timer), so a screen
                                    reader announced nothing when the operator
                                    switched paths and the guidance silently
                                    became different guidance. `status` is the
                                    polite live region for exactly this: it is
                                    announced when it appears and does not
                                    interrupt.
                                -->
                                <div class="sc-band sc-band--info" role="status">
                                    <i class="fa fa-key sc-band__icon" aria-hidden="true"/>
                                    <div class="sc-band__body">
                                        <div class="sc-band__text">This path needs the Admin API access token — not the Client ID, and not the Client Secret</div>
                                        <div class="sc-band__hint">
                                            An older-style custom app shows all three. The
                                            <strong>Admin API access token</strong> is the one that
                                            goes in the field below. If your app only shows a
                                            Client ID and Client secret, use the Dev Dashboard
                                            path above instead.
                                        </div>
                                        <div class="sc-band__hint">
                                            How long a token stays valid depends on how it was
                                            issued. Check your Shopify app's authentication method
                                            rather than assuming a fixed lifetime.
                                        </div>
                                    </div>
                                </div>
                                <div class="sc_setup__field">
                                    <label for="sc_setup_token">Admin API access token</label>
                                    <input id="sc_setup_token" type="password"
                                           class="o_input sc_setup_token"
                                           autocomplete="off" spellcheck="false"
                                           aria-describedby="sc_setup_token_help"/>
                                    <small id="sc_setup_token_help">
                                        Stored behind administrator-only access and never shown
                                        again — not here, not in the store form, not in a log. If
                                        you lose it, create a new one in Shopify and paste that.
                                    </small>
                                </div>
                            </t>

                            <p t-if="store.credential_present" class="sc_setup__note" role="status">
                                A credential is already on record for this store
                                <t t-if="store.auth_mode === 'dev_dashboard_client_credentials'">(Dev Dashboard app)</t>
                                <t t-else="">(access token)</t>.
                                Saving new values replaces it, and the connection has to be
                                tested again.
                            </p>
                            <div class="sc-band sc-band--info" role="note">
                                <i class="fa fa-lock sc-band__icon" aria-hidden="true"/>
                                <div class="sc-band__body">
                                    <div class="sc-band__text">Secrets entered here are write-only</div>
                                    <div class="sc-band__hint">
                                        They are protected by access control, not by encryption at
                                        rest. Anyone with database access can read them, so treat
                                        database backups as containing them.
                                    </div>
                                </div>
                            </div>
                        </t>

                        <!-- ===== scopes ===== -->
                        <t t-if="state.stepKey === 'scopes'">
                            <p class="sc_setup__lead">
                                These are the permissions the connector needs, and why. You
                                grant them in Shopify when you create the custom app —
                                <strong>this screen does not grant anything</strong>. We
                                verify them at the readiness step.
                            </p>
                            <ul class="sc_setup__scopes">
                                <t t-foreach="state.data.scopes" t-as="scope" t-key="scope.scope">
                                    <li>
                                        <span class="sc_setup__scope-name" t-esc="scope.scope"/>
                                        <span class="sc_setup__scope-reason" t-esc="scope.reason"/>
                                        <span t-if="scope.granted" class="sc-badge sc-badge--success">Granted</span>
                                        <span t-else="" class="sc-badge sc-badge--neutral">Not confirmed yet</span>
                                    </li>
                                </t>
                            </ul>
                        </t>

                        <!-- ===== test_connection ===== -->
                        <t t-if="state.stepKey === 'test_connection'">
                            <p class="sc_setup__lead">
                                This makes one read-only call to Shopify to prove the
                                credentials work. Nothing is created, changed or deleted.
                            </p>
                            <div t-if="store.test_connection_result === 'pass'"
                                 class="sc-band sc-band--success" role="status">
                                <i class="fa fa-check-circle sc-band__icon" aria-hidden="true"/>
                                <div class="sc-band__body">
                                    <div class="sc-band__text">The connection works</div>
                                    <div class="sc-band__hint">Shopify accepted the credential.</div>
                                </div>
                            </div>
                            <div t-elif="store.test_connection_result === 'fail'"
                                 class="sc-band sc-band--danger" role="alert">
                                <i class="fa fa-exclamation-triangle sc-band__icon" aria-hidden="true"/>
                                <div class="sc-band__body">
                                    <div class="sc-band__text">The connection did not work</div>
                                    <div class="sc-band__hint" t-esc="store.test_connection_reason"/>
                                    <div t-if="store.auth_mode === 'dev_dashboard_client_credentials'"
                                         class="sc-band__hint">
                                        Go back one step and re-enter the Client ID and Client
                                        secret from your app's page in the Dev Dashboard. Check
                                        that the app belongs to the same Shopify organization as
                                        this store, and check the shop domain.
                                    </div>
                                    <div t-else="" class="sc-band__hint">
                                        Go back one step and paste the token again — check that
                                        you copied the Admin API access token and not the Client
                                        ID or Client Secret — or check the shop domain.
                                    </div>
                                </div>
                            </div>
                            <p t-else="" class="sc_setup__note" role="status">
                                Not tested yet.
                            </p>
                            <div>
                                <button class="sc-btn sc_setup_run_test"
                                        t-att-disabled="state.busy"
                                        t-on-click="() => this.runTestConnection()">
                                    <span t-if="store.test_connection_result">Test again</span>
                                    <span t-else="">Test connection</span>
                                </button>
                            </div>
                        </t>

                        <!-- ===== directions ===== -->
                        <t t-if="state.stepKey === 'directions'">
                            <p class="sc_setup__lead">
                                Choose what this connector does. Enabling nothing is a valid
                                choice — a connected store that syncs nothing yet is fine.
                            </p>
                            <ul class="sc_setup__domains">
                                <t t-foreach="state.data.domains" t-as="domain" t-key="domain.key">
                                    <li class="sc_setup__domain">
                                        <label>
                                            <input type="checkbox"
                                                   t-att-id="'sc_setup_domain_' + domain.key"
                                                   t-att-checked="isDomainEnabled(domain.key)"
                                                   t-on-change="() => this.toggleDomain(domain.key)"/>
                                            <span class="sc_setup__domain-label" t-esc="domain.label"/>
                                            <span class="sc-badge sc-badge--neutral" t-esc="domain.direction"/>
                                        </label>
                                        <span class="sc_setup__domain-happens" t-esc="domain.happens"/>
                                        <span t-if="isDomainEnabled(domain.key)"
                                              class="sc_setup__domain-withheld" t-esc="domain.withheld"/>
                                    </li>
                                </t>
                            </ul>
                        </t>

                        <!-- ===== location_mapping ===== -->
                        <t t-if="state.stepKey === 'location_mapping'">
                            <!--
                                CONDITIONAL, AND SAID SO RATHER THAN HIDDEN.

                                When inventory is off this step is Not required. It
                                stays in the list and keeps its position: removing it
                                would renumber every step after it, which is precisely
                                the coupling this wave removes.
                            -->
                            <t t-if="!currentStepApplies">
                                <div class="sc-band sc-band--info sc_setup_location_skipped"
                                     role="status">
                                    <i class="fa fa-info-circle sc-band__icon" aria-hidden="true"/>
                                    <div class="sc-band__body">
                                        <div class="sc-band__text">Not required</div>
                                        <div class="sc-band__hint" t-esc="currentStep.skipped_reason"/>
                                    </div>
                                </div>
                            </t>
                            <t t-else="">
                                <p class="sc_setup__lead">
                                    Each Shopify location has to be paired with an Odoo
                                    internal location before its stock can be synchronised.
                                    <strong>An unmapped Shopify location is not
                                    synchronised.</strong> Pairing is always explicit —
                                    nothing is matched by name.
                                </p>

                                <!-- Refresh, and the state of the refresh. Text, never
                                     colour alone, and never silence. -->
                                <div class="sc_setup__refresh">
                                    <button class="sc-btn sc_setup_refresh_locations"
                                            t-att-disabled="state.busy"
                                            t-on-click="() => this.refreshLocations()">
                                        <span>Refresh Shopify locations</span>
                                    </button>
                                    <span class="sc-badge sc_setup_refresh_state"
                                          t-att-class="'sc-badge--' + (refreshState() === 'succeeded' ? 'success' : (refreshState() === 'failed' ? 'danger' : (refreshState() === 'none' ? 'neutral' : 'info')))"
                                          t-esc="refreshLabel()"/>
                                </div>
                                <div t-if="refreshState() === 'waiting' or refreshState() === 'running'"
                                     class="sc-band sc-band--info" role="status">
                                    <i class="fa fa-clock-o sc-band__icon" aria-hidden="true"/>
                                    <div class="sc-band__body">
                                        <div class="sc-band__text">Reading your Shopify locations</div>
                                        <div class="sc-band__hint">
                                            This runs in the background. Until it finishes, an
                                            empty list below is not a report that Shopify has no
                                            locations — it just means we have not read them yet.
                                        </div>
                                    </div>
                                </div>
                                <div t-if="refreshState() === 'failed'"
                                     class="sc-band sc-band--danger" role="alert">
                                    <i class="fa fa-exclamation-triangle sc-band__icon" aria-hidden="true"/>
                                    <div class="sc-band__body">
                                        <div class="sc-band__text">The last location refresh did not finish</div>
                                        <div class="sc-band__hint">
                                            The list below is whatever was read last time, and may
                                            be incomplete. Try refreshing again; if it keeps
                                            failing, check the connection and the read_locations
                                            permission.
                                        </div>
                                    </div>
                                </div>
                                <div t-if="refreshState() === 'none' and !locations.locations.length"
                                     class="sc-band sc-band--warning" role="status">
                                    <i class="fa fa-info-circle sc-band__icon" aria-hidden="true"/>
                                    <div class="sc-band__body">
                                        <div class="sc-band__text">No locations have been read yet</div>
                                        <div class="sc-band__hint">
                                            Press Refresh Shopify locations to read this store's
                                            list.
                                        </div>
                                    </div>
                                </div>

                                <!--
                                    Wave 5: the search IS the reachability
                                    guarantee. The lists below are bounded
                                    pages, and that stays true — but every
                                    eligible location past the first page is
                                    now reachable by typing a few letters,
                                    with honest Showing-X-of-Y counts and a
                                    Load more that pages through the full
                                    eligible set. Store and company filters
                                    are structural on the server for every
                                    page and every query.
                                -->
                                <div t-if="locationHasMore('shopify') and state.locationSearch.shopify.items === null"
                                     class="sc-band sc-band--info" role="status">
                                    <i class="fa fa-search sc-band__icon" aria-hidden="true"/>
                                    <div class="sc-band__body">
                                        <div class="sc-band__text">This store has more locations than one page shows</div>
                                        <div class="sc-band__hint">
                                            Search below to find any of them — every location can
                                            be found and mapped from this step.
                                        </div>
                                    </div>
                                </div>

                                <div class="sc_setup__field sc_setup__location-search">
                                    <label for="sc_setup_search_shopify">Find a Shopify location</label>
                                    <div class="sc_setup__search-row">
                                        <input id="sc_setup_search_shopify" type="search"
                                               class="o_input sc_setup_search_shopify"
                                               placeholder="Type part of the location's name…"
                                               t-model="state.locationSearch.shopify.query"
                                               t-on-keydown="(ev) => this.onLocationSearchKeydown(ev, 'shopify')"/>
                                        <button class="sc-btn sc_setup_search_shopify_go"
                                                t-att-disabled="state.busy"
                                                t-on-click="() => this.searchLocations('shopify')">
                                            <span>Search</span>
                                        </button>
                                        <button t-if="state.locationSearch.shopify.items !== null"
                                                class="sc-btn sc-btn--secondary sc_setup_search_shopify_clear"
                                                t-att-disabled="state.busy"
                                                t-on-click="() => this.clearLocationSearch('shopify')">
                                            <span>Clear</span>
                                        </button>
                                    </div>
                                </div>

                                <!--
                                    The counter carries its DOMAIN in a class and
                                    a data attribute. Two elements sharing one
                                    `.sc_setup__showing` class made every
                                    assertion about "the count" ambiguous: a tour
                                    matching `Showing 1 of 1` could not say
                                    whether it had found the Shopify counter or
                                    the Odoo one, and a change to either would
                                    satisfy a test written about the other.
                                -->
                                <p class="sc_setup__showing sc_setup__showing--shopify"
                                   data-showing="shopify" role="status" aria-live="polite">
                                    Showing <t t-esc="locationShowing('shopify').shown"/>
                                    of <t t-esc="locationShowing('shopify').total"/>
                                    Shopify locations<t t-if="state.locationSearch.shopify.items !== null"> matching the search</t>.
                                </p>

                                <ul class="sc_setup__locations">
                                    <t t-foreach="visibleShopifyLocations" t-as="location"
                                       t-key="location.shopify_gid">
                                        <!--
                                            `data-mapped` exists because the
                                            BADGE TEXT is not a usable state
                                            selector. hoot-dom's `:contains()`
                                            matches case-insensitive substrings,
                                            so `:contains('Mapped')` also matches
                                            "Not mapped": a tour asserting a
                                            location became mapped passed whether
                                            or not it did, including when the
                                            mapping call had failed. The
                                            attribute is machine-readable and
                                            exact; the badge stays the human
                                            text.
                                        -->
                                        <li class="sc_setup__location"
                                            t-att-data-mapped="location.mapped ? '1' : '0'"
                                            t-att-data-shopify-gid="location.shopify_gid">
                                            <span class="sc_setup__location-name" t-esc="location.name"/>
                                            <span t-if="location.mapped"
                                                  class="sc-badge sc-badge--success">Mapped</span>
                                            <span t-else=""
                                                  class="sc-badge sc-badge--warning">Not mapped</span>
                                            <span t-if="location.mapped"
                                                  class="sc_setup__location-target">
                                                Odoo location: <t t-esc="location.odoo_location_name"/>
                                            </span>
                                            <span t-else="" class="sc_setup__location-target">
                                                Stock for this Shopify location will not be
                                                synchronised until it is mapped.
                                            </span>
                                            <code class="sc_setup__location-gid"
                                                  t-esc="location.shopify_gid"/>
                                        </li>
                                    </t>
                                </ul>
                                <div t-if="locationHasMore('shopify') and state.locationSearch.shopify.items !== null">
                                    <button class="sc-btn sc-btn--secondary sc_setup_more_shopify"
                                            t-att-disabled="state.busy"
                                            t-on-click="() => this.loadMoreLocations('shopify')">
                                        <span>Load more Shopify locations</span>
                                    </button>
                                </div>

                                <!--
                                    A zero-result Shopify SEARCH used to hide
                                    this whole block: the Odoo search box, the
                                    Map button and the only visible route back,
                                    because "no rows on screen" and "this store
                                    has no locations" were the same condition.
                                    They are not: with a search active the list
                                    is empty because of the query, and the fix is
                                    to clear it, which requires the controls to
                                    still be there.
                                -->
                                <div t-if="!visibleShopifyLocations.length and locationEmptyReason('shopify')"
                                     class="sc_setup__empty sc_setup__empty--shopify sc-band sc-band--info"
                                     role="status">
                                    <i class="fa fa-search sc-band__icon" aria-hidden="true"/>
                                    <div class="sc-band__body">
                                        <div class="sc-band__text" t-esc="locationEmptyReason('shopify')"/>
                                    </div>
                                </div>
                                <div t-if="visibleShopifyLocations.length or state.locationSearch.shopify.items !== null" class="sc_setup__map">
                                    <div class="sc_setup__field">
                                        <label for="sc_setup_map_shopify">Shopify location</label>
                                        <select id="sc_setup_map_shopify" class="o_input"
                                                t-model="state.form.mapShopifyGid">
                                            <option value="">Choose a Shopify location…</option>
                                            <t t-foreach="visibleShopifyLocations" t-as="location"
                                               t-key="'opt-' + location.shopify_gid">
                                                <option t-att-value="location.shopify_gid"
                                                        t-esc="location.name"/>
                                            </t>
                                        </select>
                                        <small>
                                            The choices follow the list above — search to bring a
                                            location into it.
                                        </small>
                                    </div>
                                    <div class="sc_setup__field">
                                        <label for="sc_setup_search_odoo">Find an Odoo location</label>
                                        <div class="sc_setup__search-row">
                                            <input id="sc_setup_search_odoo" type="search"
                                                   class="o_input sc_setup_search_odoo"
                                                   placeholder="Type part of the location's name…"
                                                   t-model="state.locationSearch.odoo.query"
                                                   t-on-keydown="(ev) => this.onLocationSearchKeydown(ev, 'odoo')"/>
                                            <button class="sc-btn sc_setup_search_odoo_go"
                                                    t-att-disabled="state.busy"
                                                    t-on-click="() => this.searchLocations('odoo')">
                                                <span>Search</span>
                                            </button>
                                            <button t-if="state.locationSearch.odoo.items !== null"
                                                    class="sc-btn sc-btn--secondary sc_setup_search_odoo_clear"
                                                    t-att-disabled="state.busy"
                                                    t-on-click="() => this.clearLocationSearch('odoo')">
                                                <span>Clear</span>
                                            </button>
                                        </div>
                                    </div>
                                    <div class="sc_setup__field">
                                        <label for="sc_setup_map_odoo">Odoo location</label>
                                        <select id="sc_setup_map_odoo" class="o_input"
                                                t-model="state.form.mapOdooLocationId">
                                            <option value="">Choose an Odoo location…</option>
                                            <t t-foreach="visibleOdooLocations" t-as="odooLocation"
                                               t-key="odooLocation.id">
                                                <option t-att-value="odooLocation.id"
                                                        t-esc="odooLocation.name"/>
                                            </t>
                                        </select>
                                        <p class="sc_setup__showing sc_setup__showing--odoo"
                                           data-showing="odoo" role="status" aria-live="polite">
                                            Showing <t t-esc="locationShowing('odoo').shown"/>
                                            of <t t-esc="locationShowing('odoo').total"/>
                                            Odoo locations<t t-if="state.locationSearch.odoo.items !== null"> matching the search</t>.
                                        </p>
                                        <div t-if="locationHasMore('odoo') and state.locationSearch.odoo.items !== null">
                                            <button class="sc-btn sc-btn--secondary sc_setup_more_odoo"
                                                    t-att-disabled="state.busy"
                                                    t-on-click="() => this.loadMoreLocations('odoo')">
                                                <span>Load more Odoo locations</span>
                                            </button>
                                        </div>
                                        <!--
                                            FOUR empty states, not one. This
                                            single line was shown for all of
                                            them, so an operator whose SEARCH
                                            matched nothing was told they had no
                                            Inventory access, and one who really
                                            had no Inventory access was told to
                                            create a warehouse they would not be
                                            able to see. `locationEmptyReason`
                                            returns the server's own reason.
                                        -->
                                        <small t-if="!visibleOdooLocations.length and locationEmptyReason('odoo')"
                                               class="sc_setup__empty sc_setup__empty--odoo"
                                               role="status"
                                               t-esc="locationEmptyReason('odoo')"/>
                                        <small t-elif="!visibleOdooLocations.length"
                                               class="sc_setup__empty sc_setup__empty--odoo"
                                               role="status">
                                            No internal Odoo locations are visible to you in this
                                            company. Ask an Odoo administrator for Inventory
                                            access, or create a warehouse first.
                                        </small>
                                    </div>
                                    <div>
                                        <button class="sc-btn sc_setup_create_mapping"
                                                t-att-disabled="state.busy"
                                                t-on-click="() => this.createMapping()">
                                            <span>Map this location</span>
                                        </button>
                                    </div>
                                </div>
                            </t>
                        </t>

                        <!-- ===== source_of_truth ===== -->
                        <t t-if="state.stepKey === 'source_of_truth'">
                            <p class="sc_setup__lead">
                                Two decisions with real consequences, and no default. Both
                                are required.
                            </p>
                            <fieldset class="sc_setup__choices">
                                <legend>Which system is the source for your catalog?</legend>
                                <t t-foreach="state.data.matching_choices" t-as="choice" t-key="choice.value">
                                    <label class="sc_setup__choice">
                                        <input type="radio" name="sc_setup_matching"
                                               t-att-value="choice.value"
                                               t-att-checked="state.form.matching === choice.value"
                                               t-on-change="() => this.state.form.matching = choice.value"/>
                                        <span class="sc_setup__choice-label" t-esc="choice.label"/>
                                        <span class="sc_setup__choice-consequence" t-esc="choice.consequence"/>
                                    </label>
                                </t>
                            </fieldset>
                            <fieldset class="sc_setup__choices">
                                <legend>Which system is the price authority?</legend>
                                <t t-foreach="state.data.price_choices" t-as="choice" t-key="choice.value">
                                    <label class="sc_setup__choice">
                                        <input type="radio" name="sc_setup_price"
                                               t-att-value="choice.value"
                                               t-att-checked="state.form.price === choice.value"
                                               t-on-change="() => this.state.form.price = choice.value"/>
                                        <span class="sc_setup__choice-label" t-esc="choice.label"/>
                                        <span class="sc_setup__choice-consequence" t-esc="choice.consequence"/>
                                    </label>
                                </t>
                            </fieldset>
                            <p class="sc_setup__note">
                                Leaving the price authority with Shopify means prices are never
                                exported. That block is stated here so it is never a surprise
                                later.
                            </p>
                        </t>

                        <!-- ===== notification ===== -->
                        <t t-if="state.stepKey === 'notification'">
                            <p class="sc_setup__lead">
                                Right now, <strong>customers will not be emailed</strong> when
                                a delivery is fulfilled. You can change this any time in Store
                                Settings.
                            </p>
                            <label class="sc_setup__choice">
                                <input type="checkbox" id="sc_setup_notify"
                                       t-model="state.form.notification"/>
                                <span class="sc_setup__choice-label">
                                    Let Shopify email my customers about fulfillments
                                </span>
                            </label>
                            <div t-if="state.form.notification" class="sc-band sc-band--warning" role="alert">
                                <i class="fa fa-envelope sc-band__icon" aria-hidden="true"/>
                                <div class="sc-band__body">
                                    <div class="sc-band__text">Shopify will email your customers</div>
                                    <div class="sc-band__hint">
                                        Every delivery you validate in Odoo will send a Shopify
                                        shipping notification to that order's customer.
                                    </div>
                                    <label class="sc_setup__choice">
                                        <input type="checkbox" id="sc_setup_notify_confirm"
                                               t-model="state.form.notificationConfirmed"/>
                                        <span class="sc_setup__choice-label">
                                            I understand, and I want customers emailed
                                        </span>
                                    </label>
                                </div>
                            </div>
                        </t>

                        <!-- ===== first_push ===== -->
                        <t t-if="state.stepKey === 'first_push'">
                            <t t-if="isDomainEnabled('inventory')">
                                <p class="sc_setup__lead">
                                    <strong>Nothing is written to Shopify until you review and
                                    confirm the stock preview.</strong> This step only picks
                                    when the connector starts looking.
                                </p>
                                <label class="sc_setup__choice">
                                    <input type="radio" name="sc_setup_push"
                                           t-att-checked="state.form.schedulePush"
                                           t-on-change="() => this.state.form.schedulePush = true"/>
                                    <span class="sc_setup__choice-label">Schedule stock scanning now</span>
                                    <span class="sc_setup__choice-consequence">
                                        The connector starts preparing a first-push preview for
                                        you to review.
                                    </span>
                                </label>
                                <label class="sc_setup__choice">
                                    <input type="radio" name="sc_setup_push"
                                           t-att-checked="!state.form.schedulePush"
                                           t-on-change="() => this.state.form.schedulePush = false"/>
                                    <span class="sc_setup__choice-label">Do it later</span>
                                    <span class="sc_setup__choice-consequence">
                                        A first-class choice. The dashboard keeps it visible
                                        without nagging.
                                    </span>
                                </label>
                            </t>
                            <t t-else="">
                                <div class="sc-band sc-band--info" role="status">
                                    <i class="fa fa-info-circle sc-band__icon" aria-hidden="true"/>
                                    <div class="sc-band__body">
                                        <div class="sc-band__text">Not required</div>
                                        <div class="sc-band__hint">
                                            Inventory syncing is not enabled for this store, so
                                            there is no first stock push to schedule. You can
                                            enable it later in Store Settings, and this step will
                                            be waiting.
                                        </div>
                                    </div>
                                </div>
                            </t>
                        </t>

                        <!-- ===== final_readiness ===== -->
                        <t t-if="state.stepKey === 'final_readiness'">
                            <p class="sc_setup__lead">
                                Everything known to break a first sync, checked against the
                                choices you have just made. Each result says what it means,
                                who fixes it, and whether it blocks activation.
                            </p>
                            <div t-if="readiness.stale" class="sc-band sc-band--info" role="status">
                                <i class="fa fa-clock-o sc-band__icon" aria-hidden="true"/>
                                <div class="sc-band__body">
                                    <div class="sc-band__text">These results are out of date</div>
                                    <div class="sc-band__hint">
                                        Something these checks read has changed since they last
                                        ran. Run them again before activating.
                                    </div>
                                </div>
                            </div>
                            <t t-if="readiness.ran">
                                <ul class="sc_setup__checks">
                                    <t t-foreach="readiness.checks" t-as="check" t-key="check.code">
                                        <li t-att-class="checkClass(check)"
                                            t-att-data-check-state="check.state">
                                            <span class="sc_setup__check-label" t-esc="check.label"/>
                                            <!-- Text, not colour alone. All five states. -->
                                            <span class="sc-badge" t-att-class="'sc-badge--' + check.tone"
                                                  t-esc="check.state_label"/>
                                            <span class="sc_setup__check-reason" t-esc="check.reason"/>
                                            <span class="sc_setup__check-owner">
                                                Who fixes this: <t t-esc="check.owner"/>
                                            </span>
                                            <!--
                                                The one control that fixes this check,
                                                addressed by STEP KEY. Never by ordinal:
                                                a numeric deep link silently points at a
                                                different screen the next time a step is
                                                added.
                                            -->
                                            <button t-if="check.action_step_key"
                                                    class="sc-btn sc_setup_check_action"
                                                    t-att-data-step-key="check.action_step_key"
                                                    t-att-disabled="state.busy"
                                                    t-on-click="() => this.goToStep(check.action_step_key)">
                                                <span t-esc="check.action_label"/>
                                            </button>
                                        </li>
                                    </t>
                                </ul>
                            </t>
                            <p t-else="" class="sc_setup__note" role="status">
                                Not run yet.
                            </p>
                            <div>
                                <button class="sc-btn sc_setup_run_readiness"
                                        t-att-disabled="state.busy"
                                        t-on-click="() => this.runReadiness()">
                                    <span t-if="readiness.ran">Run the checks again</span>
                                    <span t-else="">Run readiness checks</span>
                                </button>
                            </div>
                        </t>

                        <!-- ===== review ===== -->
                        <t t-if="state.stepKey === 'review'">
                            <p class="sc_setup__lead">
                                Here is what this connector will do. You can change any of it
                                later in Store Settings.
                            </p>
                            <dl class="sc_setup__summary">
                                <dt>Store</dt>
                                <dd><t t-esc="store.name"/> — <t t-esc="store.shop_domain"/></dd>
                                <dt>What syncs</dt>
                                <dd>
                                    <t t-if="summary.domains.length" t-esc="summary.domains.join(', ')"/>
                                    <t t-else="">Nothing yet — the store is connected and idle.</t>
                                </dd>
                                <dt>Location mapping</dt>
                                <dd t-esc="summary.location_mapping"/>
                                <dt>Catalog source</dt>
                                <dd><t t-esc="summary.matching or 'Not chosen'"/></dd>
                                <dt>Price authority</dt>
                                <dd><t t-esc="summary.price or 'Not chosen'"/></dd>
                                <dt>Customer notifications</dt>
                                <dd t-esc="summary.notification"/>
                                <dt>First stock push</dt>
                                <dd t-esc="summary.first_push"/>
                                <dt>Readiness</dt>
                                <dd>
                                    <t t-if="!readiness.ran">Not run yet</t>
                                    <t t-elif="readiness.stale">
                                        Out of date — something these checks read has changed
                                    </t>
                                    <t t-elif="readiness.blocking.length">
                                        Blocked: <t t-esc="readiness.blocking.length"/> essential
                                        check(s) still failing
                                    </t>
                                    <t t-elif="readiness.waiting.length">
                                        Waiting: <t t-esc="readiness.waiting.length"/> check(s)
                                        have not finished
                                    </t>
                                    <t t-else="">All essential checks passed</t>
                                </dd>
                            </dl>
                            <div t-if="readiness.blocking.length" class="sc-band sc-band--danger" role="alert">
                                <i class="fa fa-exclamation-triangle sc-band__icon" aria-hidden="true"/>
                                <div class="sc-band__body">
                                    <div class="sc-band__text">These must be fixed before activating</div>
                                    <ul>
                                        <t t-foreach="readiness.blocking" t-as="check" t-key="check.code">
                                            <li><t t-esc="check.label"/> — <t t-esc="check.reason"/></li>
                                        </t>
                                    </ul>
                                </div>
                            </div>
                            <div t-if="!readiness.blocking.length and summary.domains.length === 0"
                                 class="sc-band sc-band--warning sc_setup_connect_only" role="status">
                                <i class="fa fa-info-circle sc-band__icon" aria-hidden="true"/>
                                <div class="sc-band__body">
                                    <div class="sc-band__text">
                                        No sync features are enabled. This store will connect
                                        without syncing. You can enable features later from
                                        Store Settings.
                                    </div>
                                </div>
                            </div>
                            <p class="sc_setup__note">
                                Activating does not start a sync. Nothing is written to Shopify.
                                The dashboard shows you the next explicit action.
                            </p>
                        </t>

                    </section>

                    <!-- ===== Actions =====
                         STICKY to the bottom of the viewport while long content
                         scrolls past it. On a long step the primary control used
                         to sit below the fold, so a mobile operator had to scroll
                         through the whole Permissions list to find Continue and
                         scroll back up to read the error it produced.

                         It is a normal-flow sibling AFTER the panel, so at the
                         very bottom of the page it sits in its own space and
                         covers nothing — sticky only lifts it while there is
                         still content below. Keyboard focus is kept clear of it
                         by `scroll-margin-block-end` on every focusable element
                         in this surface, which is in the stylesheet and is
                         asserted in the rendered-browser evidence rather than
                         read out of the CSS.
                    -->
                    <div t-if="!state.showCompletion" class="sc_setup__actions" role="group" aria-label="Setup navigation">
                        <button class="sc-btn sc_setup_back"
                                t-att-disabled="isFirstStep or state.busy"
                                t-on-click="() => this.back()">
                            <span>Back</span>
                        </button>
                        <button class="sc-btn sc-btn--primary sc_setup_continue"
                                t-att-disabled="state.busy"
                                t-att-aria-describedby="state.errorMessage ? 'sc_setup_error' : false"
                                t-on-click="() => this.continueStep()">
                            <span t-if="isLastStep">Activate</span>
                            <span t-else="">Continue</span>
                        </button>
                        <button t-if="store.id" class="sc-btn sc_setup_exit"
                                t-att-disabled="state.busy"
                                t-on-click="() => this.saveAndExit()">
                            <span>Save &amp; Exit</span>
                        </button>
                    </div>
                </t>
            </div>
        </div>
    </t>

</templates>
