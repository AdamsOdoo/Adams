# .claude — Claude Code project assets

**Purpose:** project-scoped Claude Code assets (skills and agents) for the Odoo
19 Shopify Connector project.

**What belongs here (later):** narrow, safe, **docs-only or read-only** skills
(`skills/`) and agents (`agents/`) that remove repeated prompt overhead and
enforce the project's citation, claim-classification, and handoff rules.

**What does not belong here yet:** **any active skill or agent.** See the
recommendation in [`skills/README.md`](./skills/README.md) and
[`agents/README.md`](./agents/README.md).

**Current status:** Placeholders only. Governance authority is the root
`CLAUDE.md`; proposed future agents are catalogued in the root `AGENTS.md`
(marked proposed). No active automation exists in this branch by design.
