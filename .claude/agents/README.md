# .claude/agents — (deferred)

**Purpose:** project-scoped Claude Code **agents** — scoped subagents for
recurring research lenses (competitor research, Shopify API, Odoo architecture,
UX benchmark, QA review, prompt control). Proposed roles are catalogued in the
root `AGENTS.md`.

**What belongs here (later):** narrow, **read-only (or docs-only)** agents with
least-privilege tools and the project's citation/handoff rules embedded.

**What does not belong here yet:** functioning agents.

## Recommendation: defer

> **Skills and agents should be created later, after the research workflow
> stabilizes. Do not create active skills or agents now, because premature
> automation may encode weak assumptions** before the methodology and feature
> taxonomy are settled.

Each proposed agent in the root `AGENTS.md` is marked **Proposed**. An agent
moves to **Active** only after the research workflow it supports is stable and
ChatGPT approves its purpose, scope, and guardrails.

**Current status:** Empty placeholder. No active agents defined in this branch.
