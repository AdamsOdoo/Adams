# Handoff

Current state of in-progress work, for the next session or the other model. Overwrite it; don't append history (git keeps it). Keep it under about 40 lines.

- Goal: build the new single module `executive_dashboard` ("Executive Dashboard") to the agreed redesign, plug and play on any Odoo 19 (Odoo.sh Enterprise) database.
- Start by reading: `docs/executive-dashboard/implementation-brief.md` (what, how, phases), `docs/executive-dashboard/build-log.md` (Phase 1 notes), then `docs/executive-dashboard/ux-improvement-plan-20260925.md` (rev 3) and `docs/executive-dashboard/reference/executive-360-concept.html` (visual source of truth).
- Branch: `feature/executive-dashboard-phase1` (from `feature/executive-dashboard-report-first`). Continue there or branch from it. Never push to main, staging or test.
- Harness and Odoo: odoo-harness 1.2.1 @ 176f07317e5a; Odoo 19.0 @ 8d05257d83f9 (Community locally; Enterprise-only parts verified on Odoo.sh).
- Done: Phase 1 (shell, Welcome, side panel, `get_section` framework and caches) and Phase 2 (Finance: figures, 12-month chart, Bank & Cash, Receivables/Payables Aged + Expected, drawers, Open in Odoo, report-line overrides in Settings; module 19.0.1.1.0). 23 tests pass; EN/AR shots ok; reviewer pass + re-check. Notes and the Odoo.sh (Enterprise) checklist: `docs/executive-dashboard/build-log.md`.
- Framework for the next phases:
  - server: implement `_section_<key>(scope)` in `models/sections/<key>.py` returning `{widget_key: payload}`; `scope` has `date_from`, `date_to`, `today`, `company`, `companies`, `period`. Drawers are `_drawer_<section>_<name>(args)` (key `"<section>.<name>"`) returning `{title, sub, rows: [{label, sub, value, open: {key, args, crumb}}], total, action: {key, args}, dest}`; native actions are `_action_<section>_<name>(args)`. Validate every client argument server-side (see `_fin_args`, `_fin_account` in `finance.py`). Pass the period to drawers as `{period, date_from, date_to}` and rebuild it with `_period_scope` (see `_fin_scope`). A section can require groups via `SECTIONS[...]['groups']`.
  - client: `static/src/sections/<key>.js` + `.xml` registering a component in `executive_dashboard.sections` (props `data`, `openPanel`); reuse `widgets/charts.js` (`LineChart`, `ColumnChart`) and `widgets/format.js` (`compact`, `whole`, `percent`). `sections/finance.*` is the worked example. CSS classes are the reference's with an `ed-` prefix (already in `dashboard.scss`).
- Next action (exact first step of Phase 3): run `.odoo-harness/oh src "delivery_status = fields.Selection" --module sale_stock` and `.odoo-harness/oh src "invoice_status = fields.Selection" --module sale`, then write `_section_sales` in `addons/executive_dashboard/models/sections/sales.py`: KPIs Invoiced sales (posted `out_invoice`/`out_refund`, `amount_untaxed_signed` in the period, `_read_group`), Confirmed orders (`sale.order` `state = 'sale'`, `date_order` in period), Open quotations (`state in ('draft','sent')`), Orders to invoice (`invoice_status = 'to invoice'`), with a test comparing Invoiced sales to the same posted invoices on demo data. Then the monthly chart, salespeople (`invoice_user_id`), top products by quantity (`product_id` + `product_uom_id`), top customers by payments (`account.payment` inbound), recent orders with the native `delivery_status` (only when `sale_stock` is installed) and its `picking_ids` drawer; then CRM (brief rows). Note: `oh test` installs only the module's dependencies (`web`, `account`), so Sales/CRM tests must skip cleanly when `sale.order` is absent, and to run them locally install `sale_stock,crm` into the kept DB (`oh test executive_dashboard --keep`, then the harness venv's `odoo-bin -d <db> -i sale_stock,crm --stop-after-init`, then `odoo-bin -d <db> -u executive_dashboard --test-tags /executive_dashboard --stop-after-init`); record which tests ran in the build log.
- Verification policy (owner): light. oh src before APIs; oh test per phase; one oh shot per phase (EN/AR); no ledgers, sweeps or evidence bundles; build-log line per phase; odoo-reviewer after Finance and at the end.
- Local screenshots with every section: after `oh test executive_dashboard --keep`, install `sale_stock,crm,purchase_stock,hr_attendance,hr_holidays` into the kept DB with the harness venv's `odoo-bin -i ... --stop-after-init`, then `oh shot /odoo/executive`.
- Old modules `adams_executive_dashboard` and `adams_dashboard_finance` (19.0.1.8.0, draft PR #214) stay untouched until Phase 6 (data move + uninstall on a staging copy).
- Owner decisions still open: none for Phase 3.
- Tried and failed (don't repeat): nested template literals in JS (the asset minifier drops their inner spaces; use concatenation); putting `grid-template-columns` on the high-specificity `.o_action_manager > .ed-app.o_action` selector (breakpoints stop applying); `oh i18n` export from a DB without Arabic loaded (drops existing msgstr: merge with polib); `.po` occurrences without the `code:` prefix (the loader rejects the file); Community `web.assets_web_dark` does not make pages dark (only web_enterprise sets the dark scheme); `:dir(rtl)` never matches in the Odoo web client (use `body.o_rtl`); running browser checks in parallel with Arabic checks corrupts both (they switch the admin language); CSS `min()` with mixed units or `var()` must be escaped (`#{"min(...)"}`) or the whole backend bundle fails to compile; `html .o_web_client > .o_action_manager > .o_action` forces `display:flex` on the action root (the dashboard overrides it with a 4-class selector); rtlcss already mirrors physical properties in the Arabic bundle, so do not add `.o_rtl` overrides for padding/left/right/translateX.
