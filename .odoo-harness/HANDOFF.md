# Handoff

Current state of in-progress work, for the next session or the other model. Overwrite it; don't append history (git keeps it). Keep it under about 40 lines.

- Goal: build the new single module `executive_dashboard` ("Executive Dashboard") to the agreed redesign, plug and play on any Odoo 19 (Odoo.sh Enterprise) database.
- Start by reading: `docs/executive-dashboard/implementation-brief.md` (what, how, phases), `docs/executive-dashboard/build-log.md` (notes per phase), then `docs/executive-dashboard/ux-improvement-plan-20260925.md` (rev 3) and `docs/executive-dashboard/reference/executive-360-concept.html` (visual source of truth; People markup in `pages.people`, drawers `employees`, `attendance`, `leave`, `shifts`, `dept:`, `emp:`, and the directory host `#empHost`).
- Branch: `feature/executive-dashboard-phase1`. Continue there. Never push to main, staging or test.
- Harness and Odoo: odoo-harness 1.2.1 @ 176f07317e5a; Odoo 19.0 @ 8d05257d83f9 (Community locally; Enterprise-only parts verified on Odoo.sh).
- Done: Phase 1 (shell, Welcome, side panel, framework), 2 (Finance), 3 (Sales and CRM), 4 (Procurement and Inventory; module 19.0.1.3.0, commit `912c1e8`). Local: 51 tests pass with `sale_stock,crm,purchase_stock,stock_account`; EN/AR shots ok; 12/12 browser checks. No reviewer pass this phase (brief: after Finance and at the end).
- Owner decision (2026-09-25): invoiced sales keep the accounting date. Phase 6 must add one line to Definitions: Odoo's Invoice Analysis uses the invoice date and can differ at month ends (recorded in the brief and the build log).
- Framework (unchanged):
  - server: `_section_<key>(scope)` in `models/sections/<key>.py`; drawers `_drawer_<section>_<name>(args)`, native screens `_action_<section>_<name>(args)`; validate every client argument (`_positive_id(args, key, optional)`). Shared helpers on the model: `_in_period(fname, scope)` / `_utc_bounds(d1, d2)` (user's days as UTC), `_money(scope, [(currency, company, amount)], day)`, `_fin_convert`, `_ranked(...)` (company-currency measures only), `_window(xmlid, name, model, domain, res_id=None)`, `_sal_can_read(model)`, `_sal_selection(model, field, value)`.
  - client: `static/src/sections/<key>.js` + `.xml` registered in `executive_dashboard.sections`; custom drawer bodies in `executive_dashboard.drawers`. Server-paged widgets fetch pages with `get_drawer('<section>.<name>', args)` (example `StockReport` in `inventory.js`). Owl templates cannot call globals (`String`, `Number`).
- Next action (exact first step of Phase 5): run `.odoo-harness/oh src "check_in = fields|check_out = fields|worked_hours = fields" --module hr_attendance`, `.odoo-harness/oh src "state = fields.Selection|date_from = fields|date_to = fields|holiday_status_id = fields" --module hr_holidays` and `.odoo-harness/oh src "department_id = fields" --module hr` (note: in Odoo 19 `department_id` is defined on `hr.version`; check whether `hr.employee.department_id` is stored before grouping on it), then write `_section_people` in `addons/executive_dashboard/models/sections/people.py` (currently an empty stub; `SECTIONS['people']` needs only `hr.employee`): KPIs Headcount (active `hr.employee`), Checked in today (`hr.attendance` with `check_in` in today via `_utc_bounds(today, today)`, still in = no `check_out`), On time off today (`hr.leave` `state = 'validate'` overlapping today), Shifts today (`planning.slot` published overlapping today; `None` when `planning` is absent: Enterprise); widgets Attendance today (first check-in, last check-out, `worked_hours` per employee), Time off today and next 7 days, Shifts by time slot, Headcount by department, Employees directory (server-paged like the stock report); with a test that Checked in today equals the native `hr.attendance` count for today. Install `hr_attendance,hr_holidays` in the kept DB for local tests.
- Local test setup: `oh test executive_dashboard --keep`, then with the harness venv `python <odoo>/odoo-bin -c ~/.cache/odoo-harness/projects/adams-2a91192c/odoo.conf --addons-path=<odoo>/addons,addons -d <kept db> -i sale_stock,crm,purchase_stock,stock_account,hr_attendance,hr_holidays --stop-after-init`, then the same with `-u executive_dashboard --test-tags /executive_dashboard --stop-after-init`. Tests for apps not in `depends` must skip when the model is absent (`SalesCrmCase.required`). `oh shot` and `oh i18n` refuse a DB built before the latest source change (including `.po` edits): rebuild with `oh test --keep`, reinstall the apps, then shoot. Sections have no URL; drive them with Playwright (`/opt/node22/lib/node_modules/playwright`, Chromium at `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`), clicking `button[data-sec=<key>]:visible`; restart `oh serve` after JS/SCSS edits (assets are cached).
- Verification policy (owner): light. oh src before APIs; oh test per phase; one oh shot per phase (EN/AR); no ledgers, sweeps or evidence bundles; build-log line per phase; odoo-reviewer at the end.
- Old modules `adams_executive_dashboard` and `adams_dashboard_finance` stay untouched until Phase 6.
- Owner decisions still open: none for Phase 5.
- Tried and failed (don't repeat): nested template literals in JS (minifier drops spaces; concatenate); `grid-template-columns` on `.o_action_manager > .ed-app.o_action`; `oh i18n` alone (drops existing msgstr: merge with polib from the previous file); `.po` occurrences without `code:`; Community dark bundle; `:dir(rtl)` (use `body.o_rtl`); parallel browser checks with Arabic ones; unescaped CSS `min()` with mixed units; `.o_rtl` overrides for physical properties (rtlcss already mirrors them); `self.env.tz.utc` (use `pytz.utc`); `assertQueryCount` keyed by the `with_user` login (it counts under `__system__`); `String()` in Owl templates; heredocs in Bash (the harness guard rejects them: write files with the editor).
