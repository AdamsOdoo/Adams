# Handoff

Current state of in-progress work, for the next session or the other model. Overwrite it; don't append history (git keeps it). Keep it under about 40 lines.

- Goal: build the new single module `executive_dashboard` ("Executive Dashboard") to the agreed redesign, plug and play on any Odoo 19 (Odoo.sh Enterprise) database.
- Start by reading: `docs/executive-dashboard/implementation-brief.md` (what, how, phases), `docs/executive-dashboard/build-log.md` (notes per phase), then `docs/executive-dashboard/ux-improvement-plan-20260925.md` (rev 3) and `docs/executive-dashboard/reference/executive-360-concept.html` (visual source of truth; Procurement/Inventory markup in `pages.procurement` / `pages.inventory`, drawers `approve`, `late`, `today`, `week`, `wait`, `product`).
- Branch: `feature/executive-dashboard-phase1`. Continue there. Never push to main, staging or test.
- Harness and Odoo: odoo-harness 1.2.1 @ 176f07317e5a; Odoo 19.0 @ 8d05257d83f9 (Community locally; Enterprise-only parts verified on Odoo.sh).
- Done: Phase 1 (shell, Welcome, side panel, framework), Phase 2 (Finance), Phase 3 (Sales and CRM; module 19.0.1.2.0). Local: 37 tests pass with `sale_stock,crm` installed; EN/AR shots ok; 12/12 browser checks. No reviewer pass this phase (brief: after Finance and at the end).
- Framework (unchanged; Sales/CRM added shared helpers on the model):
  - server: `_section_<key>(scope)` in `models/sections/<key>.py`; drawers `_drawer_<section>_<name>(args)`, native screens `_action_<section>_<name>(args)`; validate every client argument (`_positive_id(args, key, optional)` in `sales.py`). Helpers usable by every section: `_in_period(fname, scope)` / `_utc_bounds` (datetime fields in the user's timezone), `_money(scope, [(currency, company, amount)], day)`, `_ranked(scope, model, domain, key, measure, limit, day)` (top N in company currency, multi-company safe), `_window(xmlid, name, model, domain, res_id=None)` (native action with a plain fallback), `_fin_convert`.
  - client: `static/src/sections/<key>.js` + `.xml` registered in `executive_dashboard.sections`; custom drawer bodies in `executive_dashboard.drawers` (example `SalesOrderDrawer`). `widgets/format.js` now also has `periodLabel`, `avatar`, `deliveryChip`; tables use `t-att-data-label` with `_t` labels (static `data-label` is not translated).
- Next action (exact first step of Phase 4): run `.odoo-harness/oh src "state = fields.Selection" --module purchase`, `.odoo-harness/oh src "value = fields" --module stock_account` and `.odoo-harness/oh src "warehouse_id = fields" --module stock` (note: `stock.quant.warehouse_id` is a non-stored related and `stock.quant.value` is non-stored and manager-only, so group quants by `location_id` / `location_id.warehouse_id` and find the Odoo 19 valuation source before using it), then write `_section_procurement` in `addons/executive_dashboard/models/sections/procurement.py`: KPIs Purchases confirmed (`purchase.order` `state = 'purchase'`, `date_approve` in period via `_in_period`, `amount_untaxed` converted with `_money`), To approve (`state = 'to approve'`), Late receipts (incoming `stock.picking` not done/cancelled with `scheduled_date` < now), Open purchase value; widgets Waiting for approval (oldest first), Late receipts, Top suppliers (`_ranked`), Purchases by month; with a test comparing Purchases confirmed to the native orders. Then Inventory: KPIs and Deliveries/Receipts tiles from `stock.picking` (`picking_type_code`, `state`, `scheduled_date`), and the stock report (`stock.quant` `_read_group` on internal locations by product and warehouse, filters warehouse / category / name-or-reference, "Hide zero and negative stock" on by default, server-side paging with count).
- Local test setup: `oh test executive_dashboard --keep`, then with the harness venv `python <odoo>/odoo-bin -c ~/.cache/odoo-harness/projects/adams-2a91192c/odoo.conf --addons-path=<odoo>/addons,addons -d <kept db> -i sale_stock,crm,purchase_stock --stop-after-init`, then the same with `-u executive_dashboard --test-tags /executive_dashboard --stop-after-init`. Tests for apps not in `depends` must skip when the model is absent (see `SalesCrmCase.required`). `oh shot` refuses a DB built before the latest source change: rebuild with `oh test --keep` first. Sections have no URL; drive them with Playwright (global `playwright` under `npm root -g`, Chromium at `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`), clicking `button[data-sec=<key>]:visible`.
- Verification policy (owner): light. oh src before APIs; oh test per phase; one oh shot per phase (EN/AR); no ledgers, sweeps or evidence bundles; build-log line per phase; odoo-reviewer at the end.
- Old modules `adams_executive_dashboard` and `adams_dashboard_finance` stay untouched until Phase 6.
- Owner decisions still open: none for Phase 4.
- Tried and failed (don't repeat): nested template literals in JS (minifier drops spaces; concatenate); `grid-template-columns` on `.o_action_manager > .ed-app.o_action`; `oh i18n` alone (drops existing msgstr: merge with polib from the previous file); `.po` occurrences without `code:`; Community dark bundle; `:dir(rtl)` (use `body.o_rtl`); parallel browser checks with Arabic ones; unescaped CSS `min()` with mixed units; `.o_rtl` overrides for physical properties (rtlcss already mirrors them); `self.env.tz.utc` (use `pytz.utc`); `assertQueryCount` keyed by the `with_user` login (it counts under the test's own user, `__system__`).
