# Handoff

Current state of in-progress work, for the next session or the other model. Overwrite it; don't append history (git keeps it). Keep it under about 40 lines.

- Goal:
- Acceptance criteria: A1 … → test `TestX.test_a1` / A2 …
- Branch and head commit:
- Harness and Odoo: odoo-harness <version> @ <commit> (`.odoo-harness/lock.json`); Odoo <commit> (`oh env`)
- Uncommitted work: none
- Done:
- Remaining:
- Next action (exact command or step):
- Blocked on (who or what):
- Evidence (`oh evidence`, candidate digest …): `test-<modules>.json` PASSED, intact, matches HEAD; `upgrade-…`; `shot-…`; Odoo.sh build: …
- Review: independent (odoo-reviewer at <commit>) | self-review | not yet
- Tried and failed (don't repeat):
- Assumptions awaiting the user's confirmation:
- Outcome so far: completed | blocked | not verified
