# Handoff

Current state of in-progress work, for the next session or the other model. Overwrite it; don't append history (git keeps it). Keep it under about 40 lines.

- Goal: build the new single module `executive_dashboard` ("Executive Dashboard") to the agreed redesign, plug and play on any Odoo 19 (Odoo.sh Enterprise) database.
- Start by reading: `docs/executive-dashboard/implementation-brief.md` (what, how, phases), `docs/executive-dashboard/build-log.md` (notes per phase), then `docs/executive-dashboard/ux-improvement-plan-20260925.md` (rev 3) and `docs/executive-dashboard/reference/executive-360-concept.html` (visual source of truth; Needs attention = the `attention` array and drawer `attention`, Definitions = drawer `definitions`, quick-access buttons `data-open="attention"`/`"definitions"` in the rail and topbar).
- Branch: `feature/executive-dashboard-phase1`. Continue there. Never push to main, staging or test.
- Harness and Odoo: odoo-harness 1.2.1 @ 176f07317e5a; Odoo 19.0 @ 8d05257d83f9 (Community locally; Enterprise-only parts verified on Odoo.sh).
- Done: Phases 1–5, the Planning shifts fix (`3df3f98`) and the owner decisions of 2026-09-25 (`7677ff0`, module 19.0.1.5.0): one access group Administrator with elevated reads limited to the user's companies, native-screen buttons only when the user can open them, no Finance report-line settings, Sales order panel + Deliveries + Recent quotations, buttons "Open record / Open list / Open report". Then the Finance usability changes and UAT preparation (19.0.1.6.0, build log). Local: 67 tests pass with `sale_stock,crm,purchase_stock,stock_account,hr_attendance,hr_holidays`; Sales browser check EN/AR ok. Planning and the Enterprise report path are listed under "Verify on Odoo.sh". Owner staging feedback of 2026-09-26 (19.0.1.7.0): eight points, tracked in `docs/executive-dashboard/owner-feedback-20260926.md` (build log "Owner feedback on staging"); independent review, 9 findings fixed; local 32 passed (5 skipped), with all Community apps 73 passed. Verify on Odoo.sh: Aged totals/sign for payables, native screens at the period end, dark mode. Round 2 (19.0.1.8.0): nine points (chips clickable, View all, Show more paging via `get_drawer(key, args, limit)`, fixed table columns, new icon, General Ledger for accounts, one panel per P&L figure `finance.profit`), tracked in the same file; local 34 passed (5 skipped), all Community apps 78 passed. Verify on Odoo.sh: General Ledger opens unfolded on the account.
- Owner decisions (2026-09-25): invoiced sales keep the accounting date (Phase 6 adds one Definitions line: Odoo's Invoice Analysis uses the invoice date and can differ at month ends); access, finance, sales and wording decisions are in the brief ("Owner decisions (2026-09-25, after Phase 5)") and the build log.
- Framework (unchanged):
  - access: public methods call `_authorize()` (group `executive_dashboard.group_admin`), then `_elevated()` (validates `env.companies`, returns `sudo()`); every domain filters on `scope['companies']`; records by id go through `_check_company(record)`; native actions are filtered by `_open_info(action)` / `_can_list(model, domain)` with the user's own rights (`_user_env()`), and a drawer's `action` gets `kind` for the button label. There is no "restricted" status any more.
  - server: `_section_<key>(scope)` in `models/sections/<key>.py`; drawers `_drawer_<section>_<name>(args)`, native screens `_action_<section>_<name>(args)`; validate every client argument (`_positive_id(args, key, optional)`). Shared helpers on the model: `_in_period(fname, scope)` / `_utc_bounds(d1, d2)` (user's days as UTC), `_money(scope, [(currency, company, amount)], day)`, `_fin_convert`, `_ranked(...)` (company-currency measures only), `_window(xmlid, name, model, domain, res_id=None)`, `_sal_can_read(model)`, `_sal_selection(model, field, value)`; People: `_ppl_can(widget)` (app installed + rights), `_ppl_status(scope, employees)` (today's in/out/off/none).
  - client: `static/src/sections/<key>.js` + `.xml` registered in `executive_dashboard.sections`; custom drawer bodies in `executive_dashboard.drawers`. Server-paged widgets fetch pages with `get_drawer('<section>.<name>', args)` (example `StockReport` in `inventory.js`). Owl templates cannot call globals (`String`, `Number`).
- Next action (exact first step of Phase 6): add `get_attention()` to `addons/executive_dashboard/models/dashboard.py` returning items `{severity: crit|warn|info, title, sub, value, section, drawer: {key, args}}` ranked by severity, then value, built only from sections whose `_section_status` is `ok` and from the existing helpers (no new queries where one exists): late deliveries `_inv_pickings_summary(scope, 'outgoing', 'late', 0)`; receipts late `_inv_pickings_summary(scope, 'incoming', 'late', 0)`; purchase orders to approve `_pro_orders_kpi(scope, _pro_approve_domain(scope))`; receivables over 90 days and vendor bills due in 7 days from the Finance journal-item helpers in `models/sections/finance.py` (read them first; reuse the Expected/Aged bucket functions); employees not checked in (active employees minus checked-in minus on time off today, only when `_ppl_can('attendance')`; add drawer `people.not_in`); opportunities past `date_deadline` (`crm.lead` pending, `oh src "date_deadline = fields" --module crm`). Each item opens the section's existing drawer. Build it through the public-method pattern (`_authorize`, then `_elevated()`). Then the client: a Needs attention side-panel kind (rail and topbar buttons, reference `attention`), a test per item against the native count, and one query-count limit. After that: Definitions (with the invoice-date line), search model list in Settings, performance tests (payload < 300 ms, drawer < 200 ms), Arabic review, phone check, final `odoo-reviewer` pass.
- Local test setup: `oh test executive_dashboard --keep`, then with the harness venv `python <odoo>/odoo-bin -c ~/.cache/odoo-harness/projects/adams-2a91192c/odoo.conf --addons-path=<odoo>/addons,addons -d <kept db> -i sale_stock,crm,purchase_stock,stock_account,hr_attendance,hr_holidays --stop-after-init` (venv python: `~/.cache/odoo-harness/venvs/py3.12.3-*/bin/python`; Odoo source `~/.cache/odoo-harness/src/odoo-8d05257d83f9`; `oh serve` listens on 8069), then the same with `-u executive_dashboard --test-tags /executive_dashboard --stop-after-init`. Tests for apps not in `depends` must skip when the model is absent (`SalesCrmCase.required`). `oh shot` and `oh i18n` refuse a DB built before the latest source change (including `.po` edits): rebuild with `oh test --keep`, reinstall the apps, then shoot. Sections have no URL; drive them with Playwright (`/opt/node22/lib/node_modules/playwright`, Chromium at `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`), clicking `button[data-sec=<key>]:visible`; restart `oh serve` after JS/SCSS edits (assets are cached).
- Verification policy (owner): light. oh src before APIs; oh test per phase; one oh shot per phase (EN/AR); no ledgers, sweeps or evidence bundles; build-log line per phase; odoo-reviewer at the end.
- Old modules removed from the repository (2026-09-25). UAT candidate 19.0.1.6.0 on branch `uat/executive-dashboard` (build log, "UAT candidate"); the owner moves `staging` to it. Later fixes: commit here, then refresh that branch the same way (staging content + `addons/executive_dashboard` from this branch). Phase 6 no longer includes a data move.
- Owner decisions still open: none for Phase 6.
- Tried and failed (don't repeat): nested template literals in JS (minifier drops spaces; concatenate); `grid-template-columns` on `.o_action_manager > .ed-app.o_action`; `oh i18n` alone (drops existing msgstr: merge with polib from the previous file); `.po` occurrences without `code:`; Community dark bundle; `:dir(rtl)` (use `body.o_rtl`); parallel browser checks with Arabic ones; unescaped CSS `min()` with mixed units; `.o_rtl` overrides for physical properties (rtlcss already mirrors them); `self.env.tz.utc` (use `pytz.utc`); `assertQueryCount` keyed by the `with_user` login (it counts under `__system__`); `String()` in Owl templates; Playwright language switch without logging out again (the session keeps the old direction: log out and in after writing `lang`); writing `lang` from `odoo-bin shell` while `oh serve` runs (restart it: the server caches the user).
