# Sync Engine — Evidence Map (Task 006A)

> Claim-by-claim evidence map. Research phase only — no architecture decision
> is made here. Source IDs (`R#`, `S#`, `O#`, `Q#`, `E#`, `C#`) refer to
> [`sync-engine-source-inventory.md`](./sync-engine-source-inventory.md);
> full discussion of each is in
> [`sync-engine-source-notes.md`](./sync-engine-source-notes.md). Confidence
> is **High** (multiple corroborating Primary sources, or one Primary source
> directly on point with no contradiction) / **Medium** (Primary source
> exists but leaves material detail open, or is this session's own
> well-grounded inference) / **Low** (thin, indirect, or contested evidence).
>
> **Revision note (2):** revision 1 updated this branch against latest
> `Shopify-connector` after PR #123 and PR #124 merged, added `R31`
> cross-references (rows 1, 9, 19, 20), and corrected overclaim/overstated-
> confidence wording in rows 19 and 20. **This revision** updates again
> after PR #126 merged, adding `R33`
> (`docs/01-research/sync-engine-queue-idempotency-source-notes.md`) — the
> canonical queue/idempotency/retry/backoff/dead-letter reference shard for
> this task family — cross-referenced in rows 6, 7, 9, 12, 13, 14, and 20,
> plus two new supplementary rows (25, 26) for findings `R33` surfaced that
> this package had not independently researched. See
> [`sync-engine-source-notes.md`](./sync-engine-source-notes.md)'s
> "Companion Task 006A shards" note and the source inventory's "Synthesis
> hierarchy" note for the full explanation of how this package, `R31`,
> `R32`, and `R33` relate.

## Part A — Mandatory claims

| # | Claim / requirement | Evidence source | Evidence type | Confidence | Core engine? | Domain module? | Design implication | Open question / caveat |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | Sync engine must be domain-neutral. | `R12` §A.5/§K.2 (accepted blueprint), `R4` (working extension-seam precedent), `R14` (structural rules), `R31` (PR #124's independent confirmation that `job.job_type` today has exactly three values, all core/diagnostic, and that no domain-neutral operation-handler registry exists yet, distinct from the readiness-check seam) | Accepted decision + repo code | High | Yes | Partial (consumes it) | Core owns job/queue/log dispatch; domain modules register via extension seams (job-type `selection_add`, webhook-topic registration) — core never imports domain logic. **This is the accepted design intent, not a claim that the mechanism is fully built** — `R31` independently confirms no domain-neutral handler registry for sync *operations* exists yet (only the readiness-*check* registry, a related but distinct pattern). | No doc explicitly names the readiness-check `_get_checks()` seam as *the* template for job-type registration — that cross-reference is this session's own **[Inference]**, not a stated repo claim. |
| 2 | Domain modules must not implement separate queue/retry/log engines. | `R21` RA-013 (binding rejected-approach), `R12` §K.2, `R14` | Accepted decision (rejected-approaches log) | High | Yes | No (forbidden) | Any domain job type is added via `selection_add` on the existing `job_type` field; no parallel job/log/error-class model may be created. | None material. |
| 3 | Store state gating from Task 005 must apply to sync jobs. | `R1` (`create()`/`write()` gating on `BUSINESS_JOB_SOURCES`), `R3` (`action_disconnect()` cancellation sweep), `R18` (live Odoo.sh validation, 0 failures) | Repo code, live-validated | High | Yes | Partial (automatically inherited) | Gating keys on `job_source` (fixed vocabulary: `webhook/manual_sync/scheduled_sync/reconciliation/odoo_event`), **not** on `job_type` (which domain modules extend via `selection_add`) — so any future domain job is gated automatically with no per-domain gating code needed. | Whether the disconnect-cancellation sweep fully closes the race against a job already `running` inside an in-flight `ir.cron` batch at the exact instant of disconnect is **not proven by any source** — would require live Odoo-runtime evidence. |
| 4 | Manual, scheduled, and webhook-triggered sync must share the same execution path. | `R7` DEC-005 (webhook enqueues → `ir.cron` drains → manual trigger, one queue model), `R1` (`job_source` values share one state machine), `R14` | Accepted decision + repo code | High | Yes | Partial | All three trigger types create `job_source`-tagged `shopify.connector.job` rows processed by the same drain mechanism; only enqueue-time context differs. | The `ir.cron` drain-loop implementation itself does not yet exist — this is a Task 006B-territory design question, correctly out of scope for this research-only session. |
| 5 | Webhooks should enqueue/signal work rather than perform complex mutations directly. | `R7` DEC-005 ("every webhook/quick action enqueues... heavy sync work never runs inline"), `R12` §A.5 seam 7, `S13`/`S14` (Shopify's own 1s/5s fast-ack requirement + delivery-not-guaranteed), `R20` MBQ-65 (enqueue-only, never-direct-write, mandatory follow-up authoritative read — accepted posture) | Accepted decision + official Shopify fact + accepted MBQ resolution | High | Yes | Partial (the follow-up read/write logic is domain-specific) | Webhook controller's only job is HMAC verify + `X-Shopify-Webhook-Id` dedupe + enqueue; the authoritative follow-up read and any write happen inside the job handler, never inline in the webhook request. | None material — already decided at posture level for products (MBQ-65); inventory webhooks are explicitly **not implemented** in Phase 1 at all (MBQ-63). |
| 6 | Idempotency is mandatory. | `R7` DEC-005, `R8` DEC-009 (layered idempotency), `R1` (`idempotency_key` field, implemented), `S16` (Shopify's own 24h-TTL mechanics), `S14` ("Process webhooks using idempotent operations"), `R33` (independently converging cross-vendor idempotency-key research: Stripe/IETF/AWS all use a client-generated, high-entropy token bound to the (client, operation, payload) tuple, store-and-replay on retry) | Accepted decision + repo code + official Shopify fact + independently-produced reference shard | High | Yes | Yes (per-mutation keys, binding-level dedup are domain-specific) | Layered design: webhook `Webhook-Id` dedup (core) + connector `idempotency_key` (core) + Shopify `@idempotent` per-mutation keys (domain) + binding-key dedup (domain, DEC-006). | The exact current `@idempotent` mutation *count* is disputed (16 vs 17 — see supplementary row 22 below); does not affect the core-engine-level claim. `R33` additionally surfaces that vendors **disagree** on how to handle a concurrent, still-in-flight duplicate request (IETF: HTTP 409; Stripe: don't cache, tell client to retry; Shopify: a distinct `IDEMPOTENCY_CONCURRENT_REQUEST` code) — no single universal shape to assume if the engine ever needs its own answer to this question. |
| 7 | Duplicate-running prevention is mandatory. | `R1` (`operation_scope_key` + unique constraint, MBQ-21 resolved), `R7` DEC-005 (per-record isolation), `O7`/`O9` (Odoo `SKIP LOCKED` locking primitives, reference for future concurrent job acquisition), `R33` (independent confirmation of the same `SKIP LOCKED`/`NOWAIT` mechanics, plus new PostgreSQL advisory-lock research — see supplementary row 25) | Repo code (implemented) + official Odoo source, independently corroborated by a second shard | High | Yes | No | `operation_scope_key`'s DB-backed unique constraint is the already-implemented guard. A future concurrent job-*acquisition* mechanism (multiple `ir.cron` workers claiming `shopify.connector.job` rows) would need its own `SKIP LOCKED`-style acquisition — `ir.cron` itself already does this for its own bookkeeping rows (`O7`, independently confirmed by `R33`'s `OD-1`), but the sync engine's job-claiming mechanism does not yet exist. | How the future drain loop acquires job rows concurrently without racing is undesigned — correctly out of scope here, flagged for the architecture gate. `R33` independently reaches the same "requires actual Odoo 19 runtime proof, not source-reading alone" conclusion as this package and `R31` — now a three-shard-corroborated open question (see row 20 and the open-questions document), plus a new, more specific open question about multi-server/load-balanced deployments `R33` surfaces that neither this package nor `R31` had named. |
| 8 | Retry must be bounded. | `R8` DEC-009, `R11` §9 (MBQ-16 resolved planning defaults: 12 attempts / exponential 30s base ×2 / capped 30 min / ±20% jitter / 24h window), `O5` (Odoo's own `retrying()` bounded at 5 tries), `E2`/`E3` (bounded retries + jitter as recognized practice), `Q2`/`Q3` (OCA `max_retries` default 5) | Accepted decision + repo planning doc + official Odoo source + engineering reference + OCA reference | High | Yes | No | Planning defaults already exist and independently corroborate recognized jittered-backoff practice; implementation must honor the bound, never retry unboundedly. | Exact constants remain **"implementation-planning default,"** not final production-tuned values, per DEC-009's own acceptance note. |
| 9 | Permanent failure/dead-letter must be visible. | `R1` (`failed_final`/`blocked_manual_review` states, implemented), `R8` DEC-009 (dead/final-failed state, accepted), `O7`/`O14` (no dead-letter/DLQ terminology or mechanism found in the `ir.cron` files/pages reviewed), `E4` (AWS SQS DLQ pattern, general reference), `R33` (substantially deeper dead-letter research — SQS/Azure Service Bus/RabbitMQ/EIP — sharpening "visible" into a distinct, opt-in design requirement) | Repo code (implemented) + official Odoo source (negative finding, scoped to sources reviewed) + engineering reference, independently deepened by a second shard | High | Yes | No | The connector must (and already does) build its own dead-letter surface — no such mechanism was found in `ir.cron`, so the connector must not rely on it for permanent-failure visibility. `ir.cron`'s own "notify admin" step is a log-line-only no-op by default (`O7`) and must not be relied on as an operator-visible signal by itself. **`R33` sharpens this claim**: across every vendor DLQ implementation it examined, visibility is implemented as a **separately-wired, opt-in alarm/metric**, never a default push notification — "a bare DLQ with no alarm/monitor wired up... provides no visibility guarantee by itself" (`R33`'s own inference, grounded in AWS/Azure facts). This repo's job/log substrate captures dead-letter *state* (already implemented); whether an alarm/dashboard is wired to surface it to an operator is a distinct, not-yet-evaluated design question this claim's "must be visible" wording implies but does not yet guarantee is met. `R33` also confirms dead-lettering is not exclusively a multi-attempt phenomenon (AWS/RabbitMQ both document single-failure triggers), relevant context for any future retry-count-to-dead-letter threshold design. | None material to the core claim; the `_notify_admin` no-op finding is a related observability caveat (see supplementary row 23). The negative finding is scoped to the specific `ir.cron` files/pages reviewed by this package (and independently by `R31`, PR #124), not asserted as a whole-Odoo-codebase claim. |
| 10 | Logs must be inspectable. | `R2` (append-only `job.log`, `event_type` vocabulary, implemented), `R7` DEC-005 (recovery-first error center), `R8` DEC-009 (user-facing log requirements) | Repo code (implemented) + accepted decisions | High | Yes | No | Already implemented; any job handler must call the existing `_system_append()` path at each transition/attempt rather than inventing a parallel logging mechanism. | None material. |
| 11 | Secrets must never be logged. | `R2` (`redact()` at the single sanctioned write path, implemented), `E9` OWASP Logging Cheat Sheet (never-log category list, corroboration) | Repo code (implemented) + engineering reference | High | Yes | No | Redaction is already centralized; any new job/log write path must route through `_system_append()`, never construct `job.log` rows directly. | None material. |
| 12 | Shopify GraphQL cost/rate limits must influence batching and retry. | `R24` (GraphQL cost model, `throttleStatus`), `S7`/`S8` (bulk operations exempt from standard rate limits), `R11` §9 (throttling gets the widest auto-retry window, honors a live wait signal), `R33` (new: a throttled GraphQL call can return **HTTP 200** with a `THROTTLED` body code, `SH-4`) | Official Shopify fact + repo planning doc + independently-produced reference shard (new fact) | High | Yes | Partial (per-query cost depends on domain-specific field selection) | Batch/page sizes and retry backoff should read the live `throttleStatus`/`Retry-After` signal rather than a fixed guess; bulk operations sidestep per-call rate limits entirely for qualifying large jobs. **New design implication from `R33`**: the client/response-parsing layer must inspect the GraphQL response **body** for a `THROTTLED` error code, not just branch on HTTP status — a client that only checks status codes (the natural pattern carried over from REST's 429 handling) would silently treat a throttled GraphQL call as successful. This package's own pre-006A rate-limit baseline had not independently verified this. | The exact per-plan GraphQL bucket size (`maximumAvailable`) is **not published** by Shopify — only restore rates are (already-known open item, `R24`). `R33` additionally logs as open: no fetched source (by this package or `R33`) documents a `Retry-After`-equivalent signal for GraphQL `THROTTLED` responses specifically — the fallback is deriving a wait time from `throttleStatus`. See supplementary row 26 for the risk framing. |
| 13 | Cursor pagination requires checkpoint/resume thinking. | `S1`/`S2` (`endCursor`/`pageInfo` mechanics), `E6`/`E7` (general checkpoint pattern, cursor-as-checkpoint), `R33` (independent corroboration of the GraphQL-cursor-durability open question, plus a new fact: REST `page_info` cursor URLs are explicitly documented as temporary/not-for-saving, `SH-16`) | Official Shopify fact + engineering reference, independently corroborated and extended by a second shard | Medium | Undecided (Yes if engine-owned) | Undecided (Yes if per-import-owned) | A sync engine touching large result sets *could* persist the last-seen `endCursor` as job state to resume later, mirroring the general checkpoint pattern — but this specific combination is this session's own synthesis, not documented by either platform. `R33` independently reached the identical conclusion via its own separate research pass. | Shopify does **not** state whether a saved cursor remains valid/reusable after a pause of unspecified length, nor guarantee ordering stability under concurrent data mutation (`S1`, `S2`) — treat resumability as **unproven safe**, not assumed. `R33` corroborates this exact gap for GraphQL specifically and adds that the **REST** equivalent (`page_info`) *is* explicitly documented as temporary ("don't recommend saving them to use later") — suggestive context for GraphQL, not proof either way. Core-vs-domain ownership of checkpoint state is also undecided. |
| 14 | Bulk operations may be useful for large imports but are not automatically MVP. | `S7`/`S8` (mechanics: whole-operation resubmission only, no partial resume), `R7` DEC-005 (does not adopt bulk ops as a Phase-1 mechanism), `R33` (independent corroboration of the same non-resumable-at-the-API-level mechanics) | Official Shopify fact + accepted decision, independently corroborated by a second shard | Medium | Partial | Yes (first-sync product import is the concrete candidate) | Bulk operations' recovery model (resubmit-the-whole-operation, no partial resume) is a materially weaker fit for the checkpoint/resume principle (claim 13) than cursor pagination — a real trade-off for a future domain-module decision, not resolved here. `R33` independently reaches the same conclusion via its own separate fetch of the same Shopify pages. | No official Shopify page explicitly recommends bulk operations "only for one-off exports, not routine sync" — this project's "not automatically MVP" framing is **this project's own design stance**, not a documented Shopify recommendation; the two should not be conflated. |
| 15 | Unit tests must not require live Shopify calls. | `R6` (existing test suite already follows this), `O2`/`O3` (`TransactionCase` savepoint-per-method; commit/rollback forbidden inside a test) | Repo code (established pattern) + official Odoo source | High | Yes | No | Sync-engine unit tests should mock/stub the Shopify API client layer (the existing pattern) and run under `TransactionCase`; no network access in the default suite. | None material. |
| 16 | External Shopify validation must be separate from unit tests. | `R18` (Task 005 validation record: "no live Shopify Admin API connection was made or attempted"), DEC-021 VAL-B2 deferral (cited, not modified) | Repo docs (accepted) | High | Yes | No | Live-Shopify validation (VAL-B2-style) is its own separately-gated activity, distinct from and not a prerequisite for the Odoo.sh live-runtime test suite already used for Tasks 001–005. | None material. |
| 17 | First-sync deduplication must be designed before product sync implementation. | `R12` §C.5 (accepted match-key priority: binding → SKU/reference → barcode → email → manual), `R23` (MBQ-59 resolved at blueprint-policy level, exact thresholds open), `R16` (task-010 proposal: exact eligibility/match-confidence thresholds open for its own final prompt) | Accepted decision (blueprint-policy level) + proposed task spec | High (that it's required) / Medium (exact mechanics) | No | Yes | Product-domain dedup *policy* is already decided; exact match-confidence thresholds are deferred to Task 010's own future final implementation prompt and must be resolved there before code is written. | Exact eligibility-check/match-confidence thresholds — open (MBQ-59 residual). |
| 18 | Lite/Full packaging requires domain enablement hooks. | `R12` §I (domain-enablement flags, accepted mechanism), `R4` (`ACCEPTED_DOMAIN_FLAGS`, implemented) — **vs.** repo-wide search finding **no "Lite/Full" packaging-tier concept in any reviewed doc** | Accepted decision (mechanism) + repo code (partial implementation) + **negative finding** (no packaging-tier concept) | Low/Medium | Yes (the flag mechanism) | Yes (each domain contributes its own flag) | The *mechanism* this claim needs — enable/disable a domain, block new enqueue when disabled, never delete history — already exists at blueprint level and is partially implemented. "Lite/Full" as a named packaging/pricing *tier* is **not** an existing concept anywhere in this repo. | Whether "Lite/Full" maps directly onto "which domain flags are enabled," or implies something additional (separate installable module sets, pricing/licensing gates), is genuinely open and requires a product-strategy (ChatGPT) clarification before any implementation assumes either shape. |

## Part B — Supplementary claims (high-value findings beyond the mandatory 18)

| # | Claim / requirement | Evidence source | Evidence type | Confidence | Core engine? | Domain module? | Design implication | Open question / caveat |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 19 | Per-record savepoint isolation (DEC-005) should be designed with PostgreSQL's own documented savepoint-performance behavior in mind. | `O8` (official Odoo coding guidelines: ">64 savepoints... PostgreSQL will slow down... limit batch size or move to a scheduled job"), independently corroborated by `R31` (PR #124, same source, same quote) | Official Odoo doc, direct quote, corroborated by a second independently-produced shard | High | Yes | No | Odoo's coding guidelines warn that PostgreSQL performance degrades when a transaction uses more than 64 savepoints, so future per-record-savepoint loops must treat this as a **performance constraint to weigh in batch sizing, not a hard functional cap** the framework enforces — `R31` additionally found that core `create()`/`write()` do not wrap themselves in a savepoint automatically; savepoints are used selectively by higher-level code. | None material — this is a documentation warning, not itself a design decision, and nothing in either shard's research found an enforced ceiling in code. |
| 20 | Whether Odoo's automatic RPC-layer serialization retry extends to a cron job's own record-processing code. | `O5`, `O6` (source-confirmed: `retrying()` governs RPC/HTTP dispatch); `O7` (`ir.cron._callback()` does not call `retrying()`, confirmed by grep — no equivalent found in the code reviewed); `R31` (PR #124, independently examined a related but distinct layer — `ir.cron`'s job-*acquisition* locking via `_acquire_one_job`/`FOR NO KEY UPDATE SKIP LOCKED`, and `_process_jobs_loop`'s rollback-and-continue on `SerializationFailure` — without itself settling the domain-record-processing question); `R33` (PR #126, a **third** independent shard, states the identical open-question framing even more explicitly: "it has not been observed running, and would need an actual concurrency test... to verify the documented intent matches real runtime behavior") | Official Odoo source (three-file synthesis) + two additional independently-produced shards examining the same/adjacent layers | Medium | Yes | No | **This is a source-backed inference, not a proven fact — now corroborated by three independently-produced research shards reaching the same open-question framing**, not merely one package's own caution. If a future sync-engine cron job hits a serialization conflict while writing domain records, it may not be transparently retried by the framework within that run based on the code reviewed — a plausible (not confirmed) design response would be for the connector's own code to lock (`lock_for_update()`) and/or implement its own retry. | Explicitly labeled **[Inference]**, relabeled in revision 1 from an earlier overstated "Fact — important negative finding"; requires live Odoo.sh runtime proof before any implementation relies on it (see open-questions document). No single sentence in any source states the domain-record-processing conclusion directly. `R33` additionally names a more specific, previously-unsurfaced open question: whether the row-lock-only approach holds with **no additional cross-server coordination** when multiple Odoo application servers share one PostgreSQL database (see supplementary row 25). |
| 21 | Webhook duplicate delivery has no documented maximum time window outside the failure-retry schedule. | `S14` ("Shopify minimizes duplicate deliveries, but your app might receive the same webhook more than once" — no bound given beyond the 8-retries/4-hours failure schedule) | Official Shopify fact | Medium | Yes | No | A webhook-dedup record must be durable (not a short-TTL cache) since Shopify does not bound the success-path duplicate window. | Genuinely undocumented by Shopify — logged as an open question, not resolved. |
| 22 | The Shopify `@idempotent` mutation list count is disputed between existing repo research and this session's fresh fetch. | `R8`/`R10` ("17 mutations," pre-006A) vs. `S16` (16 named mutations, self-dated by Shopify "2 February 2026") | Accepted decision record vs. fresh official fetch | Low (as a settled number) | Partial | Yes (inventory/fulfillment domain) | Do not hard-code either "16" or "17" into any future implementation; Shopify's own page instructs checking the live list per-mutation at build time. | **Unresolved discrepancy** — flagged prominently in the open-questions document; does not invalidate DEC-009 (which references the concept, not the count). |
| 23 | OCA `queue_job` remains reference-pattern-only; RA-004 is unchanged by this research. | `Q1`–`Q8` (full source read, this session) vs. `R21` RA-004 (binding rejection of `queue_job` as Phase-1 default); `R33` (PR #126, an independent full source read of the same module reaching the identical reference-only conclusion, plus exact-match corroboration on `DEFAULT_MAX_RETRIES=5`/`RETRY_INTERVAL=600s` and the non-terminal-only `identity_key` dedup scope) | OCA source (new this session) + accepted rejected-approach, independently corroborated by a second full source read | High | N/A (not adopted) | N/A | Nothing in this session's `queue_job` research (nor `R33`'s independent one) argues for revisiting RA-004; several of its patterns (identity-key narrow-window dedup + idempotent-body requirement, channel-as-runtime-config) are noted as **structural corroboration** of choices this repo's own accepted design already made independently. | None — RA-004's revisit conditions (named in the rejected-approaches log) are unchanged and not met by this research. One unreconciled, immaterial discrepancy: this package's pre-006A baseline (`R25`) cites `--workers > 0` as `queue_job`'s Jobrunner precondition; `R33` cites `--workers > 1`. Not resolved, does not affect RA-004. |
| 24 | `ir.cron`'s own "notify DB admin on deactivation" step has no real delivery mechanism in base Odoo. | `O7` (`_notify_admin()` base implementation is `_logger.warning(message)` only, explicitly documented as meant to be overridden) | Official Odoo source | High | Yes | No | A sync engine cannot assume an operator is actually notified of a deactivated scheduled action by Odoo core alone — this repo's own job/log dashboard (already implemented) is the real operator-visible surface, not `ir.cron`'s own admin-notify step. | None material. |
| 25 | PostgreSQL advisory locks are an alternative/complementary locking primitive to `SKIP LOCKED` row locks, with their own non-obvious hazards — new this revision, from `R33`. | `R33` (PostgreSQL's own docs, `GEN-30`/`GEN-31`: session- vs. transaction-scoped advisory locks; a documented "danger!" example where an advisory-lock call inside `SELECT ... ORDER BY ... LIMIT` can lock rows before `LIMIT` applies) | Official PostgreSQL doc, via an independently-produced reference shard | High | Yes | No | Not previously researched by this package. A future job-claiming design considering advisory locks as an alternative to `lock_for_update()`/`SKIP LOCKED` must account for: (a) session-scoped advisory locks are **not** released by a transaction rollback — persisting until explicit release or session end, a hazard for pooled-connection designs; (b) the `LIMIT`+advisory-lock ordering hazard above. | Whether a future design should use advisory locks, row-level `SKIP LOCKED`, or `operation_scope_key`-style unique constraints (or some combination) is undecided — correctly out of scope for this research-only package, flagged for the architecture gate. |
| 26 | A throttled Shopify GraphQL call returning HTTP 200 with a `THROTTLED` body code is a concrete, actionable risk for any naive HTTP-status-only client — new this revision, from `R33`. | `R33` (`SH-4`: "a throttled GraphQL call can still return HTTP 200 with a `THROTTLED` error code in the body rather than a 4xx status") | Official Shopify API reference, via an independently-produced reference shard | High | Yes | No | The sync engine's GraphQL response-handling layer must inspect the response body for a `THROTTLED` code on every call, not rely on HTTP status alone — a client written against REST's 429-based mental model would silently miscount a throttled GraphQL call as a success. | No documented `Retry-After`-equivalent signal exists for GraphQL `THROTTLED` responses specifically (`R33`); the fallback is deriving a wait time from `throttleStatus.currentlyAvailable`/`restoreRate`. |
