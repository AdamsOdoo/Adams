# Sync Engine — Source Inventory (Task 006A)

> **Status:** Research package. Documentation only. No architecture decision is
> made or implied by this file. Companion to
> [`sync-engine-source-notes.md`](./sync-engine-source-notes.md),
> [`sync-engine-evidence-map.md`](./sync-engine-evidence-map.md), and
> [`../05-qa/sync-engine-open-questions.md`](../05-qa/sync-engine-open-questions.md).
> Session access date for all newly-fetched external sources in this package:
> **2026-07-08**. Repo/internal sources retain their own original access dates
> where already dated by a prior session.
>
> **Revision note (2):** this branch's initial commit was created from PR
> #122 (`9247fea3c36afdb761a82678f3e5e66e8ef42e87`); revision 1 updated
> against latest `origin/Shopify-connector` after PR #123 and PR #124
> merged (`3735ae2292d1fcf926c83034ac8513906c9f5020`), adding `R31`/`R32`.
> **This revision** updates again after PR #126 merged
> (`3c9fad896f996c19d4978e5aca58b39674b7b35a`), adding `R33`. Four sibling
> Task 006A shards now exist, each canonical for its own scope — see the
> "Synthesis hierarchy" note directly below the source tables.

## Purpose

A source-by-source reliability grading of every input used to research the
future domain-neutral sync/queue/retry/log engine. Per `CLAUDE.md` §7/§8,
every source below is graded and dated; no fact in the companion notes/
evidence-map files should rest on a source not listed here.

## How to read the grading columns

- **Source type:** `repo code` / `repo docs` / `official Shopify` /
  `official Odoo` / `OCA` / `competitor` (via repo docs) / `engineering
  reference` / `community (non-authoritative)`.
- **Reliability grade:** `Primary` (authoritative, on-point, first-party) ·
  `Secondary` (official but tangential, or a reputable non-primary source) ·
  `Reference only` (useful pattern, not authoritative for this codebase) ·
  `Unavailable` (could not be fetched/verified).
- **Can support architecture decisions:** `Yes` / `Partial` / `No` — whether
  this source alone is strong enough to be cited in a future AR/DEC record.

---

## 1. Repository code (Primary — ground truth for "what already exists")

| # | Source | Path | Access date | Topic | Grade | Can support arch. decisions | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| R1 | `shopify_connector_job.py` | `addons/shopify_connector_core/models/shopify_connector_job.py` | 2026-07-08 | Job state machine (10 states), `job_source`/`job_type`, `BUSINESS_JOB_SOURCES`, `idempotency_key`, `operation_scope_key`, enqueue/execution store-state gating (`create()`/`write()`) | Primary | Yes | This **is** the accepted, merged core job substrate (Task 001/003/004/005, DEC-009, AR-019). Any sync-engine research must treat this schema as already-decided, not open. |
| R2 | `shopify_connector_job_log.py` | `addons/shopify_connector_core/models/shopify_connector_job_log.py` | 2026-07-08 | Append-only job-log substrate, `_system_append()` sanctioned write path, redaction-on-write | Primary | Yes | Confirms logs are redacted (`redact()`) before persistence — directly informs "secrets must never be logged" claim. |
| R3 | `shopify_connector_store.py` | `addons/shopify_connector_core/models/shopify_connector_store.py` | 2026-07-08 | Store lifecycle state machine (`setup_incomplete`/`connected`/`reconnect_needed`/`disconnected`), `action_disconnect()` non-terminal business-job cancellation sweep, `action_test_connection()`/`action_activate()`/`action_reconnect()` | Primary | Yes | The already-implemented Task 005 state-gating mechanism the sync engine must plug into (Mandatory Claim 3). |
| R4 | `shopify_connector_readiness_check.py` | `addons/shopify_connector_core/models/shopify_connector_readiness_check.py` | 2026-07-08 | Domain-extension seam pattern (`_get_checks()` override via classic Odoo inheritance), fail-closed aggregation, `ACCEPTED_DOMAIN_FLAGS` | Primary | Yes | Working precedent for "core owns dispatch, domain modules register/extend" — the same shape the job-type registration seam (§A.5 of the core-substrate blueprint) is expected to take. |
| R5 | `shopify_connector_store_credential.py` | `addons/shopify_connector_core/models/shopify_connector_store_credential.py` | 2026-07-08 | Credential state invalidation on set/replace/clear, `_get_access_token()` sanctioned `sudo()` | Primary | Yes | Confirms credential mutation invalidates `store.state` — a hard input to "store disconnects while jobs are queued" analysis. Not evidence for OAuth/token-acquisition (out of scope, MBQ-05 open). |
| R6 | `tests/` (8 files) | `addons/shopify_connector_core/tests/*.py` | 2026-07-08 | Existing `TransactionCase`-based test patterns already in use (lifecycle, credential, readiness, redaction, job-log) | Primary | Partial | Establishes the existing test-authoring convention; informs Analysis Area K (testing) but does not itself decide future sync-engine test scope. |

## 2. Repository governance / decision docs (Primary — already-accepted constraints)

| # | Source | Path | Access date (as dated by doc) | Topic | Grade | Can support arch. decisions | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| R7 | DEC-005 | `docs/04-decisions/DEC-005-sync-orchestration-strategy.md` | 2026-07-02 | **Accepted** orchestration model: webhook (HMAC + dedup) → internal queue → `ir.cron` drain, manual trigger, mandatory scheduled reconciliation, per-record savepoint isolation, retry counters, dead/final-failed state; OCA `queue_job` deferred as optional accelerator only | Primary | Yes | AR-003 accepted 2026-07-02 (confirmed via architecture-review-log, R15). This is the binding orchestration-model decision Task 006A must not re-litigate. |
| R8 | DEC-009 | `docs/04-decisions/DEC-009-error-retry-idempotency-strategy.md` | 2026-07-02 | **Accepted** classified retry policy (Option C), 16 error classes, ambiguous-outcome rule for non-`@idempotent` writes, idempotency layers | Primary | Yes | AR-006 accepted 2026-07-02. Binding error/retry/idempotency taxonomy. |
| R9 | AR-003 framing brief | `docs/03-architecture/ar-003-sync-orchestration-framing.md` | 2026-07-01/07-02 | Evidence and options behind DEC-005; `ir.cron` source facts, Shopify webhook/rate-limit facts as researched pre-006A | Primary | Yes | Superseded in decision terms by DEC-005 but retains the original evidence trail. |
| R10 | AR-006 decision brief | `docs/03-architecture/ar006-error-retry-idempotency-decision-brief.md` | 2026-07-02 | Evidence and taxonomy tables behind DEC-009 | Primary | Yes | Cites "17 `@idempotent` mutations" — **see the discrepancy noted in source-notes §Shopify idempotency** against this session's fresh recount of 16. |
| R11 | Core naming/schema planning | `docs/07-implementation-plan/core-naming-schema-planning.md` | 2026-07-05 | Exact accepted job schema (§4.5), idempotency/serialization-guard schema (§8), **retry constants and backoff policy (§9, MBQ-16 resolved as planning defaults)** | Primary | Yes | §9's 12-attempt/exponential-30s-base/×2/cap-30min/±20%-jitter/24h-window schedule is the single most load-bearing existing planning artifact for Analysis Area B. |
| R12 | Master Blueprint — Core Substrate (Part A) | `docs/03-architecture/master-blueprint-core-substrate.md` | 2026-07-03/07-04 (DEC-013 accepted) | §A.5 seven domain-extension seams (incl. job-type registration, webhook-topic registration); §D queue/job ownership; §I domain-enablement flags; §K.2 "no duplicate substrate" rule | Primary | Yes | Directly answers Mandatory Claims 1, 2, and the "Lite/Full packaging" analysis (no packaging-tier concept found, but domain-enablement mechanics are fully specified). |
| R13 | Phase 1 Domain Model Brief | `docs/03-architecture/phase1-domain-model-brief.md` | 2026-07-02 | Domain 8 (queue/log/error) — the accepted minimum job concept every other domain references rather than restates | Primary | Yes | |
| R14 | MVP Domain Implementation Sequence | `docs/07-implementation-plan/mvp-domain-implementation-sequence.md` | 2026-07-05/07-06 | Domain sequencing; explicit "no duplicated transport/queue/log/binding abstractions" structural rule; Task 005 dependency for enqueue gating | Primary | Yes | |
| R15 | Architecture Review Log | `docs/05-qa/architecture-review-log.md` | 2026-07-02 (AR-003/AR-006 acceptance patches) | Confirms AR-003 and AR-006 status = **Accepted by ChatGPT, 2026-07-02** | Primary | Yes | Load-bearing status confirmation — the sync-orchestration and error/retry/idempotency architecture is not merely proposed, it is accepted (implementation still gated separately). |
| R16 | Task 010–014 proposed specs | `docs/07-implementation-plan/task-0{10,11,12,13,14}-*-proposed.md` | 2026-07-06/07-07 | What product/customer/order/inventory/fulfillment domain tasks assume the core sync engine will already provide (job-type `selection_add` seam, shared error-class registry, no parallel logging) | Secondary | Partial | **Proposed**, not yet gate-opened — informative for "what domain modules will need," not itself a binding architecture source. |
| R17 | DEC-022 / DEC-024 (Task 005 scope/closure) | `docs/04-decisions/DEC-022-task-005-scope.md`, `docs/04-decisions/DEC-024-task-005-closure.md` | 2026-07-08 | Accepted Task 005 scope and closure; explicit non-claims (no OAuth/wizard/UI/sync authorized); "Task 006 — sync engine skeleton gate" named as one of four **unselected** next-task candidates | Primary | Yes | Establishes that this Task 006A session is research toward one *candidate* next task, not a selection of it. |
| R18 | Task 005 validation results | `docs/05-qa/task-005-validation-results.md` | 2026-07-08 | Live Odoo.sh runtime evidence; two rounds of real runtime failures a static/`py_compile`-only review missed (`write_date` freshness-guard brittleness; credential-mutation state-invalidation gap) | Primary | Yes | Directly informs the red-team question "what could cause a runtime failure similar to PR #121." |
| R19 | Technical Debt Register | `docs/05-qa/technical-debt-register.md` | 2026-07-08 | TD-001 (Resolved, target-less job idempotency-key collision pattern) and **TD-002 (Open — `read_fulfillments` readiness-scope correctness concern)** | Primary | Yes | TD-002 confirmed Open, unmodified by this session (forbidden file, cited not edited). |
| R20 | Master Blueprint Open Questions Register | `docs/03-architecture/master-blueprint-open-questions.md` | 2026-07-05/07-08 | MBQ-05 (token-acquisition, Partially routed/Open), MBQ-16 (retry/backoff, Resolved as planning defaults), MBQ-21 (`operation_scope_key`, Resolved), MBQ-06 (readiness essential-set, Resolved), MBQ-63 (inventory webhooks, not implemented Phase 1), MBQ-65 (product webhooks, enqueue-only + follow-up-read posture decided) | Primary | Yes | Confirmed MBQ-05 remains Partially routed/Open and unmodified by this session. |
| R21 | Rejected Approaches Log | `docs/05-qa/rejected-approaches-log.md` | 2026-07-02/07-07 | RA-004 (OCA `queue_job` not default), RA-013 (no per-domain duplicate queue/log/binding abstractions), RA-014/015/016/017 (retry-everything / never-retry / stack-trace-primary / binding-only-idempotency all rejected), RA-024 (write_date-as-freshness-guard rejected, ties to R18) | Primary | Yes | Checked before drafting Analysis Areas A–C per `CLAUDE.md` §10; no new rejected-approach candidate surfaced by this session (see open-questions doc). |
| R22 | Final MBQ Closure Plan | `docs/07-implementation-plan/final-mbq-closure-plan.md` | 2026-07-05 | MBQ-17 (reconciliation cadence — posture resolved, constants open), MBQ-50 (`queue_job` accelerator — accepted-open risk, containment = DEC-005 revisit triggers only) | Secondary | Partial | Closure-level planning artifact, not itself a fact source. |
| R23 | Implementation Gate Readiness Audit | `docs/05-qa/implementation-gate-readiness-audit.md` | 2026-07-05 | MBQ-59 (first-sync product dedup — resolved at blueprint-policy level, exact match-confidence thresholds open) | Secondary | Partial | |

## 3. Repository research docs — official-source syntheses (Primary as internal compilations; underlying sources are official)

| # | Source | Path | Access date (as dated by doc) | Topic | Grade | Can support arch. decisions | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| R24 | Shopify Official API Notes | `docs/01-research/shopify-official-api-notes.md` | 2026-06-30 baseline / 2026-07-01 RB-14 refresh / 2026-07-02 evidence refresh / 2026-07-04 Part E patch | REST/GraphQL strategy, rate limits, GraphQL cost model, bulk operations basics, webhooks, idempotency (pre-006A baseline) | Primary | Yes | This session's fresh fetches (§5 below) **re-verify and, in one case, correct** (25,000-object cap scope) this baseline. Where this session's fresh fetch and this doc agree, both are cited; where they diverge, the fresh fetch is noted as superseding, per the "latest dated refresh wins" convention this doc itself already uses. |
| R25 | Odoo Official Architecture Notes | `docs/01-research/odoo-official-architecture-notes.md` | 2026-06-30 baseline / 2026-07-01 RB-14 refresh / 2026-07-02 evidence refresh | `ir.cron` behavior, no core async queue, `--max-cron-threads`, `TransactionCase`, Odoo.sh best-effort cron, OCA `queue_job` community-tier note | Primary | Yes | This session's fresh fetches (§6 below) go substantially deeper (transaction/rollback source code, concurrency/locking — a genuine gap this baseline did not cover). |
| R26 | Shopify Token Acquisition Notes | `docs/01-research/shopify-token-acquisition-notes.md` | 2026-07-06/07-07 | Custom-app creation surface, token rotation, `read_fulfillments` scope concern (feeds TD-002) | Primary | Partial | Cited for TD-002/MBQ-05 status confirmation only; token-acquisition mechanics are explicitly out of scope for this session (forbidden: OAuth). |
| R27 | Avoid List | `docs/01-research/avoid-list.md` | 2026-06-30 | Competitor anti-patterns (cron-only sync, manual-only recovery, raw `ir.cron` field exposure, email-only errors) | Secondary | Partial | Competitor-claim tier per `CLAUDE.md` §8 — informs UX/quality-bar framing, not an architecture fact. |
| R28 | Common Patterns | `docs/01-research/common-patterns.md` | 2026-06-30 | Cross-competitor sync/queue/retry pattern survey (TeqStars, Emipro, VentorTech, Softhealer, Webkul, ecommerce_shopify) | Secondary | Partial | Same tier as R27. |
| R29 | Competitor Deep Dives | `docs/01-research/competitor-deep-dives.md` | 2026-06-30 | Per-competitor detail behind R28 | Secondary | Partial | |
| R30 | Best-in-Class Observations | `docs/01-research/best-in-class-observations.md` | 2026-06-30 | Cross-competitor best-practice synthesis | Secondary | Partial | |
| R31 | **Sync Engine Odoo and Repo Source Notes (PR #124, merged, "Task 006A-2")** | `docs/01-research/sync-engine-odoo-repo-source-notes.md` | 2026-07-08 | **The canonical Odoo/repo-substrate shard for this task family.** Line-cited inspection of the same job/log/store/credential/readiness substrate this package covers, plus deeper Odoo source research this package did not independently perform: `@api.constrains` propagation mechanics, the modern `models.Constraint`/`Index` DDL API (superseding legacy `_sql_constraints`), constraint-DDL timing (`NOT DEFERRABLE` by Postgres default — an inference, not a directly-stated Odoo fact), `cr.savepoint()` usage patterns (selective, not automatic on `create()`/`write()`), and `ir.cron`'s `_acquire_one_job()`/`FOR NO KEY UPDATE SKIP LOCKED` job-acquisition locking | Primary | Yes | **Added this revision.** Accepted/merged (PR #124, merge commit `3735ae2292d1fcf926c83034ac8513906c9f5020`, part of `3735ae2`). Where this package's own independent Odoo research (fetched the same day via a separate workflow) reaches the same conclusion as `R31`, both are cited as corroboration; where `R31` goes further, this package defers to it rather than restating. See `sync-engine-source-notes.md`'s "Companion Task 006A shards" note for the full cross-reference. |
| R32 | Sync Engine Competitor / Common Pattern Notes (PR #123, merged, "Task 006A-4") | `docs/01-research/sync-engine-competitor-pattern-notes.md` | 2026-07-08 | Sync-engine-specific re-lens of this repo's existing competitor research corpus (R27–R30) — manual/scheduled sync, queues, logs, retry, webhooks, dedup, mapping screens | Secondary | Partial | **Added this revision**, for completeness (accepted/merged, merge commit `aeaf7eb7782178b260edd452c9075a8dd1df323a`). Not independently integrated into this package's own "Competitor/common-pattern notes" section — out of scope for this revision, which was scoped to incorporating `R31` specifically. |
| R33 | **Sync Engine Queue / Idempotency / Retry Source Notes (PR #126, merged, "Task 006A-3")** | `docs/01-research/sync-engine-queue-idempotency-source-notes.md` | 2026-07-08 | **The canonical queue/idempotency/retry/backoff/dead-letter reference-pattern shard for this task family.** A 52-source, 9-topic, adversarially-verified (each topic independently re-fact-checked against live sources, with a dedicated "corrected on verification" trail) treatment of: OCA `queue_job` mechanics, idempotency-key design (Stripe/IETF/AWS/Google), retry/backoff/jitter (AWS/Azure/Google SRE, with corrected jitter formulas), dead-letter/permanent-failure visibility (AWS SQS/Azure Service Bus/RabbitMQ/EIP), duplicate-running prevention (PostgreSQL locking incl. advisory locks, Odoo `ir.cron` acquisition, Kubernetes leader-election), checkpoint/resume (Shopify cursors/bulk operations, AWS Glue/Step Functions, Azure Data Factory), and observability/redaction (OWASP, MITRE CWE-532, NIST, GDPR, Twelve-Factor). | Primary | Yes | **Added this revision.** Accepted/merged (PR #126, merge commit `3c9fad896f996c19d4978e5aca58b39674b7b35a`). Explicitly reaffirms RA-004/DEC-005 (OCA `queue_job` remains reference-only, not reconsidered) and the already-accepted RA-014/015/016/017 retry/idempotency taxonomy — no decision is proposed. Where this package's own research overlaps, both are cited as corroboration; where `R33` is deeper or corrects something this package stated less precisely, this package now defers to and cross-references it — see the "Wording alignment against R33" notes in `sync-engine-source-notes.md`. Can support a later architecture gate as Primary evidence, subject to the same "no decision yet" caveat every source in this package carries. |

### Synthesis hierarchy (added this revision)

Four Task 006A shards now exist in `Shopify-connector`. Each is canonical
for its own scope; this package (PR #125) is the **cross-cutting synthesis
and evidence-map**, not a fifth independent research pass that duplicates
any of the other three:

- **`R31` (PR #124)** — canonical for **Odoo/repo substrate** research
  (the existing job/log/store/credential/readiness models, plus Odoo 19
  transaction/constraint/concurrency source research).
- **`R33` (PR #126)** — canonical for **queue/idempotency/retry/backoff/
  dead-letter reference patterns** (OCA `queue_job` plus vendor-neutral and
  vendor-specific engineering references).
- **`R32` (PR #123)** — canonical for **competitor/common sync patterns**.
- **This package (PR #125)** — the **synthesis/evidence-map package**: a
  source inventory, source notes, evidence map, open-questions register, and
  risk register that draw on and cross-reference all three shards above
  (plus this package's own independent Shopify/Odoo/OCA/engineering-
  reference research, `S1`–`S16`, `O1`–`O14`, `Q1`–`Q8`, `E1`–`E9`), rather
  than replacing any of them. Where this package's own research and a sibling
  shard reach the same conclusion, both are cited as corroboration. Where a
  sibling shard is deeper, more precisely sourced, or corrects something this
  package stated less precisely, this package defers to and cross-references
  it rather than re-deriving or duplicating it.

## 4. Official Shopify sources — freshly fetched this session (2026-07-08)

| # | Source | URL | Topic | Grade | Can support arch. decisions | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| S1 | Paginating results with GraphQL | `https://shopify.dev/docs/api/usage/pagination-graphql` | Cursor pagination mechanics: edges/node/cursor, `pageInfo`, forward (`first`/`after`) and backward (`last`/`before`) paging, 250-item max page size | Primary | Yes | No statement on cursor durability/reuse across a paused-and-resumed sync (logged as open question). |
| S2 | Admin GraphQL API reference — `PageInfo` object | `https://shopify.dev/docs/api/admin-graphql/latest/objects/PageInfo` | `PageInfo` schema (Relay-spec conformant): `startCursor`/`endCursor`/`hasNextPage`/`hasPreviousPage` | Primary | Yes | |
| S3 | GraphQL queries basics | `https://shopify.dev/docs/apps/build/graphql/basics/queries` | Connections concept; `first`/`last` mandatory when selecting a connection; `nodes` vs `edges` | Primary | Partial | |
| S4 | Shopify API usage limits (general) | `https://shopify.dev/docs/api/usage/limits` | GraphQL cost model, leaky-bucket rate limiting, 25,000-object pagination-cap section | Primary | Partial | The "Pagination limits" section on this page is **ambiguous** as to API scope in isolation — see S5, which resolves the ambiguity. |
| S5 | Changelog — New pagination limits for Liquid & Storefront GraphQL API | `https://shopify.dev/changelog/new-pagination-limits-for-liquid-storefront-graphql-api` | **Explicit confirmation the 25,000-object cap applies only to Liquid/Storefront GraphQL, not Admin GraphQL**, which "continues to support higher limits for merchant facing workflows" | Primary | Yes | **Correction to prior repo research**: `shopify-official-api-notes.md` (R24) logged the 25,000 cap as an open question re: scope; this changelog entry resolves it. See source-notes for full analysis. |
| S6 | Bulk operations overview | `https://shopify.dev/docs/api/usage/bulk-operations` | One-line landing-page description only; no when-to-use guidance beyond child pages | Secondary | No | |
| S7 | Perform bulk operations with the GraphQL Admin API (queries) | `https://shopify.dev/docs/api/usage/bulk-operations/queries` | JSONL result format, `__parentId`, ordering guarantee, 7-day URL expiry, 10-day query ceiling, cancellation, retry-by-resubmission (no partial-resume), concurrency (1 pre-2026-01 / 5 post-2026-01), operation-level error codes | Primary | Yes | |
| S8 | Bulk import data with the GraphQL Admin API (imports/mutations) | `https://shopify.dev/docs/api/usage/bulk-operations/imports` | Per-line error handling (JSON-parse / validation / execution / access-denied), 24-hour mutation ceiling, exemption from standard rate limits, no explicit "don't use for routine sync" guidance | Primary | Yes | |
| S9 | `bulkOperationCancel` mutation reference | `https://shopify.dev/docs/api/admin-graphql/latest/mutations/bulkOperationCancel` | Cancellation is asynchronous (`CANCELING` → `CANCELED`); no statement on repeat-call safety | Primary | Partial | |
| S10 | `BulkOperationStatus` enum reference | `https://shopify.dev/docs/api/admin-graphql/latest/enums/BulkOperationStatus` | Full status set: `CANCELED, CANCELING, COMPLETED, CREATED, EXPIRED, FAILED, RUNNING` | Primary | Yes | |
| S11 | `BulkOperationErrorCode` enum reference | `https://shopify.dev/docs/api/admin-graphql/latest/enums/BulkOperationErrorCode` | Exactly 3 operation-level failure codes: `ACCESS_DENIED`, `INTERNAL_SERVER_ERROR`, `TIMEOUT` | Primary | Yes | |
| S12 | `BulkOperation` object reference | `https://shopify.dev/docs/api/admin-graphql/latest/objects/BulkOperation` | `url` (7-day expiry), `partialDataUrl` (failed-only, per docs wording), `errorCode` field definitions | Primary | Yes | Whether `partialDataUrl` populates on `CANCELED` (vs only `FAILED`) is undocumented — open question. |
| S13 | About webhooks (redirect target of the former "best practices" URL) | `https://shopify.dev/docs/apps/build/webhooks` | Delivery-not-guaranteed, out-of-order delivery, `X-Shopify-Triggered-At`/`updated_at` sequencing guidance, reconciliation-job guidance | Primary | Yes | **URL structure changed** since prior repo research cited `.../webhooks/best-practices` as a distinct page — now consolidated under "About webhooks." Flagged so future sessions don't cite a dead path. |
| S14 | Verify webhook deliveries | `https://shopify.dev/docs/apps/build/webhooks/verify-deliveries` | HMAC-SHA256 raw-body verification, `X-Shopify-Webhook-Id` dedup, `X-Shopify-Event-Id` correlation, 1s/5s timeout, 8-retries-over-4-hours, 8-consecutive-failures auto-delete | Primary | Yes | |
| S15 | Idempotent requests (Admin API usage) | `https://shopify.dev/docs/api/usage/idempotent-requests` | `@idempotent` concept, argument-style vs directive-style, bulk-mutation per-row scoping | Primary | Yes | Only 4 illustrative mutation names on this page — the comprehensive list is on S16. |
| S16 | Implementing idempotency (Admin API usage) | `https://shopify.dev/docs/api/usage/implementing-idempotency` | 24-hour TTL (reconfirmed unchanged), `IDEMPOTENCY_CONCURRENT_REQUEST`/`IDEMPOTENCY_KEY_PARAMETER_MISMATCH` errors, bulk-operation idempotency guidance, **full mutation list (self-dated 2 Feb 2026, 16 names)** | Primary | Yes | **Discrepancy flagged**: prior repo research (R8/R10) cites "17 mutations"; this session's fresh count is 16 named mutations. Not resolved — logged as an open question, not silently reconciled. |

## 5. Community / non-authoritative (Shopify-adjacent)

| # | Source | URL | Topic | Grade | Can support arch. decisions | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| C1 | Shopify Community Forums — "GraphQL pagination - 250 page size limit?" | `https://community.shopify.dev/t/graphql-pagination-250-page-size-limit/28932` | A Shopify staff reply corroborating the 250-item cap applies across all Shopify GraphQL APIs | Reference only | Partial | Included only as corroboration of S1's own directly-stated 250 figure; not load-bearing on its own. |

## 6. Official Odoo sources — freshly fetched this session (2026-07-08)

| # | Source | URL | Topic | Grade | Can support arch. decisions | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| O1 | Developer Reference — ORM API (SQL Execution section) | `https://www.odoo.com/documentation/19.0/developer/reference/backend/orm.html` | `self.env.cr` raw-SQL escape hatch; confirms **no dedicated transaction/commit/rollback/savepoint section exists on this reference page** | Primary | Partial | Retrieved via the page's RST source (`odoo/documentation` repo, 19.0 branch) because the live page is JS-rendered and WebFetch could not extract body text — confirmed independently reachable (HTTP 200) at the cited URL. |
| O2 | Developer Reference — Testing Odoo | `https://www.odoo.com/documentation/19.0/developer/reference/backend/testing.html` | `TransactionCase` (savepoint-per-method) / `SingleTransactionCase` docstrings, embedded via Sphinx autodoc | Primary | Yes | Same JS-rendering caveat as O1; content confirmed via the underlying Python docstring (O3) that Sphinx injects verbatim. |
| O3 | Odoo 19.0 source — `odoo/tests/common.py` | `https://github.com/odoo/odoo/blob/19.0/odoo/tests/common.py` | Source-level `TransactionCase`/`SingleTransactionCase`; explicit monkey-patch **forbidding** `commit()`/`rollback()`/`close()` inside a test | Primary | Yes | `github.com/blob` HTML 403'd through the session proxy (anti-bot, not an access denial) — content fetched via `raw.githubusercontent.com` instead, byte-identical for the same ref. |
| O4 | Odoo 19.0 source — `odoo/sql_db.py` | `https://github.com/odoo/odoo/blob/19.0/odoo/sql_db.py` | `Savepoint` class (SQL `SAVEPOINT`/`ROLLBACK TO`/`RELEASE`), `Cursor.commit()`/`rollback()`/`_close()`, **`REPEATABLE READ` chosen as the default isolation level** (Odoo owns its own app-level locking rather than relying on `SERIALIZABLE`), cursor-context-manager auto-commit/close semantics, request-scoped `Cursor.cache` (rollback-unaware) | Primary | Yes | Directly load-bearing for Analysis Areas A (idempotency) and C (duplicate-running prevention). |
| O5 | Odoo 19.0 source — `odoo/service/model.py` | `https://github.com/odoo/odoo/blob/19.0/odoo/service/model.py` | `retrying()` — automatic serialization-failure retry wrapper for RPC/ORM method dispatch; `PG_CONCURRENCY_ERRORS_TO_RETRY` (lock-not-available, serialization-failure, deadlock); `MAX_TRIES_ON_CONCURRENCY_FAILURE = 5`; random exponential backoff (`random.uniform(0.0, 2**i)`) | Primary | Yes | The single cleanest confirmation that Odoo has an **automatic** app-level retry-on-conflict mechanism, distinct from and additional to `ir.cron`'s own bookkeeping. |
| O6 | Odoo 19.0 source — `odoo/http.py` | `https://github.com/odoo/odoo/blob/19.0/odoo/http.py` | Confirms HTTP controller dispatch also routes through `service_model.retrying()`; unconditional `cr.close()` in a `finally` block | Secondary | Partial | Confirmatory/cross-referencing rather than a primary definition. |
| O7 | Odoo 19.0 source — `odoo/addons/base/models/ir_cron.py` | `https://github.com/odoo/odoo/blob/19.0/odoo/addons/base/models/ir_cron.py` | `_callback()` explicit commit-on-success / rollback-and-reraise-on-exception; `_acquire_one_job()` `FOR NO KEY UPDATE SKIP LOCKED` job acquisition; `_process_jobs_loop()` skip-on-`SerializationFailure` (not retry); `CompletionStatus` (fully/partially done, failed); failure-count/deactivation constants (3 consecutive timeouts → skip run; 5 failures over ≥7 days → deactivate); **no dead-letter/DLQ terminology or mechanism found in this file, no persisted error/traceback field on `ir.cron.progress`** | Primary | Yes | The single richest source in this package for Analysis Areas B, C, and H. `R31` (PR #124) independently examined `_acquire_one_job()`'s `SerializationFailure` docstring and `_process_jobs_loop`'s catch-and-continue behavior — corroborating, at the job-*acquisition* layer specifically. Whether that non-retry behavior at the job-acquisition layer says anything about retry-safety of a job's own domain-record-processing code is **not** established by either shard — see the source notes' explicit inference labeling. |
| O8 | Odoo 19.0 Contributing Docs — Coding Guidelines | `https://www.odoo.com/documentation/19.0/contributing/development/coding_guidelines.html` | Official prose: "the transaction is rolled back atomically" on any RPC-call error; "NEVER call `cr.commit()`/`cr.rollback()` yourself" rule; recommended `with self.env.cr.savepoint():` pattern; **explicit warning: PostgreSQL performance degrades past 64 savepoints in one transaction, worse with replicas — a batch-sizing input, not an enforced cap** | Primary | Yes | One of the most architecturally load-bearing sources in the package — a performance constraint to weigh for DEC-005's "per-record savepoint isolation" design, not an automatically-enforced ceiling. Independently fetched and quoted by `R31` (PR #124) — corroborating, not duplicative. |
| O9 | Odoo 19.0 source — `odoo/orm/models.py` | `https://github.com/odoo/odoo/blob/19.0/odoo/orm/models.py` | `BaseModel.lock_for_update()` / `try_lock_for_update()` — explicit ORM row-locking primitive using `FOR UPDATE SKIP LOCKED` (`FOR NO KEY UPDATE SKIP LOCKED` with `allow_referencing=True`), both decorated `@api.private` | Primary | Yes | **Structural note:** in 19.0 the legacy `odoo/models.py` path no longer exists (404) — relocated to `odoo/orm/models.py`. Any future reference to "the ORM source" must use the new path. |
| O10 | Odoo 19.0 source — `odoo/addons/base/models/ir_sequence.py` | `https://github.com/odoo/odoo/blob/19.0/odoo/addons/base/models/ir_sequence.py` | Real in-core example of raw `SELECT ... FOR UPDATE NOWAIT` (contrast with O9's `SKIP LOCKED` helper) | Primary | Partial | Confirms two coexisting locking idioms in core: hard-fail `NOWAIT` vs. skip-and-continue `SKIP LOCKED`. |
| O11 | Odoo 19.0 source — `odoo/exceptions.py` | `https://github.com/odoo/odoo/blob/19.0/odoo/exceptions.py` | `LockError` (`UserError`, HTTP 409) raised by `lock_for_update()`; `ConcurrencyError` docstring explicitly cross-references `retrying()` | Primary | Partial | |
| O12 | Odoo 19.0 source — `odoo/orm/decorators.py` (`@api.private`) | `https://github.com/odoo/odoo/blob/19.0/odoo/orm/decorators.py` | `@api.private` marks a method non-RPC-callable; cross-referenced against `get_public_method()`'s RPC guard in O5 | Primary | Yes | Establishes that `lock_for_update()`/`try_lock_for_update()` are **not reachable from a pure external RPC client** — relevant only if a future design contemplates Odoo-external callers, not for in-module cron code. |
| O13 | Odoo 19.0 official docs — Actions reference ("Writing cron functions" section) | `https://www.odoo.com/documentation/19.0/developer/reference/backend/actions.html#writing-cron-functions` | Official batching guidance; **worked code example using `try_lock_for_update()`** inside a cron loop; official rollback-first exception-handling pattern | Primary | Yes | The *only* official documentation location that demonstrates the locking primitive (it is absent from the formal ORM reference page, O1). |
| O14 | Odoo 19.0 official docs — Actions reference (full Scheduled Actions section) | `https://www.odoo.com/documentation/19.0/developer/reference/backend/actions.html` | 3-consecutive-failures skip rule; 5-failures-over-≥7-days deactivation + DB-admin notification; hard DB-level process-kill time limit; `_commit_progress()` signature | Primary | Partial | No dead-letter/DLQ/redrive terminology or mechanism was found in this official section, by full-text review of the section actually fetched — not asserted as a claim about the entire Odoo documentation set. |

## 7. OCA `queue_job` — reference pattern only (not adopted; RA-004 remains binding)

| # | Source | URL | Topic | Grade | Can support arch. decisions | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| Q1 | OCA/queue repo tree, 19.0 branch | `https://github.com/OCA/queue/tree/19.0/queue_job` | Module file/directory structure | Secondary | Partial | Retrieved via WebFetch (paraphrase-only); structural claim cross-checked by independently fetching Q2–Q7 as raw files. |
| Q2 | `queue_job/README.rst` | `https://raw.githubusercontent.com/OCA/queue/19.0/queue_job/README.rst` | Channels (capacity lanes), retry pattern (`{tries: seconds}`), `max_retries` (default 5, 0=infinite), `identity_key`, `related_action`, auto-requeue of orphaned `enqueued`/`started` jobs, mandatory job-body idempotency | Primary | Yes | |
| Q3 | `queue_job/job.py` | `https://raw.githubusercontent.com/OCA/queue/19.0/queue_job/job.py` | 7-state machine (`wait_dependencies/pending/enqueued/started/done/cancelled/failed`); `perform()` retry-counter/backoff logic; `identity_exact_hasher` (SHA1 of model+method+ids+args+kwargs); **dedup query only matches `wait_dependencies`/`pending`/`enqueued`** (not historical) | Primary | Yes | |
| Q4 | `queue_job/models/queue_job.py` | `https://raw.githubusercontent.com/OCA/queue/19.0/queue_job/models/queue_job.py` | Manual `button_done`/`button_cancelled`/`requeue()`; `needaction` surfacing of `failed` jobs; `mail.thread`-based failure notification/chatter; cron-driven autovacuum of old done/cancelled jobs | Primary | Yes | |
| Q5 | `queue_job/models/queue_job_channel.py` | `https://raw.githubusercontent.com/OCA/queue/19.0/queue_job/models/queue_job_channel.py` | Channel hierarchy model — **no capacity field**; capacity is jobrunner-process runtime config only, not a DB record | Primary | Partial | |
| Q6 | `queue_job/delay.py` | `https://raw.githubusercontent.com/OCA/queue/19.0/queue_job/delay.py` | `identity_key` dedup is all-or-nothing at graph-delay time; authors' own comment flags this as a known rough edge | Primary | Yes | |
| Q7 | `queue_job/controllers/main.py` | `https://raw.githubusercontent.com/OCA/queue/19.0/queue_job/controllers/main.py` | `_runjob()` control flow: `RetryableJobError` → postpone + rollback; any other exception → log + terminal `failed` + re-raise; PG concurrency errors auto-converted to `RetryableJobError` | Primary | Yes | |
| Q8 | `queue_job/exception.py` | `https://raw.githubusercontent.com/OCA/queue/19.0/queue_job/exception.py` | `RetryableJobError` vs `FailedJobError` vocabulary; per-raise `seconds`/`ignore_retry` overrides | Primary | Yes | |

## 8. Engineering references (general patterns — never authoritative for Shopify/Odoo facts; used only where official docs are silent)

| # | Source | URL | Topic | Grade | Can support arch. decisions | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| E1 | Stripe API Reference — Idempotent Requests | `https://docs.stripe.com/api/idempotent_requests` | Client-generated idempotency-key design (UUID v4, ≤255 chars), 24h+ pruning, cached-response replay, "don't use PII as a key" | Secondary | Partial | Stripe-specific numeric parameters (24h, 255 chars) are illustrative, not binding on this connector. |
| E2 | Amazon Builders' Library — "Timeouts, retries, and backoff with jitter" (Marc Brooker) | `https://aws.amazon.com/builders-library/timeouts-retries-and-backoff-with-jitter/` | Retry amplification across a call-stack ("243x" worked example), capped exponential backoff, jitter to break correlated retries, "APIs with side effects aren't safe to retry unless idempotent" | Secondary | Partial | Retrieved via a Wayback Machine snapshot of the same official AWS URL (live page is JS-rendered and not extractable by session tooling); treated as equivalent first-party content. |
| E3 | Google SRE Book — Ch. 22, "Addressing Cascading Failures" | `https://sre.google/sre-book/addressing-cascading-failures/` | Retry-amplification worked example; randomized exponential backoff; per-request retry caps **and** process-wide retry budgets | Secondary | Partial | Supplementary corroboration of E2, not independently load-bearing. |
| E4 | AWS SQS Developer Guide — "Using dead-letter queues in Amazon SQS" | `https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-dead-letter-queues.html` | DLQ purpose (isolate poison messages for inspection), `maxReceiveCount`-driven routing, DLQ retention-period best practice | Secondary | Partial | SQS-specific API surface not transferable; the *pattern* (isolate + inspect + optional redrive) is. |
| E5 | AWS Batch User Guide — "Use Amazon EC2 Spot best practices for AWS Batch" | `https://docs.aws.amazon.com/batch/latest/userguide/bestpractice6.html` | Chunk/checkpoint long jobs into ≤30-minute parts; up to 10 automated retries; conditional retry-by-failure-reason | Secondary | Partial | Framed around Spot-interruption handling, not API-pull sync specifically. |
| E6 | Google Cloud Run docs — "Jobs retries and checkpoints best practices" | `https://docs.cloud.google.com/run/docs/jobs-retries` | Periodic external progress-write, resume-from-last-checkpoint, idempotent task design, separate output location, default 3 auto-retries | Secondary | Partial | Strongest of the "engineering reference" pagination/checkpoint sources for general principle (write progress externally, resume on restart). |
| E7 | Fivetran Connector SDK Docs — "State Management" | `https://fivetran.com/docs/connector-sdk/connector-sdk-concepts/state-management` | Named "cursor"/"checkpoint" concept explicitly tied to pagination boundaries; state handed back on every sync regardless of prior success/failure | Secondary | Partial | Commercial ELT vendor's proprietary SDK terminology, not a standard — illustrative only. |
| E8 | AWS re:Post — "Using pagination tokens as Checkpoints with AWS API Pagination" | `https://repost.aws/articles/AR4YxHfaVMTimiWNf6vocBwg/...` | (Attempted) pagination-token-as-checkpoint pattern | **Unavailable** | No | WebFetch returned HTTP 403. Not used to support any claim; listed per the "record unavailable sources" rule. |
| E9 | OWASP Cheat Sheet Series — "Logging Cheat Sheet" | `https://cheatsheetseries.owasp.org/cheatsheets/Logging_Cheat_Sheet.html` | Explicit "never log" list (tokens, session IDs, passwords, connection strings, encryption keys, payment data, PII); de-identification techniques; sanitize-at-collection to prevent log injection | Secondary | Partial | Canonical, widely-cited secure-logging reference; general web-app guidance, not connector-specific. |

## Unavailable sources

- **E8** (AWS re:Post pagination-checkpoint article) — HTTP 403 on direct fetch, 2026-07-08. A prior WebSearch snippet suggested relevant content, but per the source rules a search snippet is not verified page content and was **not** used to support any claim in the companion documents.

## Sources intentionally rejected

- **Blog posts / third-party "Shopify API guide" sites** were not used anywhere in this package where an official `shopify.dev` page existed on the same topic, per the explicit instruction not to cite blogs as authoritative when official docs exist. No such site is cited in this inventory.
- **`odoo/models.py` (legacy path)** was not usable as a citation target — confirmed 404 in Odoo 19.0 (relocated to `odoo/orm/models.py`, see O9's notes). Recorded as a structural finding, not a source.

## Sources that are useful but not authoritative

- All of §8 (Engineering references, E1–E9) — general distributed-systems/API-client patterns, cited only because Shopify/Odoo official docs are silent on these specific topics (idempotency-key *design* for our own outbound calls, dead-letter-queue *pattern*, resumable-checkpoint *pattern*, log-redaction *pattern*). None may be cited as a Shopify or Odoo fact.
- **C1** (Shopify Community Forums) — corroboration only.
- §3 competitor-tier docs (R27–R30) — `[Competitor claim]`/synthesis tier per `CLAUDE.md` §8, useful for UX/quality-bar framing, not architecture facts.
- **Q1–Q8** (OCA `queue_job`) — explicitly a reference pattern only. RA-004 (OCA `queue_job` not the Phase 1 default substrate) remains binding and is **not** reconsidered by this research; nothing in §7 argues for adopting `queue_job`. `R33` (PR #126) independently reached the same OCA/`queue_job` source material via a separate, adversarially-verified research pass — see the discrepancy note below on the exact `--workers` threshold.

## Version / API caveats

1. **Shopify GraphQL API version drift.** Prior repo research (R24) recorded the `latest` alias resolving to `2026-04` (2026-06-30) and then `2026-07` (2026-07-01). This session did not re-confirm the current `latest` alias value; any load-bearing version-specific fact above should be re-checked against the API version active at implementation time.
2. **The `@idempotent` mutation list is self-dated by Shopify** ("last updated 2 February 2026," S16) and is explicitly presented as non-exhaustive/re-checkable-per-mutation — it must not be hard-coded as a permanent, complete set.
3. **Bulk-operation concurrency limits are version-gated**: 1 concurrent operation of each type per shop before API version 2026-01; up to 5 of each from 2026-01 onward (S7, S8).
4. **Odoo 19.0 source layout changed mid-line**: `odoo/models.py` → `odoo/orm/models.py` (O9). Any future citation of "the ORM source" must target the new path.
5. **This package's Odoo source-code citations are a live-branch (19.0) snapshot as of 2026-07-08** — line numbers cited in the companion notes will drift as upstream edits land; re-locate by method/constant name, not line number, on any future re-verification.
6. **The 25,000-object pagination cap is Liquid/Storefront-only** (S5), not Admin GraphQL — this corrects an ambiguity left open in the pre-006A baseline (R24).
7. **R31 (PR #124) is the canonical Odoo/repo-substrate shard for this task family, added revision 1.** Where this package's own Odoo research (O1–O14) and R31 independently reach the same conclusion, both are cited as corroboration between two independently-produced shards produced the same day (2026-07-08). Two overclaim-risk conclusions this package originally stated too strongly — (a) that "Tasks 001–005 already satisfies most mandatory claims," and (b) that Odoo's RPC-layer retry "does not extend" to a cron job's own record-processing code — have been relabeled as, respectively, a partial-primitives statement with explicit named gaps, and a source-backed inference requiring runtime proof, not settled facts. See `sync-engine-source-notes.md`'s "Companion Task 006A shards" note and `sync-engine-evidence-map.md` rows 1 and 20 for the corrected wording.
8. **R33 (PR #126) is the canonical queue/idempotency/retry/backoff/dead-letter reference shard, added this revision.** Where this package's own engineering-reference research (`E1`–`E9`) and R33 independently reach the same conclusion (bounded retry + exponential backoff + jitter as a converged, cross-vendor requirement; dead-letter visibility as a structural pattern this repo's own job states already satisfy; OCA `queue_job` as reference-only, RA-004 unchanged), both are cited as corroboration. Where R33 is genuinely new or corrects this package: (a) a throttled Shopify GraphQL call can return **HTTP 200** with a `THROTTLED` code in the body rather than a 4xx status — this package's own rate-limit research (carried from the pre-006A baseline, `R24`, not independently re-verified for GraphQL response-status behavior this session) did not previously surface this; (b) dead-letter-queue visibility across every vendor examined is an **opt-in alarm/metric**, not a default push notification, and dead-lettering can trigger on a **single** failed attempt (not only after multiple retries) — both nuances now cross-referenced into this package's dead-letter claim; (c) PostgreSQL advisory locks (session- vs. transaction-scoped, and a documented `LIMIT`+advisory-lock hazard) were not previously researched by this package; (d) R33's own duplicate-running-prevention research independently reaches the identical "requires actual Odoo 19 runtime proof, not source-reading alone" conclusion this package and R31 already carried — now a three-shard-corroborated open question, not a single package's inference. See `sync-engine-source-notes.md`'s "Wording alignment against R33" notes for the full list.
9. **A discrepancy on OCA `queue_job`'s minimum worker count is noted, not resolved.** This package's pre-006A baseline (`R25`) cites `--workers > 0` as the Jobrunner's operational precondition; `R33` (PR #126, OCA-4) cites `--workers > 1`. Neither this package nor R33 resolves this — recorded here as an unreconciled discrepancy between two source passes, consistent with the source rules' "record the conflict, do not resolve it prematurely" instruction. Immaterial to RA-004 either way (queue_job remains reference-only).
10. **A methodological note on `E2`/`GEN-9` (the AWS Builders' Library "Timeouts, retries, and backoff with jitter" essay).** `R33` (PR #126) reports this exact URL as unextractable (JS-rendered) in **both** of its own research passes and treats it as "Reference only... not used as evidence for any fact" (`GEN-9`). This package's own `E2` citation of the same URL used a different retrieval method — a Wayback Machine archived snapshot — and did extract quotable content from it (see `E2`'s inventory notes). This is disclosed as a methodological difference between the two shards' retrieval approaches, not a factual contradiction; `E2`'s content is independently corroborated by R33's own successfully-fetched sources on the same topic (`GEN-10`, `GEN-12`, `GEN-13`, `GEN-15` — Google SRE, Azure, and AWS Well-Architected all converge on the same bounded-retry-plus-jitter conclusion `E2` describes).
