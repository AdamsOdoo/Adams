# Sync Ownership Matrix

## Purpose

Define which system owns each entity after setup and how changes flow between Odoo and Shopify. Rows below state what current code does today; DELTA notes identify confirmed product decisions that differ from code.

## Matrix

| Entity | Master system | Direction | Trigger mode | Manual/scheduled/webhook/Odoo event/reconciliation | Conflict behavior | Notes | Status |
|---|---|---|---|---|---|---|---|
| Product/template | Mixed today; DEC-018 target is Odoo after initial import | Shopify → Odoo import and Odoo → Shopify export | Import batch/webhook refresh/export batch | Manual/scheduled/webhook/Odoo event | Binding checksum skips unchanged; SKU match can update existing Odoo product | Import creates/updates product and binding (`addons/shopify_connector_pro/sync/product_sync.py:263-309`); export creates/updates Shopify product (`addons/shopify_connector_pro/sync/product_sync.py:70-154`). DELTA vs DEC-018 because backend default is bidirectional. | DELTA |
| Variant | Mixed today | Shopify → Odoo import and Odoo → Shopify export | Product import/export | Manual/scheduled/webhook/Odoo event | Existing variant binding by backend/shopify_id wins; option match then index fallback | Variant import creates bindings (`addons/shopify_connector_pro/sync/product_sync.py:440-475`); export bulk-updates SKU/price/barcode (`addons/shopify_connector_pro/sync/product_sync.py:133-154`). | DELTA |
| Price | Mixed today | Shopify → Odoo on import; Odoo/pricelist → Shopify on export | Product import/export | Manual/scheduled/Odoo event | Last sync direction wins; no conflict arbiter found | Import sets `list_price` from first variant (`addons/shopify_connector_pro/sync/product_sync.py:327-333`); export price uses pricelist or `lst_price` (`addons/shopify_connector_pro/sync/product_sync.py:60-68`). DELTA vs DEC-018 for post-import master. | DELTA |
| Inventory level | Odoo | Odoo → Shopify | Inventory sync | Manual/scheduled/Odoo event | Skip unchanged; Shopify compareQuantity detects drift for retry | Inventory sync reads Odoo warehouse quantities and calls `inventorySetQuantities` (`addons/shopify_connector_pro/sync/inventory_sync.py:119-214`). | SPECIFIED |
| Customer | Shopify-biased today | Shopify → Odoo import; export support exists by direction | Customer sync | Manual/scheduled/webhook | Dedup by backend-configured email/phone strategy | Backend default customer direction is import (`addons/shopify_connector_pro/models/shopify_backend.py:93-106`); order guest/customer dedup uses email/phone (`addons/shopify_connector_pro/sync/order_sync.py:813-843`). | SPECIFIED |
| Order | Shopify | Shopify → Odoo | Order import/status webhook | Manual/scheduled/webhook | Existing binding updated; duplicate binding avoided by backend/shopify_id | Order importer creates sale orders from Shopify data (`addons/shopify_connector_pro/sync/order_sync.py:185-360`) and stores binding total/status (`addons/shopify_connector_pro/sync/order_sync.py:164-175`). | SPECIFIED |
| Order/payment status | Shopify inbound; Odoo can reverse-mark paid when enabled | Shopify → Odoo and optional Odoo → Shopify | Payment transition/import and invoice post | Webhook/scheduled/Odoo event | Total guard blocks wrong posting; reverse failure schedules activity | Payment status changes invoke handler (`addons/shopify_connector_pro/sync/order_sync.py:122-140`); reverse mark-paid runs on posted invoice (`addons/shopify_connector_pro/models/account_move.py:48-107`). | SPECIFIED |
| Fulfillment/tracking | Odoo outbound; Shopify inbound configurable | Bidirectional | Picking validation / fulfillment webhook | Odoo event/webhook/manual setting | Remaining quantity and SKU matching; inbound modes ignore/activity/auto_validate | Outbound fulfillment from picking (`addons/shopify_connector_pro/models/stock_picking.py:11-59`); inbound handler modes (`addons/shopify_connector_pro/sync/fulfillment_sync.py:230-288`). | SPECIFIED |
| Refund/credit note | Shopify inbound; Odoo outbound optional/undecided | Bidirectional in code | Refund import / credit note post | Scheduled/webhook/Odoo event | Refund GID recovery guard, over-refund guard; reverse path lacks explicit idempotency key found | Inbound refund creates credit note (`addons/shopify_connector_pro/sync/refund_sync.py:167-535`); reverse refund on credit note post (`addons/shopify_connector_pro/models/account_move.py:108-198`). | DECISION-NEEDED |
| Payout | Shopify | Shopify → Odoo | Payout import | Manual/scheduled/reconciliation | Existing payout updated by backend+payout ID | Payout import creates/updates payout and transactions (`addons/shopify_connector_pro/sync/payout_sync.py:67-190`); unique payout constraint (`addons/shopify_connector_pro/models/shopify_payout.py:47-50`). | DECISION-NEEDED |
| Collection | Mixed | Shopify → Odoo import and Odoo → Shopify export | Collection sync/export | Manual/scheduled | Binding/checksum behavior; current contract not deeply specified | Collection sync/export files exist (`addons/shopify_connector_pro/sync/collection_sync.py:1`, `addons/shopify_connector_pro/sync/collection_export.py:1`). Needs Goal 6 coverage. | DELTA |
| Discount/promoter | Odoo/promoter export plus Shopify import | Bidirectional | Discount export/import | Manual/scheduled | Checksums skip unchanged; imported records update existing matches | Discount export creates Shopify code (`addons/shopify_connector_pro/sync/discount_sync.py:28-76`); import stores Shopify discounts (`addons/shopify_connector_pro/sync/discount_import_sync.py:50-150`). | SPECIFIED |
| Gift card | Shopify | Shopify → Odoo | Gift-card import | Manual/scheduled | Existing backend/shopify_id updated | Gift card import mirrors Shopify records (`addons/shopify_connector_pro/sync/gift_card_sync.py:19-96`). Accounting treatment undecided. | DECISION-NEEDED |
| Metafield | Mapping-dependent | Import/export depending on mapping | Metafield sync | Manual/scheduled | Mapping config controls paths; conflict behavior undecided | Metafield sync imports/exports mapped metafields (`addons/shopify_connector_pro/sync/metafield_sync.py:35-140`). | DECISION-NEEDED |
| Abandoned cart | Shopify source; Odoo quotation created manually | Shopify → Odoo, then Odoo quotation | Abandoned cart import and manual action | Manual/scheduled | Existing quotation opens instead of duplicate | Abandoned cart record creates draft quotation and links it (`addons/shopify_connector_pro/models/shopify_abandoned_cart.py:123-200`). DELTA vs DEC-019 tag requirement. | DELTA |
