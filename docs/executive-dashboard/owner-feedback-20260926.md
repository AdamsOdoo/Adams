# Owner feedback on staging — 2026-09-26

Module `executive_dashboard` 19.0.1.7.0, branch `feature/executive-dashboard-phase1`. One row per point the owner raised, what was built, and the test or check that covers it.

| # | Asked | Built | Covered by |
|---|---|---|---|
| 1 | Bank & Cash: list every bank and cash account, also negative or zero balances | Every active bank/cash account of the dashboard's companies is listed, with 0 when it has no entries (negative balances were already listed). The KPI counts all listed accounts. | `TestFinance.test_bank_cash_lists_every_account`, `test_bank_cash_matches_account_balances` |
| 2 | Dark mode follows the user's preference, including System | No change. The dashboard's styles are compiled into Odoo's own light and dark bundles, so it is dark exactly when Odoo is. Following the operating system on its own would make the dashboard dark inside a light Odoo (navbar, dialogs, reports). Whether Odoo 19 Enterprise offers a "System" choice cannot be checked locally (no Enterprise source). | To verify on Odoo.sh: set Odoo's colour preference, reload |
| 3 | Year to date by default; switching periods smooth, data preloaded | Year to date is selected on open. Changing the period keeps the current figures on screen (dimmed) until the new ones arrive; once a section is shown, its other periods load in the background one after the other, so a later switch is instant. Custom dates load on demand. | Browser check: Year to date selected; Last month shown in 45 ms, no placeholder |
| 4 | Aged and Expected in one box with a switch; "Nothing overdue" contradicts the figures; drawer spacing | One Receivables box and one Payables box, each with an Aged / Expected switch (kept when coming back from a native screen). The Overdue chip now counts past-due invoices (bills) only; unapplied credits (advances and unmatched payments for payables) are a separate chip; shares are hidden when buckets mix signs; a negative total says why. The Expected view's first bucket is now called "Past due (net)" because it includes the credits. Drawer rows without a button now line up with the others. | `test_overdue_counts_invoices_and_credits_are_apart`; browser check: all drawer values end at the same x |
| 5 | Payables negative is confusing; account opens the Trial Balance, no "latest journal items" page | Amounts owed are shown positive (receivables: customers owe us; payables: we owe suppliers). A negative figure is a net debit/credit balance and the box says so. Account rows (Bank & Cash, By account) open the Trial Balance at once (General Ledger, then journal items, when it is unavailable); the intermediate panel is removed. | `test_accounts_open_the_trial_balance`, `test_drawers`, `test_other_company_account_is_refused` |
| 6 | Long lists stretch the chart beside them | Lists inside a card stop at about six rows (300 px) and scroll inside the card, the total stays at the bottom. Applies to every card list (bank accounts, salespeople, customers, …). | Browser check: 15 bank accounts, list scrolls, 19 px under the chart |
| 7 | Top customers: payments miss journal entries; use money movement | Top customers by collections: money received from each customer in the period, from payments, bank statement lines and journal entries that hit a bank or cash account. The money side is measured (the payment's amount, or the bank/cash lines of the entry), so a write-off, early-payment discount or withheld tax settled with a payment is not counted as money received. Refunds paid out reduce it; credit notes are not money and do not count. The panel lists those entries and opens them as Journal Entries. | `TestSales.test_top_customers_by_collections` |
| 8 | Dates do not apply to every figure | Balances are taken at the period's end (never after today): Bank & Cash, Receivables and Payables, their buckets (counted from that date), drawers and native screens (Balance Sheet, Aged report, Partner Ledger, Trial Balance). An invoice matched after that date (a later payment, or a post-dated cheque at today) is still open at it, as in the Aged report. Live figures (open quotations, stock on hand, people today) stay "now". | `test_balances_follow_the_period_end`, `test_post_dated_payment_keeps_the_invoice_open_today` |

## Found while checking the screenshots

- The staging Receivables box fell back from the Aged Receivable report to journal items because the two totals differed by 496,672 (account 100103 VAT Receivable, typed Receivable). The Aged reports leave Non Trade accounts out by default; the journal items now do the same, and the native total is always the figure shown, with any remaining difference stated in the box. Owner / accountant: confirm 100103 is marked Non Trade (or change its type to Current Assets). (`test_non_trade_receivables_are_left_out`)
- Large unreconciled credits on marketplace/courier customers (Bosta, Kashier) and negative cash boxes, and a single large August cost entry, are accounting data to review; the dashboard reports them as the books hold them.

## Still to verify on Odoo.sh (Enterprise)

- Aged Receivable / Payable totals equal the staging reports at the same dates, with amounts owed positive (the dashboard takes the native figure as it is, including for payables); the difference note appears only when they really differ.
- Aged report, Partner Ledger, Trial Balance and Balance Sheet open at the period end.
- Dark mode with the owner's colour preference.
