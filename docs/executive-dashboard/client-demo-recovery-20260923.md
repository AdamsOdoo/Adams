# Client demo recovery — 23 September 2026

## Source and evidence boundary

The approved HTML is `Adams_Dashboard_UI_Proposal.html`, SHA-256 `36ec95831f3f1e82e0709594d5c177938e3b3805ccd763b1e59c13933b2d7f4a`. PR #214 remains draft at application commit `ed36589fe7376eb7f0d5a21fa140045433068b33`, with addon trees core `cb9f01c242b8fbab64c855082405f28ed0a864be` and finance `6c711b9fae089f46f5a9116324aed8993f222382`. The dashboard-only staging commit is `af0d2327184e96dfb3eecd65de3a79c4747d6045` with those same two trees. Private paired captures and signed data remain outside this repository. The pre-change `64043764`/`28b37d90` checkpoints and their 120 native/65 frontend/28 browser-case counts are historical evidence only.

## Ranked visual and interaction deltas

| Priority | Paired visual observation at 1366 px | Development action and qualification boundary |
|---|---|---|
| 1 | Finance warning banner occupies a full row; the prototype places report status within the context panel. Accounting warnings must remain visible. The Odoo chart has one actual month for the chosen period; the prototype has six fictional months. | Retain the true report warning and signed actuals. A full warning hierarchy and chart geometry comparison is pending a rendered candidate. Never fill missing months with sample data. |
| 2 | Sales rankings began about 60 px lower with a last-updated note between cards and rankings. At equal 1162–1164 px content widths on build `38520910`, the first ranking panel was still about 42 px below the Site because its measure selector wrapped into a 67 px toolbar. Inventory's native reservation explanation also occupied a row above the table header. | Move the timestamp to the section footer; keep the Sales selector on one compact row and move the Inventory explanation below the table. Recheck the changed candidate's actual rendered layout before staging. |
| 3 | Inventory's live table is much wider than the prototype because real multilingual product/location identities and distinct free-to-use measures require additional columns. | Retain the report columns and horizontal scroll; physically verify on desktop, RTL and narrow widths before accepting the layout. |
| 4 | Procurement uses labelled native order counts while the mockup shows monetary cards. CRM and HR show true empty/missing-app states and real names. | Keep the real source worklists and capability states. UI20's monetary choice is deferred. |
| 5 | HR's date controls sit unframed above its tabs in Odoo, while the prototype places company and dates together in a bordered toolbar. The Attendance table cannot populate on staging because its app is absent. | Add the authorized company label and shared filter-bar styling in the existing HR Owl view. Preserve the truthful missing-app panel; validate the candidate at desktop and narrow widths. |
| 6 | The Odoo host bar, profile/company controls, warning banner and signed/unusual values differ from the simulated prototype shell/sample data. | Keep Odoo host UI and real data. Treat remaining styling differences as open visual review, not 1:1 parity. |

The private evidence contains 15 pre-change HTML/Odoo pairs across all six departments, five HR views, one employee profile and four Arabic/dark combinations. The selected HTML's bytes were hash verified. After the owner signed into the [approved design Site](https://adams-dashboard-design-review.mostafaessam94.chatgpt.site/), this session physically operated all six departments and five HR tabs there, including Inventory and Employees search with Apply semantics, numbered pagination, the employee profile drawer, three company identities, access and partial-widget states, Arabic, dark mode and the 390 px preview. The browser blocked local HTTP preview of the extracted file, so byte identity between the published Site and the selected file has not been independently proven. The Site shows sample data. Ten private, paired Site/Odoo screenshots of actual rendered development build `38522330` were then reviewed at matched 1162–1164 px content width before staging. Finance KPI geometry was within roughly 3 px; Sales ranking began roughly 15 px below the Site; Inventory table began roughly 12 px above the Site. Real data, warning height, host navigation and wide tables remain material visual differences. The render review supports a coherent demo, not pixel parity or formal UAT.

## Settled choices implemented in the development candidate

- **UI07:** remove the comparable-period percentage/status row entirely. Retain native report margins, target availability and warnings. No new comparison formula.
- **UI08:** use active Bank/Cash journals' default accounts to classify active `asset_cash` accounts. Read their signed closing balances from the mapped native General Ledger at the same company/cutoff/currency. Display bank/cash numbers only when every account is uniquely classified and their sum reconciles to the approved native cash headline within currency rounding. Shared/unlinked accounts are explicitly counted; unreconciled or inaccessible results display no invented amounts. Existing account directory and report actions remain. Current staging has unlinked accounts that prevent a defensible split; both component amounts show **Breakdown unavailable**, and the signed native headline remains visible. The ambiguous mapping needs accounting-manager review, not a fabricated remainder.
- **UI20:** retain clearly labelled approval/late-receipt order-count worklists. The monetary alternative is deferred.

## Demo route and present limit

[Staging entry](https://adamsmen-staging-38326320.dev.odoo.com/odoo/action-1004): Finance source/warnings/cash and Partner Ledger; Sales rankings and source records; Inventory filter/rows and stock sources; Procurement approval and late-receipt lists; CRM records/capabilities; HR Overview and Employees/profile followed by Attendance, Time off and Shifts capability states. This route now serves the candidate: the branch page retains live hostname build ID `38326320`, while `update.log` records database/build ID `38524406` running `odoo-bin -u adams_dashboard_finance,adams_executive_dashboard` at 09:00 UTC. Odoo Apps subsequently showed **both** installed modules at `19.0.1.6.0`; Odoo.sh marks the commit Test: Warning because dashboard and Documents settings fields share the label “Human Resources”, not because its update log reported a failed module install. The manual backup was created 08:19:43 UTC before the commit.

Staging browser checks: six department headings loaded; all five HR tabs opened; HR Employees search returned one authorized matching profile only after **Apply**; the bank account drawer showed permitted accounts and the explicit unlinked warning; native Profit and Loss opened from Accounting revenue and the sampled revenue, gross profit and net profit agreed with the cards at the same period. A nonempty summary CSV and a valid one-page A4 native P&L PDF were generated. Arabic labels and the dashboard's computed RTL direction rendered in dark appearance; the user was restored to English/system theme. The browser reported no Odoo-origin console errors in this session; its extension emitted unrelated metadata errors. This is a sampled interaction campaign, not full 1920/1440/1366/narrow, keyboard, negative/retry or performance qualification on this exact staged tree.

Staging has one authorized company and no Attendances, Time Off or Planning. It can show honest missing-app states but cannot demo populated shifts or leave. Development build `38522330` uses two US-localized demo companies and populated optional-app fixtures, with both addons installed `19.0.1.6.0`; a separate **blank** Enterprise database, prior-version upgrade and clean uninstall were not provisioned, nor was a second localization. Do not call the package plug-and-play. Source-specific owner visual/financial acceptance and formal UAT remain open. Keep PR #214 draft; do not merge or touch main/production.
