# Executive Dashboard future work — client-demo cutoff

1. **UI20:** decide whether Procurement attention cards should retain the current native approval/late-receipt **order counts** or show a carefully defined financial amount. Until specified and reconciled, keep labelled counts and no invented totals.
2. Extend the ten actual side-by-side Site/development pairs at matched content width to the remaining focus, hover, empty/error/restricted, RTL/light/dark, 1920/1440/1366/narrow/zoom and vertical/horizontal scrolling cases. The exact HTML file's hash was verified, but published Site byte identity could not be independently established because the browser blocked its local HTTP preview. Resolve the material Finance warning/chart and native-table differences in `client-demo-recovery-20260923.md`; preserve real Odoo host/data.
3. Have an accounting manager classify the unlinked staging accounts by their real Bank/Cash journal associations; then independently reconcile both signed groups to native accounting at identical company/cutoff/currency. Qualify shared/archived accounts, missing mappings, user rights, multiple currencies, pagination and execution time. The current unavailable split is the correct honest state until then.
4. Prove fresh installation, prior-version upgrade and clean uninstall on an isolated Enterprise Odoo 19 database; repeat identity/mapping checks with a second company/localization. Current staging has just one authorized company.
5. Populated HR optional-app states exist in the separate development demo fixtures. If a **live staging** populated workflow is required, arrange an authorized environment with Attendances, Time Off and Planning; do not install those apps or fabricate records on Adams For Men staging merely to fill cards.
6. Finish a full new-tree native/browser/keyboard/negative-recovery/viewport/performance campaign and verify staging RPC logs, beyond the sampled six departments, five HR tabs, source report, CSV/PDF and Arabic/dark checks. The historical 120 native/28 browser-case counts belong to `38514095`; the changed development build is `38522330` and staged commit is `af0d2327`.
