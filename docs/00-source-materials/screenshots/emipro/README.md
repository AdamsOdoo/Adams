# Emipro (R3) — screenshots

> Visual evidence for **Emipro — Shopify Odoo Connector** (R3).
> Source tree: https://docs.emiprotechnologies.com/shopify-odoo-connector/v19/...
> Accessed 2026-06-30.

- **Binary files saved here:** none — **but** Emipro's docs use **real,
  addressable `.png` image URLs** (recorded per entry in the inventory), so these
  are the most directly capturable of the survey **if ChatGPT wants binaries
  saved later**. Not saved now to avoid repo bloat (sprint rule allows the
  markdown-inventory fallback).
- **Analysed inventory:** see the `Emipro (R3)` section of
  [`../../competitor-screenshot-inventory.md`](../../competitor-screenshot-inventory.md)
  — groups **EM-SETUP**, **EM-PRODUCT**, **EM-QUEUE**, **EM-SCHEDULER**,
  **EM-ORDER/RETURN/FULFILLMENT**, **EM-MARKETS/METAFIELDS/CHANNEL/PAYOUT/LOG**.
- **Key UX takeaways (most demonstrative set in the survey):** state-coloured
  **Data Queues** (Draft/Failed/Cancelled/Done) with per-line **Log Lines** and a
  processed ribbon; a **Mismatch Log / Log Book** with reason-coded failures;
  CSV/XLSX product-mapping fallback; per-process scheduler + "Last Operation
  Details"; explicit URL/trailing-slash auth-failure warning.
- Allowed extensions if files are later added: `.png .jpg .jpeg .webp`.
