# Teqstars (R2) — screenshots

> Visual evidence for **Teqstars — Shopify Connector for Odoo** (R2).
> Original access 2026-06-30; **rebaselined 2026-07-01 (Research Sprint C2)**.

## Current status (Sprint C2, 2026-07-01) — supersedes the note below

The **Teqstars documentation host (`docs.teqstars.com`) is now Accessible**
(the Sprint A/C 403 was a bot/UA filter, not a login wall — cleared with a
browser user-agent fetch; no auth bypassed). **31 Odoo 19.0 Shopify doc pages**
were read in full, containing **~98 embedded screenshots** (real `.jpg`/`.png`
page assets; pixels not individually inspected). **Binary files saved here:
still none** — the images were reviewed in place on the doc pages, not
downloaded. The page-classified, per-page screenshot inventory is in the
`Teqstars (R2)` **Sprint C2** section of
[`../../competitor-screenshot-inventory.md`](../../competitor-screenshot-inventory.md).
**Confidence rose from low/caption-only to page-classified** (demonstrated vs
claim vs implied vs not-found per `../../competitor-source-notes.md` R2 Sprint
C2 subsections) — no longer all `competitor claim`.

## Original note (2026-06-30, Sprint A/C) — retained as history, superseded above

- **Important access note:** the **Teqstars documentation host
  (`docs.teqstars.com`, 19.0 and 16.0) was HTTP 403 bot-blocked**, so **its
  rendered screenshots could NOT be reviewed** (no auth was bypassed). The only
  reviewable visuals were the **17 screenshot captions on the Odoo Apps Store
  listing** (https://apps.odoo.com/apps/modules/19.0/shopify) — captions only,
  rendered images not inspected.
- **Analysed inventory (Sprint A/C):** `TQ-*` caption groups (credentials,
  product-sync-config, order-config, workflow-config, webhook-config,
  import/export wizard, **Queue Manager**, payout, refund/cancel,
  fulfillment/tracking, dashboards) — all entries were `competitor claim`.

Allowed extensions if files are later added: `.png .jpg .jpeg .webp`.
