# Teqstars (R2) — screenshots

> Visual evidence for **Teqstars — Shopify Connector for Odoo** (R2).
> Accessed 2026-06-30.

- **Binary files saved here:** none.
- **Important access note:** the **Teqstars documentation host
  (`docs.teqstars.com`, 19.0 and 16.0) is HTTP 403 bot-blocked**, so **its
  rendered screenshots could NOT be reviewed** (no auth was bypassed). The only
  reviewable visuals are the **17 screenshot captions on the Odoo Apps Store
  listing** (https://apps.odoo.com/apps/modules/19.0/shopify) — captions only,
  rendered images not inspected.
- **Analysed inventory:** see the `Teqstars (R2)` section of
  [`../../competitor-screenshot-inventory.md`](../../competitor-screenshot-inventory.md)
  — `TQ-*` caption groups (credentials, product-sync-config, order-config,
  workflow-config, webhook-config, import/export wizard, **Queue Manager**,
  payout, refund/cancel, fulfillment/tracking, dashboards).
- **Confidence:** **low** — caption-only; all entries are `competitor claim`.
- Allowed extensions if files are later added: `.png .jpg .jpeg .webp`.
