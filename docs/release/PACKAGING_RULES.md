# Packaging Rules

## Current Rules

- `shopify_simulator` is internal QA only and must never be included in the public app-store package.
- Internal docs and commercial strategy docs must not ship inside the public package unless they are intentionally converted into merchant-facing docs.

## Future Expansion

Goal 10 should expand this document into a release packaging checklist after package structure and submission requirements are verified.
