# Technical Debt Register

> **No technical debt should be silently accepted.** Every shortcut, compromise,
> deferred hardening, or "good enough for now" decision is recorded here with
> its risk and a target resolution phase.

## How to use

1. Add a row whenever a compromise is knowingly accepted.
2. **Severity** values: `Low`, `Medium`, `High`, `Critical`.
3. **Reason accepted temporarily** must be honest (deadline, missing evidence,
   scope boundary) — not a justification that hides risk.
4. **Target resolution phase** ties the debt to a phase in
   [`../07-implementation-plan/`](../07-implementation-plan/) or a future
   session, so it cannot be forgotten.
5. **Status** values: `Open`, `Scheduled`, `In progress`, `Resolved`,
   `Accepted (won't fix)` — the last requires a linked decision record.

---

## Register

| ID | Date added | Area | Description | Severity | Reason accepted temporarily | Risk | Owner | Target resolution phase | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| _TD-000_ | _YYYY-MM-DD_ | _e.g. Sync engine_ | _What the debt is_ | _Low/Med/High_ | _Why now_ | _What could go wrong_ | _Who owns it_ | _Phase / session_ | _Open_ |
| TD-001 | 2026-07-07 | Core job framework (`shopify_connector_job.py`) | The already-merged `core_readiness_check` `job_type` (AR-019, Task 001) is target-less (`res_model`/`res_id`/`shopify_target_gid` empty) with no per-run `payload_hash` value, so its `idempotency_key` is identical across every run for a given store; since `(store_id, idempotency_key)` is uniquely constrained and never cleared on terminal state (unlike `operation_scope_key`), a **second** `core_readiness_check` job for the same store would collide with the first, forever. Task 003 (AR-027, accepted with F1 revision) fixes the identical defect for the new `core_test_connection` job type only, by design — `core_readiness_check` is explicitly excluded from Task 003's scope so as not to silently touch already-accepted Task 001 schema/behavior without a named authorization. Separately, `payload_hash` is being repurposed from its originally-planned semantics ("a hash of the normalized outbound payload," `core-naming-schema-planning.md:476-481`) into a per-run nonce for target-less job types — a naming/schema-cleanliness overload, not just a `core_readiness_check`-specific issue. Re-confirmed still reproducing via VAL-F1 in the 2026-07-07 live validation session (`../05-qa/task-003-validation-results.md`). | Medium | Task 003's own scope (AR-027, F1 revision) deliberately excludes fixing already-merged Task 001 schema; fixing it requires its own named authorization, not an incidental Task 003 side-effect | If `core_readiness_check` jobs are ever created more than once per store before this is fixed, the second attempt fails on `store_idempotency_key_uniq` instead of succeeding | Control room (ChatGPT) to route: fold into a future Task 003 (or later) gate by explicit name, or schedule as its own tiny follow-up patch (candidate name: "Task 001B — job-framework target-less idempotency patch"). **Task 004 gate routing note (2026-07-07, per [`DEC-021`](../04-decisions/DEC-021-val-b2-deferral-for-task-004.md)): TD-001 must be explicitly handled in the Task 004 gate-opening act — either named as a mandatory first Task 004 implementation acceptance criterion, or split out as its own separate pre-Task-004 patch.** **ROUTE SELECTED (2026-07-07), by ChatGPT's Task 004 gate-acceptance decision (see [`task-004-readiness-check-substrate-gate.md`](../07-implementation-plan/task-004-readiness-check-substrate-gate.md) §TD-001 route): fix TD-001 INSIDE Task 004, as the first mandatory Task 004 implementation acceptance criterion.** The "Task 001B" separate-patch candidate is retired — no longer the chosen route. This is still a routing decision, not a fix — **TD-001 is NOT fixed, NOT closed, and NOT resolved by this note**; it stays `Open` until the future Task 004 implementation PR actually merges the fix and it is validated (mandatory regression test: two `core_readiness_check` runs for the same store must not collide). **RESOLVED (2026-07-08), by PR #115 (merge commit `4145faf69ae6c1d541006890fc2b997fe4c07238`, merged into `Shopify-connector`) — the Task 004 readiness-check substrate now gives `core_readiness_check` job creation its own fresh UUID4 `payload_hash` nonce (inside the new `shopify_connector_readiness_check.py` service's own `run_for_store`, mirroring the accepted `core_test_connection` pattern; `shopify_connector_job.py` and `shopify_connector_store.py` were not touched), so a second `core_readiness_check` job for the same store no longer collides on `store_idempotency_key_uniq`. Regression evidence: `test_td001_repeated_readiness_job_does_not_collide` proves two `core_readiness_check` job-creation attempts for the same store both succeed with distinct `payload_hash`/`idempotency_key` values. This regression test, and the full `shopify_connector_core` suite, were both run live against a real Odoo 19/PostgreSQL registry on Odoo.sh (branch database `adamsmen-claude-task-004-readiness-substrate-me21qg-34601850`) before merge — full module: `0 failed, 0 error(s) of 78 tests`; focused `TestReadinessCheck`: `0 failed, 0 error(s) of 31 tests` — see [`task-004-validation-results.md`](./task-004-validation-results.md) for the full evidence record.** | **Task 004 implementation** — fixed inside Task 004's own scope, as its first mandatory acceptance criterion, per the gate-acceptance decision above; no separate follow-up patch is planned. **Resolved by PR #115 (2026-07-08).** | Resolved |
| TD-002 | 2026-07-08 | Readiness scopes / Shopify access scopes (`shopify_connector_readiness_check.py`) | The already-shipped `REQUIRED_MVP_SCOPES` constant includes `read_fulfillments`, but current official Shopify Admin API access-scopes documentation (re-verified 2026-07-08, see [`../01-research/shopify-token-acquisition-notes.md`](../01-research/shopify-token-acquisition-notes.md) §3 and [`../04-decisions/DEC-023-token-acquisition-and-val-b2.md`](../04-decisions/DEC-023-token-acquisition-and-val-b2.md) §5) indicates that `read_fulfillments` governs only the `FulfillmentService` resource, not read access to an order's `Fulfillment`/`FulfillmentOrder` data — fulfillment-object access is actually covered by `read_orders` (already in the required set) and/or the `FulfillmentOrder`-family scopes, depending on which fulfillment API model the connector's fulfillment domain ultimately adopts. This is a least-privilege/readiness correctness concern, not merely a naming nitpick: requiring a scope that does not grant the access its name implies is exactly the kind of over-broad/mis-scoped grant DEC-004's least-privilege posture exists to avoid, and it must be resolved before any customer-facing setup/readiness claim relies on this check. **Clarification note (2026-07-09, AR-038 audit session — status unchanged, still Open, no fix authorized):** this row's original wording ("depends on which fulfillment API model … the connector ultimately adopts … which is not yet decided") is imprecise as written — the **write-side** fulfillment model was already decided before this row was logged: [`DEC-011`](../04-decisions/DEC-011-fulfillment-architecture-strategy.md) (Accepted 2026-07-02) fixes FulfillmentOrder-based mutations exclusively, and RA-022 makes the legacy flow a binding rejection. The genuinely open dependency is narrower: the **exact scope set** the fulfillment domain needs. Per Shopify's official access-scopes page (re-verified 2026-07-09, Accessible, https://shopify.dev/docs/api/usage/access-scopes): `read_fulfillments`/`write_fulfillments` govern the `FulfillmentService` resource only, while the `FulfillmentOrder` resource is governed by the `read/write_assigned_fulfillment_orders`, `read/write_merchant_managed_fulfillment_orders`, `read/write_third_party_fulfillment_orders` (+ `read_marketplace_fulfillment_orders`) family, with `write_merchant_managed_fulfillment_orders` governing fulfillment creation for merchant-managed FulfillmentOrders (the connector's DEC-011 target case). The fix therefore waits only on the fulfillment domain's own naming/final-prompt pass picking its exact scope list (see `../08-release-readiness/open-points-closure-register.md` OP-20/OP-03) — not on any undecided architecture. Routing options unchanged. **Wave 0 research closure (2026-07-15; code status still Open):** official Shopify evidence and Task 014 D-014-2 now fix the exact narrow route: replace `read_fulfillments` with `read_merchant_managed_fulfillment_orders`, and require `write_merchant_managed_fulfillment_orders` only when the fulfillment domain is enabled for the merchant-managed write flow. See `../01-research/wave-0-roles-permissions-and-fulfillment-scope-refresh.md`. DEC-033 proposes control-room acceptance; no addon fix is authorized by this note. | Medium | The exact correction is now researched and routed, but Wave 0 is docs-only and DEC-033 remains Proposed; addon implementation belongs to the accepted Task 014/Wave 4 allowlist | The readiness-check's required-scopes gate may ask for or validate a misleading scope, and may imply fulfillment-readiness assurance that is not actually proven by the granted scope set | Claude control room to accept/revise DEC-033; Sol to implement Task 014 D-014-2 in Wave 4 after Layer 2, with focused readiness-scope tests | Before customer-facing setup/readiness UI or fulfillment domain implementation, whichever comes first | **In progress (2026-07-22, Wave 4 Gate B, draft PR #189 — static swap implemented and unit-tested, unmerged, pending independent Claude review and Gate C Odoo.sh runtime)** — the one named core edit swaps `read_fulfillments` → `read_merchant_managed_fulfillment_orders` in `shopify_connector_core/models/shopify_connector_readiness_check.py::REQUIRED_MVP_SCOPES`, with the matching core-test assertion (`test_readiness_check.py::test_required_scopes_use_merchant_managed_fulfillment_orders`); the fulfillment addon appends a conditional essential `write_merchant_managed_fulfillment_orders` readiness check (active only when `fulfillment_domain_enabled`). Runtime execution of the readiness tests is Gate C (`IMPLEMENTED—RUNTIME PENDING`); the static swap is verified. **Status correction (2026-07-23, Wave 4 Tier-1 findings synthesis, finding F-12):** this row was previously marked `Resolved`, which broke this same register's own TD-001 precedent (a named merge commit into a shared branch plus completed, passing, pre-merge Odoo.sh runtime evidence) — PR #189 is still an open, unmerged draft with no independent review and no Gate C runtime evidence, so `Resolved` was premature. Reverted to `In progress` per this file's own status legend (line 16). Will move to `Resolved` only once (a) independent Claude review of PR #189 completes per CLAUDE.md §13/DEC-040, (b) Gate C Odoo.sh runtime evidence exists, and (c) the PR merges. |
| TD-003 | 2026-07-23 | Fulfillment UI vocabulary (product docs vs shipped Wave 4 code) | The pre-implementation product docs use several review-case/status vocabulary values that **differ from the shipped Wave 4 `shopify_connector_fulfillment` code** (verified against exact head `2d9cff02dd5459f4ec7afee33c84fec5d00b0b8a`): `fulfillment-operating-modes.md` §3 uses origin classes `external_service`/`carrier_event_only`, but the code `ORIGIN_CLASS_SELECTION` is `connector`/`external_merchant`/`external_app`/`external_unknown`; §4 uses review reason `over_fulfillment`, but the evidence `REVIEW_REASON_SELECTION` uses `quantity_overrun` (and the core job persists `ambiguous_match` as `manual_review_subreason`); §5 uses reconciled states `under_review`/`auto_matched`/`rejected`, but the code `RECONCILED_STATE_SELECTION` is `observed`/`review`/`acknowledged`/`applied`/`superseded`. This is a documentation/consistency debt, not a code defect — the code is authoritative and internally consistent. Recorded so the Wave 5 U1 UI binds to the **code** values and the product docs are annotated as carrying superseded vocabulary. **Annotation applied 2026-07-23 (control-room comment `5056513213`): both `fulfillment-operating-modes.md` and `shopify-fulfillment-status-model.md` now carry a non-destructive superseded-vocabulary note + section-anchored pending-edit table; the docs remain `Proposed` and their section values are left unchanged. The remaining fix — the code→label copy deck and any eventual product-doc value edit — stays a Wave 5 U1 implementation deliverable.** Detail: `docs/07-implementation-plan/wave-5-u1-gate-a/u1-backend-ui-contract-inventory.md` §10. **Status-layer reset addendum (2026-07-23, control-room ruling `5058042330`):** the U1 status-badge taxonomy was reset to the canonical status-source matrix (contract §12) — `display_status_*` = A7 (not A5), A2 deferred (no seam). **Final-backend reconciliation addendum (2026-07-25, PR #194 re-anchor onto `2583081f`):** the doc↔code vocabulary divergence is **re-verified and unchanged** — every superseded product-doc value listed above is still superseded at the current integrated implementation. Two additions to this row's scope, both for the U1 copy deck: (a) the evidence `review_reason` selection is now **21** values, not 20 — the Wave 4 Tier-1 correction (Theme H) added `external_fulfillment_observed` ("External Fulfillment Observed"), so a copy deck that maps only the historical 20 would leave a real code value rendering as an unmapped raw string; (b) the shipped SEC-2 group `name` strings are `User` and `Administrator` **within the `Shopify Connector` privilege**, not the literal "Connector User"/"Connector Administrator" strings this program uses as role concepts — the copy deck must decide explicitly which string each surface shows, and must never rename a group to match a document (logged as OQ-5 in `u1-risks-and-open-questions.md`). The two residual un-annotated `under_review` locations below are unchanged and still outside the U1 Gate-A allowed-files set. **Residual superseded-vocabulary locations still un-annotated (outside the reset's allowed-files set — logged here for a separately-authorized product-doc reconciliation, not edited):** `under_review` in `docs/05-qa/fulfillment-mode-uat-matrix.md` (UAT-FM-3.3) and `docs/02-product/premium-ux-master-specification.md` (§3 S21); code value is `review`. | Low | Scope boundary — surfaced during docs-only U1 Gate A; the U1 copy deck (a U1-implementation deliverable) is the correct place to fix the code→label mapping; the product docs' non-destructive superseded-vocabulary annotations were applied in the 2026-07-23 correction session (authorized by control-room comment `5056513213`), leaving the docs `Proposed` and their section values unchanged | If a future U1 UI or test author copies a product-doc value (e.g. `over_fulfillment`, `external_service`) into a view/selection/test instead of the code value, it will reference a non-existent selection and fail at runtime — exactly the invented-value trap the Gate A static-validation rule guards against | Wave 5 U1 implementation (copy deck) + a light product-doc annotation pass | Wave 5 U1 implementation | **Implemented (2026-07-27, this branch, unmerged and unreviewed) — pending external validation.** Correction: `TD-003: authoritative vocabulary reconciliation` (`b99df8d`). `docs/06-prompts/connector-vocabulary-reconciliation.md` is now the single authoritative code→label mapping: origin classes, reconciliation states, all **21** review reasons including `external_fulfillment_observed`, and the role table recording that the shipped `res.groups` names are `User`/`Operator`/`Reviewer`/`Administrator`/`Auditor` within the `Shopify Connector` privilege — never the "Connector User" concept strings — and that three of the five carry no `privilege_id` and so are not on the user form at all (closing OQ-5: the document changes, never the group). The two residual stale locations are corrected to the real value with a pointer to the reconciliation: `fulfillment-mode-uat-matrix.md` (UAT-FM-3.3) and `premium-ux-master-specification.md` (§3 S21). The 2026-07-23 annotations elsewhere are left exactly as that pass made them; this is a bounded vocabulary correction, not a product-document rewrite. Regression: `test_vocabulary_reconciliation.py`, 7 tests holding the document to the code in both directions. **Not claimed:** no independent review of the corrected head, no Odoo.sh runtime, no live-Shopify evidence, no UAT. Moves to `Resolved` only after all four. |
| TD-004 | 2026-07-26 | Product export — media replacement (`shopify_connector_product_export`) | **Media export is append-only, so a replaced image is never removed from the Shopify product.** When an Odoo image changes, the new image is uploaded and appended; the superseded connector-created `File` **and its product association** are retained and the registry row is flagged `orphan_cleanup_candidate`. The product therefore accumulates one media entry per image version until an operator removes the old ones in Shopify by hand. This is a deliberate compromise, not an oversight: removing the old association safely requires proving the `File` is not used elsewhere, and the 2026-07 `File` interface exposes `alt`/`createdAt`/`fileErrors`/`fileStatus`/`id`/`preview`/`updatedAt` and **no reverse-reference connection** — re-verified against the pinned reference on 2026-07-26. `fileDelete` appears in no GraphQL document in the module, and no detach or reorder mutation is reachable from any code path. | Medium | The 2026-07-26 control-room ruling requires append-only; the alternative (detach-only, per D-015B-2/6) would still need the same non-existent exclusivity proof to be safe, and auto-deleting a File a merchant may have reused elsewhere is a worse failure than an extra image | An operator who repeatedly changes a product image sees the product's media list grow, and must clean it up manually. A merchant who does not notice keeps stale images visible on the storefront alongside the current one | Control room to schedule the explicit, admin-gated manual orphan-cleanup capability D-015B-2 already names as the only path that may ever call `fileDelete` | Post-MVP, or earlier if Shopify documents a testable `File` reverse-reference query | **Retained as a scheduled MVP/UAT limitation (re-affirmed 2026-07-27). NOT resolved, and not corrected in the 2026-07-27 cycle.** Media export stays append-only for this cycle: `fileDelete` is called nowhere, no automatic detach or orphan cleanup was added, and none may be added without an independently reviewed ownership and variant-association safety design. **Exact UAT limitation to record and test for:** replacing a product image leaves the older media association in place on the Shopify product, so the product accumulates one media entry per image version until an operator removes the old ones by hand in Shopify. The superseded registry row is flagged `orphan_cleanup_candidate`, which is a queue for a future explicit capability and is on no automatic path. |
| TD-005 | 2026-07-26 | Product export — media scopes (`shopify_connector_product_export`) | **Media export needs `write_files`, which is broader than the `write_images` the earlier least-privilege analysis assumed.** `fileCreate` accepts `write_files`, `write_themes` **or** `write_images`, but `fileUpdate` — the only 2026-07 mutation that associates an **existing** `File` with a product, and therefore the only path that can honour D-015B-4's READY gate — accepts only `write_files` or `write_themes`. `write_themes` is never requested and its presence is a readiness failure. The narrower `write_images` is only obtainable by abandoning the READY gate and associating through `productUpdate(media:)` with a staged URL, i.e. by submitting an association before Shopify reports `READY`. | Low | D-015B-4's READY gate is binding and the wider scope is the documented cost of honouring it; the trade is a product decision and is recorded rather than made silently | `write_files` grants write access to every file in the store, not only product images. A defect or a compromised token could touch unrelated store files | Control room to rule on whether the READY gate or the narrower scope wins, if it ever wants the narrower scope | Wave 6 / release-readiness scope review | **Retained as an accepted MVP scope trade-off (re-affirmed 2026-07-27), pending release-readiness review. NOT resolved.** The READY-gated media design and its `write_files` requirement are both kept. `fileUpdate` — the only 2026-07 mutation that associates an existing File with a product, and therefore the only path that can honour D-015B-4's READY gate — accepts `write_files` or `write_themes` and not `write_images`. `write_themes` is never requested and its presence remains a readiness failure; the READY gate was not abandoned to claim the narrower scope. **The accepted cost:** `write_files` grants write access to every file in the store, not only product images. |
| TD-006 | 2026-07-26 | Product export — U3 UI completeness (`shopify_connector_product_export`) | **U3 is delivered without the browser-evidence layer its own packet makes an acceptance criterion.** Shipped: the export preview/diff surfaces, the confirm wizard, the media registry surface, the settings/ownership/retention surface, the reconnect block and the menu branch — all Odoo-native, with server-side visibility, wiring and delegation tested. Absent: any Owl component (S7 is not the Owl diff surface the master specification assigns to U3), all `web_tour` tours, all HOOT unit tests, the screenshot set, the §13 accessibility checklist, `ui-u3-copy-deck.md`, the reconnect/backfill banner and watermark progress (S25/S26), the diagnostics screen (S31), and the motion/keyboard/contrast polish pass. | Medium | Capacity, not a dependency. Stated plainly because the packet's UI acceptance criteria are explicit and this batch does not meet them | A reviewer or a later session could read "U3 delivered" as complete and skip the browser-evidence layer, leaving the operator surfaces unverified in a browser and the accessibility gates unchecked | Control room to schedule the U3 completion slice; the export backend it consumes is complete and tested | Wave 5 U3 completion slice, before Wave 6 UAT | **Largely resolved (2026-07-26, this branch, unmerged and unreviewed).** Delivered since this row was written: the S7 Owl diff surface and its read-only projection service; the reconnect/backfill catch-up surface and the store banners (S25/S26); the export diagnostics screen (S31); `docs/06-prompts/ui-u3-copy-deck.md`; three `web_tour` tours (navigation, review disclosure, keyboard/`:focus-visible`) and an 11-test HOOT suite, both **executed** in a real Chromium; and the motion/RTL/contrast pass implemented structurally (logical properties throughout, every transition behind `prefers-reduced-motion: no-preference`, design-system token pairs only). **Update (2026-07-27, same branch).** The five items listed as still open are now delivered and MEASURED, by `shopify_connector_core/tests/test_ui_visual_evidence.py`: a screenshot set at 1366/768/390 CSS px plus RTL, reduced-motion and focused-control captures; a contrast table computed from rendered colours against WCAG 2.2 SC 1.4.3/1.4.11; a reduced-motion check run under an emulated `prefers-reduced-motion: reduce`; and a focus-indicator check with `:focus-visible` forced through `CSS.forcePseudoState`. Artifacts: `docs/05-qa/evidence/wave-5-u2-u3-2026-07-27/`. **Rendering it found what reading it could not:** the RTL implementation did not work. Odoo 19's backend never sets `direction: rtl` (its RTL mechanism is rtlcss bundle-flipping), the connector stylesheets are written entirely in logical properties which resolve against `direction`, and `dir="auto"` resolved from the English content — so nothing mirrored. Both connector Owl roots now bind `dir` to the user's locale. **Still not claimed:** no Odoo.sh/independent-review/UAT evidence. Full record: [`ui-u3-validation-results.md`](./ui-u3-validation-results.md) and [`ui-u2-validation-results.md`](./ui-u2-validation-results.md). Moves to `Resolved` only after independent review and an exact-head Odoo.sh run. |
| TD-007 | 2026-07-26 | Product export — options on an existing product (`shopify_connector_product_export`) | **The connector never mutates product options on an existing Shopify product.** A divergence between the Odoo attribute structure and the remote option structure is disclosed in the preview as `remote_option_divergence`, refused, and additionally withholds every variant write for that product (because `optionValues` are positional against the remote option set). Options are therefore exportable only on the create path. No 2026-07 option mutation was source-verified as non-destructive — each either removes values or reshapes the variant matrix — so none is used. | Medium | The ruling permits dedicated option mutations "only where source verification proves the operation non-destructive", and no such proof exists | A merchant who changes an option name or value in Shopify puts that product into a state where the connector exports nothing for it until somebody reconciles the structure by hand. The operator sees a clear refusal rather than a silent partial export, but the product is stuck | Control room to authorize a source-verification pass over `productOptionUpdate`/`productOptionsCreate`/`productOptionsDelete`, or accept the refusal as MVP behaviour | Post-MVP, or a Wave 5 follow-up if operators hit it | **Retained as an accepted MVP limitation (re-affirmed 2026-07-27), pending a separately authorized option-reconciliation feature. NOT resolved.** The fail-closed refusal stands: a divergence between the Odoo attribute structure and the remote option structure is disclosed as `remote_option_divergence`, refused, and additionally withholds every variant write for that product because `optionValues` are positional against the remote option set. No destructive option restructuring was added in the 2026-07-27 cycle. **Exact UAT limitation to record and test for:** a merchant who renames an option or changes an option value in Shopify puts that product into a state where the connector exports nothing for it until somebody reconciles the structure by hand. The operator sees a clear refusal rather than a silent partial export, but the product is stuck. |
| TD-008 | 2026-07-26 | Core API client (`shopify_connector_api_client.py`) | **The store's `api_version` column still exists and is still writable by a superuser/RPC caller, even though it is no longer authoritative.** The endpoint is built from the centralized `SHOPIFY_API_VERSION` constant, and a store whose recorded value disagrees now fails closed before any request — so a divergent value cannot cause a wrongly-versioned call. What remains is a field that *looks* like configuration and is not: it can only ever agree with the constant or break the store. It is `readonly="1"` on the store form and every fixture sets `2026-07`, so the exposure is narrow. | Low | Making the column computed-from-constant would change an already-merged required field and rewrite existing rows; doing that as a side effect of this batch is exactly the kind of unauthorized schema touch the register exists to prevent | An operator or integration that writes a different value through RPC silently disables the store's Shopify calls (fail-closed, so not dangerous — but the failure reads as a connection problem rather than as the configuration mistake it is) | Control room to route: either make it a stored-computed mirror of the constant with a migration, or retire the column | Wave 6 / release-readiness schema pass | **Implemented (2026-07-27, this branch, unmerged and unreviewed) — pending external validation.** Correction: `TD-008: the stored API version may only be the connector constant` (`f7986b5`). The column stays and no schema migration is performed. A default gives every new store `SHOPIFY_API_VERSION` without anyone passing it, and an `@api.constrains` refuses any row that disagrees — `constrains` rather than a `models.Constraint` because the acceptable value is a Python constant, and because it fires on `sudo()`, `load()` and RPC, the three paths the form's `readonly` attribute does not cover. Rows already holding the constant are unaffected. Regression: `test_api_version_binding.py`, 12 tests. Two pre-existing tests that assert genuine defence-in-depth behaviour on a divergent row now plant it in SQL, since an upgraded database can still contain one. **Not claimed:** no independent review of the corrected head, no Odoo.sh runtime, no live-Shopify evidence, no UAT. Moves to `Resolved` only after all four. |
| TD-009 | 2026-07-26 | Operator UI unit tests (`shopify_connector_core/static/tests/shopify_connector_dashboard.test.js`) | **The U0 dashboard HOOT suite has never executed, and still cannot register.** Two separate problems. First, **no runner existed**: a `.test.js` in `web.assets_unit_tests` is only run by something that opens `/web/tests`, and nothing in this repository did — so the file has been shipping since U0 as documentation with a misleading extension. Second, the file is **broken**: it called `mockService` at module scope, which mutates the services registry while HOOT is still registering the suite and fails with `HootError: error while registering suite \"shopify_connector_dashboard\"`. The module-scope call was corrected in this batch (moved into `beforeEach`), and **the suite still fails to register for a further, unidentified reason**. The runner added in this batch (`test_u3_hoot_suite.py`) is therefore scoped to the export suite, which passes 11/11. | Medium | Diagnosing the residual registration failure needs its own investigation; guessing at it inside a U3 batch would mean editing an already-merged U0 test file on speculation, and the export suite's own evidence does not depend on it | The U0 operational dashboard — the connector's primary operator surface — has **no executed unit evidence at all**. Its render states, navigation and accessible labels are asserted only by a file that does not run | Control room to schedule a short U0 HOOT-repair slice; once it registers, widen the runner in `test_u3_hoot_suite.py` so one runner covers both suites | Wave 5 completion follow-up, before Wave 6 UAT | **Resolved (2026-07-27, this branch, unmerged and unreviewed).** **Root cause identified, and it was never in the dashboard test.** The failure is `Error while loading "@shopify_connector_core/js/tours/shopify_connector_u0_tour": TypeError: Cannot destructure property 'stepUtils' of 'require(...)' as it is undefined.` `web.assets_unit_tests_setup` does `('include', 'web.assets_backend')`, and HOOT builds a per-suite MODULE SET from the test file's addon plus that addon's **declared Odoo dependencies**, then starts every module in it (`web/static/tests/_framework/module_set.hoot.js::defineModuleSet`, read at the pin). `web_tour` is not a declared dependency of `shopify_connector_core`, so `@web_tour/tour_utils` was filtered OUT of the set while the tour that imports it was not: the tour threw, the whole module set failed, and the suite that merely shared the bundle could not register. **Fix:** the three connector tours moved from `web.assets_backend` to `web.assets_tests` — Odoo's own home for `HttpCase` tours (`account`, `crm`, `calendar` … at the pin), loaded into the backend only when `test_mode_enabled` and present in no unit-test bundle. **Evidence:** `TestConnectorHootSuite` now runs BOTH suites in separate browser runs and asserts each one's success marker AND its exact executed count — `shopify connector dashboard` 8/8, `shopify connector export diff` 11/11 — with `test_every_hoot_file_is_executed` proving the inventory matches the `.test.js` files on disk, and `test_tours_are_not_in_a_unit_test_bundle` failing if a tour is ever put back into `web.assets_backend`. Moves to the same footing as TD-001 only after independent review and an exact-head Odoo.sh run. |
| TD-010 | 2026-07-26 | Test harness — browser evidence (`tools/run_connector_suite.sh`, CI) | **`websocket-client` is not installed by the canonical runner, and without it every `HttpCase` browser test SKIPS while the suite still reports `0 failed, 0 error(s)`.** That is the most dangerous shape a test result can take: a green run that proves nothing, in which a tour "passes" by never executing. It was found in this session only because the tours were expected to fail and did not. The package is not in Odoo's `requirements.txt`, so the venv the runner builds does not get it. Separately, the runner sets no `ODOO_BROWSER_BIN`, so a machine whose Chromium is not on `PATH` under one of Odoo's four expected names silently skips as well. | **High** | The fix is a one-line dependency and an env var, but it changes what the canonical runner and CI *execute*, and the runner is the repository's evidence instrument — changing it belongs in its own named, reviewed change rather than riding inside a UI batch | Any past or future "full suite green" that includes tour or HOOT tests may have executed **none of them**. Every browser-evidence claim made without verifying that the tests were not skipped is unsound | Control room to authorize a runner change: add `websocket-client` to the venv install, resolve a browser explicitly, and — most importantly — make the runner **fail** rather than pass when a browser test skips, so this class of false green cannot recur | Before any Wave 5 gate quotes browser evidence | **Resolved (2026-07-27, this branch, unmerged and unreviewed).** `tools/run_connector_suite.sh` now: (a) installs `websocket-client` on **every** run, not only when the venv is created, so a cached venv cannot keep skipping silently; (b) resolves the browser explicitly and exports `ODOO_BROWSER_BIN`, including the Playwright bundle path that matches none of Odoo's four expected PATH names; (c) **proves** both prerequisites before running anything browser-bearing, and additionally boots the browser headlessly against `about:blank` — because a binary that resolves and then dies also becomes a `SkipTest`; (d) inspects every pass's log and FAILS the run on an unexpected skip, a required tour that did not start, a short `tour succeeded` marker count, or a missing HOOT evidence line. **The skip allowance is bound to one exact identity AND its exact reason** (`TestMutationRecovery.test_real_process_death_harness` / `real process-death harness is opt-in outside Odoo.sh`) — deliberately not a "one skip allowed" count, which would let any test skip. **Regression verification:** `tools/run_connector_suite.sh --self-test` drives all of it against synthetic logs and asserts each check rejects what it must — including the subtle case of a tour that never ran while the marker count still adds up, and the sanctioned test skipping for a *different* reason. `test_suite_runner_fails_closed.py` runs that mode inside the ordinary suite, and `test_phase_contract.py::test_every_tour_test_is_listed_in_the_suite_runner` asserts the required-tour inventory equals the tours that actually exist, so coverage cannot shrink silently. Moves to the same footing as TD-001 only after independent review and an exact-head Odoo.sh run. |
| TD-011 | 2026-07-27 | Product export — media idempotency (`shopify_connector_media_export_service.py`) | **A failed, non-associated media step cannot be retried or resumed.** `_enqueue_media_step` overrides the enqueue nonce with a deterministic `payload_hash = '<step>:<checksum>'`, which feeds `idempotency_key`. Unlike `operation_scope_key`, `idempotency_key` is **never cleared on a terminal state**, and `_ensure_media_row` reuses the existing row, so a legitimate re-preview reproduces every key component and collides permanently. The row cannot be unlinked (`unlink` raises unconditionally) and the checksum index blocks a replacement. This is the same shape as the resolved TD-001, chosen in the opposite direction. | High | Found by the 2026-07-27 correction session while closing the P0; correcting it needs a resume-vs-retry identity decision that must not weaken uniqueness globally or delete audit history, which is a design call rather than a mechanical edit | A media step that fails once can never complete. Under `_dispatch_one` the resulting `23505` is not in `PG_CONCURRENCY_EXCEPTIONS_TO_RETRY`, so it escapes `_drain_one` and ends the whole drain pass, taking unrelated stores' queued jobs with it | Next authorized correction session | Before external validation of PR #204 | **Implemented (2026-07-27, this branch, unmerged and unreviewed) — pending external validation.** Correction: `TD-011: a stopped media export can be resumed` (`ad66530`). A `resume_attempt` ordinal on the media row now feeds the payload hash, so re-dispatching an admitted job replays under its original identity while an authorised resume gets its own. `(store_id, idempotency_key)` is not weakened anywhere; no job, log, attempt, checksum or remote GID is rewritten. `_resume_media_export` refuses an already-associated image outright and refuses while the previous attempt's outcome is still unresolved, routing to the existing reconciliation boundary rather than racing it. `_admit_media_job` contains a duplicate admission in a savepoint so a `23505` can no longer abort the transaction and end the drain pass for unrelated stores. Regression: `test_media_resume.py`, 15 tests. **Not claimed:** no independent review of the corrected head, no Odoo.sh runtime, no live-Shopify evidence, no UAT. Moves to `Resolved` only after all four. |
| TD-012 | 2026-07-27 | Inventory — first-push preview (`shopify_connector_inventory_service.py`) | **The first-push confirmation flow has no production entry point.** `_enqueue_first_push_preview` has zero production callers (its only two callers are tests), and its handler is the only writer of `first_push_state = 'previewed'`. No button, cron, RPC-exposed method, wizard or other job type reaches it, so the `pending -> previewed` transition cannot occur in production and the `Confirm First Push` control can never render. The shipped view copy promises "the preview runs on the next scheduled pass", and the only scheduled pass enqueues `push_sync` exclusively. The U2 browser evidence drives this flow only because the tour manufactures `previewed` via `sudo()`. | High | Found 2026-07-27; the fix must add a sanctioned production path that preserves the accepted Operator/Administrator matrix, company isolation and server-side admission, and must NOT be solved by relaxing the server guard to match the view | The sanctioned first-push confirmation workflow cannot be completed by any operator. The UI promises a trigger that does not exist | Next authorized correction session | Before external validation of PR #204 | **Implemented (2026-07-27, this branch, unmerged and unreviewed) — pending external validation.** Correction: `TD-012: the first-push ceremony is reachable from production` (`c341280`). The scheduled push scan now admits an `inventory_first_push_preview` for every push-enabled pair still in `pending` — the trigger the shipped `pending` empty state already promised ("the preview runs on the next scheduled pass"). Admitted *instead of* a `push_sync`, because an unconfirmed pair has nothing to push and both job types share the pair's `operation_scope_key`. Authorisation is split from admission: `_enqueue_first_push_preview` keeps the Operator/Administrator gate and now checks company access before elevating; `_admit_first_push_preview` is the system path the scan uses. Also fixes the duplicate-prevention query both paths depend on, which compared `operation_scope_key` against the pair key and so could never match. Regression: `test_inventory_first_push_reachability.py`, 13 tests, none of which writes `first_push_state`; plus a U2 browser tour that starts at the model default and reaches `confirmed` through the real scan job, the real preview job and a real button press. **Not claimed:** no independent review of the corrected head, no Odoo.sh runtime, no live-Shopify evidence, no UAT. Moves to `Resolved` only after all four. |
| TD-013 | 2026-07-27 | Product export — preview expiry (`shopify_connector_product_export_service.py`) | **Preview expiry is never re-checked at any mutation boundary.** `_assert_confirmed_preview_pre_c2` is documented as re-verifying "immediately before every mutation", but checks only `state`, `confirmed_uid` and `confirmed_at`; it never calls `_is_expired()`. No mutation family (create, scalar update, variant create/update, identity/metafield, media stage/file-create/associate, or child-step enqueue) re-checks expiry. The create and scalar-update paths re-derive their payload from the **live** template at mutation time while gating on a confirmation taken against older data. The media upload path re-checks its image checksum, which is a content check, not an expiry check. | High | Found 2026-07-27; correcting it touches every mutation family and needs time-crossing tests per family | A confirmation can authorise a write arbitrarily long after it was given, against data the operator never reviewed. The shipped UI states that "a stale confirmation can never authorise a write" | Next authorized correction session | Before external validation of PR #204 | **Implemented (2026-07-27, this branch, unmerged and unreviewed) — pending external validation.** Correction: `TD-013: expiry re-checked at every mutation boundary` (`02b14bc`). `_assert_preview_unexpired_pre_c2` is reached by all 8 mutation families — the 5 product families through `_assert_confirmed_preview_pre_c2`, and the 3 media families directly, which had never passed through it because they resolve their preview by row rather than by job. Pre-transport it raises the repository's "nothing was transported" signal, which blocks the job with an operator-facing reason. Post-transport, `_advance_plan` keeps the completed step's outcome, marks the remaining steps `blocked` and enqueues no child — expiry may not retroactively deny a mutation that already reached Shopify. Regression: `test_export_mutation_expiry.py`, 13 tests covering both expiry directions across all 8 families, plus a structural guard that a new family cannot skip the check. **Not claimed:** no independent review of the corrected head, no Odoo.sh runtime, no live-Shopify evidence, no UAT. Moves to `Resolved` only after all four. |
| TD-014 | 2026-07-27 | Core dispatch — PERF-1 backpressure (`shopify_connector_job_dispatch.py`) | **The backpressure rule can never fire.** `BACKPRESSURE_HEALTH_STATES = ('throttled', 'degraded')`, but production has exactly one writer of `api_health_state` and it writes `'normal'`; `'throttled'` has never been written by production code in any commit, and the last `'degraded'` writer was deleted by `48ab97c` in this same PR that added the reader in `de257b2`. The source guard that would have caught it was inverted to `assertNotIn` on a comment asserting `degraded` "is still reachable through other health transitions", which is false at this SHA. The PERF-1 packet specified **throttle head-room** from Shopify's `throttleStatus` with a documented linear back-off; `_parse_throttle_status` already extracts `maximumAvailable`/`currentlyAvailable`/`restoreRate` on every response and has **zero consumers**. | High | Found 2026-07-27; the accepted mechanism has to be rebuilt on the real throttle signal, or the substitution declared as an explicit evidence-backed deviation | Drain throughput does not self-limit under Shopify throttle pressure, which is the entire purpose of D-PERF1-4. The store-degraded UI banner is likewise unreachable | Next authorized correction session | Before external validation of PR #204 | **Implemented (2026-07-27, this branch, unmerged and unreviewed) — pending external validation.** Correction: `TD-014: PERF-1 backpressure driven by real throttle data` (`f1c1470`). The accepted D-PERF1-4 mechanism is used as specified; what was missing was its input. `_normalize_response` — the single choke point both `execute` and `execute_business` land on — now folds the parsed `throttleStatus` into four store fields (three floats and a timestamp; nothing that could carry a payload, header or credential). Head-room below 20% defers the store via the existing lever, and recovery above 50% releases it, the gap being hysteresis so the state cannot flap. Deferred stores recover without any Shopify call: the bucket refills at `restoreRate` points/second (https://shopify.dev/docs/api/usage/limits, read 2026-07-27), so `_recover_throttled_stores` recomputes head-room from the last observation and the clock at the start of each drain pass — without it the first deferral would be permanent, which is a worse outage than the throttling. Absent or malformed data changes nothing. No bucket size is hard-coded; thresholds are fractions of the store's own `maximumAvailable`, so MBQ-51 is untouched. Regression: `test_throttle_backpressure.py`, 16 tests. **Not claimed:** no independent review of the corrected head, no Odoo.sh runtime, no live-Shopify evidence, no UAT. Moves to `Resolved` only after all four. |
| TD-015 | 2026-07-27 | Product export — reconnect reconciliation (`shopify_connector_product_export_seams.py`) | **The PD-PX-7 reconnect reconciliation pass does not exist.** The specification requires an automatic pass over all previously exported bindings on reconnect (re-read each by GID, verify existence, verify the variant GID set, verify media checksums; deleted/archived remote routes to review, never silent re-create) with exports blocked until it completes. What shipped is a manually-invoked button that expires open previews, makes no Shopify call, verifies nothing, covers only bindings that happen to have an open preview, and blocks nothing. The export module contains zero references to `action_reconnect`, so the real credential-rotation lifecycle never invokes it. | High | Found 2026-07-27; implementing the specified boundary is a substantial addition needing its own authorization | After a credential rotation or reconnect, exports resume against bindings that were never re-verified. Remote products deleted or archived while disconnected are not routed to review | Next authorized correction session | Before external validation of PR #204 | **Implemented (2026-07-27, this branch, unmerged and unreviewed) — pending external validation.** Correction: `TD-015: the PD-PX-7 reconnect reconciliation pass` (`091d3e7`). `action_reconnect` is overridden in the export module and compares `connection_generation` across `super()` — core bumps it exactly once and only on success — so the block attaches to the lifecycle rather than to a button somebody must remember. One job per bound template re-reads its product by GID through the module's existing `_read_remote_product` seam and checks existence, archive state, the governed variant GID set and connector-owned media identity; a remote on a different `myshopifyDomain` is refused outright. Missing, archived or divergent goes to explicit review and is never re-created — a property of there being no mutation path in the handler at all, asserted structurally. Exports are blocked at `enqueue_preview` and again at `_enqueue_apply` until a terminal verdict; nothing to verify settles as `complete`, and a transport failure leaves the block in place. Regression: `test_export_reconnect_reconcile.py`, 23 tests, zero Shopify operations. **Not claimed:** no independent review of the corrected head, no Odoo.sh runtime, no live-Shopify evidence, no UAT. Moves to `Resolved` only after all four. |
| TD-016 | 2026-07-27 | Test harness — overflow instrument (`test_ui_visual_evidence.py`) | **The horizontal-overflow check cannot fail for a connector surface.** It compares `document.documentElement.scrollWidth` against `window.innerWidth`, but every connector surface renders inside `.o_action_manager`, which carries an unconditional `overflow: hidden` in the backend bundle at the pinned Odoo. Clipped content therefore never reaches the document scroll width. All 15 measured surfaces recorded exactly `1366 == 1366`, and `responsive.json` stores no per-surface measurements at all. | Medium | Found 2026-07-27. No connector-owned clipping was demonstrated, so this is recorded as evidence weakness rather than a product defect; a replacement needs a measurement that survives an ancestor clip | Responsive and RTL overflow claims rest on an instrument that is structurally incapable of failing, so a real clipping regression would pass unnoticed | Next authorized correction session | Before external validation of PR #204 | **Implemented (2026-07-27, this branch, unmerged and unreviewed) — pending external validation.** Correction: `TD-016: an overflow instrument that can actually fail` (`01ddc1a`). **Classified as an EVIDENCE correction: the improved instrument reproduced no connector-owned clipping at any width in either direction, so no production CSS was changed.** It now measures, per surface and per width, the surface's own `scrollWidth` against `clientWidth`, whether it declares `overflow-x: auto|scroll`, the nearest ancestor that constrains it and whether that ancestor *scrolls* (reachable — §10's sanctioned treatment for wide content) or *hides* (gone), every descendant rendered outside a hiding ancestor with interactive ones flagged separately, and the surface's own displacement. RTL is measured at all three required widths rather than desktop alone. `test_the_overflow_instrument_can_actually_fail` injects a 4000px element and requires the instrument to report it while the document total stays unchanged — which is precisely why the old check could not have caught it. The one candidate defect initially reported (the export-diff table at 390px) was traced to the measurement itself walking from the surface rather than the descendant, which skipped `.sc-x-table`'s own `overflow-x: auto`; the stylesheet was not touched. Evidence regenerated with readable per-surface measurements: `docs/05-qa/evidence/wave-5-u2-u3-2026-07-27/`. **Not claimed:** no independent review of the corrected head, no Odoo.sh runtime, no live-Shopify evidence, no UAT. Moves to `Resolved` only after all four. |
| TD-020 | 2026-07-29 | Inventory — location remap (`shopify_connector_inventory_service.py`) | **A Shopify location whose pairs have a CONFIRMED first push can never be remapped.** `remap_location_mapping` refuses while any inventory-level binding on the mapping is `previewed` or `confirmed`, and that refusal is correct: re-pointing the Odoo target under a confirmed pair would reuse a human's explicit first-push confirmation for a warehouse nobody reviewed, which is exactly what the first-push guard exists to prevent. But `first_push_state = 'confirmed'` is terminal — there is no route that returns a confirmed pair to `pending` — so the refusal is permanent rather than a wait. `previewed` is recoverable in principle (the pair can be re-previewed) but has no operator control that does so either. | Medium | Recorded rather than solved because unwinding a confirmed first push is a first-push-guard decision, not a location-mapping one: it needs its own governed route with its own consequence disclosure, and inventing one inside a bounded onboarding batch would be exactly the scope drift that batch forbids. The safe refusal ships; the unwinding does not. | A merchant who physically moves a warehouse after their first stock push cannot re-point the Shopify location at the new Odoo location through the product. The workaround is per-pair and manual, and for confirmed pairs there is no workaround inside the connector at all | Next authorized inventory correction session | Before Wave 6 / external UAT | **Implemented, 2026-07-29 (merchant-operability closure batch), pending independent review.** `withdraw_first_push_decision` on the inventory service is the governed route out: Administrator-only, consequence-confirmed, reason-audited through `_create_lifecycle_audit_job`, refused outside a proven safe terminal state (any non-terminal inventory job, any `pending`/unresolved-`uncertain` mutation attempt, or a `stale`/`review` flag must be resolved through its own accepted route first), row-locked with an expected-state check so a stale dialog or concurrent decision loses. The pair returns to `pending` — the old confirmation is never reused, and the untouched D-013-4 gate forces a complete new preview + confirmation. Reached from the pair form (`Withdraw First Push`, Administrator group) via `shopify.connector.first.push.withdraw.wizard`. Proven by `test_location_mapping.py::TestFirstPushWithdrawal` (journey: confirmed → remap refused → withdraw → remap succeeds → ceremony required again; every refusal; staleness; concurrency; audit content) and the real-browser `shopify_connector_u2_first_push_withdraw_tour`. Local evidence only: NOT independently reviewed, NOT Odoo.sh-qualified, NOT live-Shopify validated — the register keeps this row until those gates pass. The original refusal and its tests are unchanged. |

## Status after the 2026-07-27 consolidated final correction

> **Status: implementing-session record. NOT an acceptance, NOT a review.**
> **The implementation worker has not reviewed or accepted its own work.**

A third correction cycle ran on `fable/wave-5-completion` from head
`cb9f0ad`, again fast-forward. It exists because four of the previous
cycle's rows claimed more than their evidence supported. **The rows below
stay `Implemented — pending external validation`; none moves to
`Resolved`.**

| Row | Why the previous claim was insufficient | What this cycle added |
| --- | --- | --- |
| **TD-011** | The resume service had **no production caller** — every visible caller was a test. The row read "a stopped media export can be resumed"; an operator could not resume one. The 15 tests all called the private helper directly | A public `action_shopify_resume_media_export` on the exported-media registry, wired to a button on the form the existing menu opens. Operator/Administrator, `check_access('read')` and a company check before any elevation. The resume ordinal is consumed only on real admission, and a repeated click coalesces instead of admitting a second live attempt at one image. **13** regressions through the public action, plus a browser tour that presses the control on the real surface and asserts the queued job afterwards |
| **TD-013** | Correctly implemented, **insufficiently evidenced**. All 13 tests called `_prepare_preconditions_*`/`_advance_plan` directly, which is unit coverage of the guard and not proof it is bound into the route a real mutation job takes | One `-standard` class drives `run_drain()` on a genuine pooled connection through the real claim, `_drain_mutation_one` and the registered `prepare_preconditions`, asserting zero calls at the transport choke point, the accepted fail-closed state, and no child mutation job. A companion control lets an unexpired confirmation through, so the refusal cannot pass on a dead route |
| **TD-014** | Throttle pressure was evaluated **once per pass, before the claim loop**. A store reporting 2% head-room on its first job could still have four more claimed by that same pass — the precise burst backpressure exists to prevent. The 16 tests pulled the lever themselves and never asked whether mid-pass state changed later claims | The deferred set is re-read before every claim and **unioned**, so pressure binds the rest of the pass while unrelated stores keep draining; the mid-pass read does not re-project recovery, so a pass can only accumulate. 3 regressions through `run_drain()` driving a real-shaped `throttleStatus` through the production response choke point, with no forced 429 |
| **TD-015** | Two distinct gaps. (a) Media divergence was decided from **local rows alone** — an `associated` row with a File GID and a checksum produced `verified` with nothing having read Shopify, which is exactly the claim a reconnect invalidates. (b) Store settlement was neither atomic nor generation-scoped: two final jobs could each see the other as pending and both decline, leaving every binding terminal and the store permanently `in_progress`; and the verdict was stamped with `connection_generation` as read at settle time rather than the epoch it covered | (a) A read-only re-read of the product's media connection, plus a Files-by-connector-filename read for any association missing from it — the same proofs the module's own accepted mutation reconciliations use. **Checksum correspondence cannot be proven remotely and PD-PX-7 requires it, so a binding that claims an associated media File routes to `review` and its store settles `review_required`** — corrected 2026-07-27 after a search of the accepted Task 015/015B decisions found no accepted authorization for the narrower identity-and-association proof (every such statement was written inside this same unmerged cycle, `4a9ec8f`). A binding claiming no associated media still reaches `verified`. Truncation beyond one page routes to review, never to a proven absence. **Correction, 2026-07-27 (operator resolution).** The
statement that "an operator must clear it before exports resume" was written
before any route existed that could clear it: the only public action re-RAN the
pass, which re-read the same product, re-derived the same unprovable checksum
and landed in the same review, and `export_reconcile_state` was rendered on no
screen. A reconnected store that had ever exported product media was therefore
blocked from exporting permanently, by construction. That is now a real route.
Exactly one review reason is resolvable — the stored, machine-readable
`export_reconcile_reason == 'checksum_unverifiable'`, reached only after store
identity, product identity, archive state, the governed variant set, every File
identity, every File status and response completeness have all been established
remotely, leaving the byte digest Shopify does not expose as the single unknown.
Every other reason — missing, archived, detached, failed, foreign, mismatched,
truncated, in-flight, variant-divergent, inconclusive — is **not**
acknowledgeable and stays blocked; there is no general-purpose override.
Acknowledgement is Connector Administrator only (not Reviewer, which the
Connector User role implies), checks record access and company consistency
before anything elevates, requires an explicit consequence-stating
confirmation, makes no Shopify request of any kind, and is bound to the
connection generation, the binding, the remote product GID, the remote File GID
set and a digest of the local media claim — re-derived on every read, so a
later reconnect, a fresh pass, a re-pointed product, a re-uploaded File or a
changed local checksum withdraws it automatically. **Acknowledgement is not
verification and this register does not call it one:** byte correspondence was
never proven remotely, and the audit record and the operator dialog both say
so. The store note for a `complete` reached this way names the acknowledgement
count and its exact limit. 42 regressions plus a browser tour that walks the
whole route. (b) Settlement serializes on the store row via an unconditional sequence bump flushed before the sibling read, so a concurrent settlement raises `40001` and is re-driven under the pass's already-declared replay policy; each job settles its own epoch; stale-generation jobs are retired at enqueue and refuse themselves at dispatch; a repeated reconnect coalesces or replaces instead of failing on `UNIQUE(store_id, operation_scope_key)`. **27** regressions (9 media + 2 checksum-disposition + 12 single-transaction convergence + 4 genuine cross-transaction), plus 3 rewritten parent-class media tests. **Evidence correction, 2026-07-27:** the 12 convergence tests are single-transaction and never were a cross-transaction proof, though this register previously implied otherwise; the mechanism is now proven by `TestExportReconnectSettlementRace` on two genuine `db_connect` connections, with an observed SQLSTATE `40001`, the dispatcher's real no-replay re-drive to `complete`/`review_required`, and a sensitivity case that strands the store when the serialization boundary is removed |

**Not reopened:** TD-012 and TD-016. **Still retained by decision and NOT
resolved:** TD-004, TD-005, TD-007.

**TD-002 `[Corrected 2026-07-27]`.** Its backend implementation is **accepted
and merged through [PR #189](https://github.com/AdamsOdoo/Adams/pull/189)**
(merge `3a1afa43f8d07a7dae1799968273fa0ab8049490`, merged PR head
`e12145ce8bb88c099208f025d3cbb656bf0393ca`, runtime-tested candidate
`25639f17be14b30a52a8453f0813aa0b764de310`). Describing it as "owned by PR
#189" was accurate while that PR was an open draft and is not accurate now: a
merged pull request owns nothing outstanding. What **does** remain open, and is
not claimed by any Wave 5 work, is TD-002's deferred external evidence —
live-Shopify validation, Gate D, [CV-013 #185](https://github.com/AdamsOdoo/Adams/issues/185),
external UAT and final release evidence. It was not touched, duplicated or
transplanted by this branch.

Each new regression was shown to **fail when its production correction is
reverted**; the TD-011 button is additionally load-bearing, because removing
the action makes Odoo's view validator refuse to install the module at all.
That is source-inspection and local automated-test evidence. **No Odoo.sh
runtime, no live-Shopify contact, no UAT and no independent review of this
head** is claimed.

## Status after the 2026-07-27 correction cycle

> **Status: implementing-session record. NOT an acceptance, NOT a review.**

The cycle corrected TD-003, TD-008 and TD-011 through TD-016, and
re-affirmed TD-004, TD-005 and TD-007 as retained limitations rather than
resolving them. Read the individual rows for the detail; the summary a
reader needs is this:

**The connector does NOT have zero technical debt.** `[Corrected 2026-07-27]`
TD-002's backend implementation is accepted and merged through PR #189; its
deferred live-Shopify, Gate D, CV-013, external UAT and release evidence
remain open and are not claimed here. TD-004, TD-005 and TD-007 remain
open by decision, and two of them (TD-004, TD-007) carry limitations an
operator will meet in ordinary use and which belong in UAT.

What can be said, and only after the evidence below exists, is narrower:
**whether the connector has zero known pre-UAT functional blockers.** At this
head that statement is *not yet* established. Every corrected row is
`Implemented — pending external validation`, and each of the four evidence
classes it is pending on is genuinely absent:

| Evidence | State at this head |
| --- | --- |
| Independent review of the corrected head | **Not performed** |
| Odoo.sh runtime validation at the exact head | **Not performed** |
| Live-Shopify validation | **Not performed** — no store, credential, request or mutation of any kind |
| UAT | **Not begun** |

A row moves to `Resolved` when all four exist for it. Nothing here should be
read as a release-readiness claim.

## Status after the 2026-07-28 correction (independent review comment `5100097485`)

> **Status: implementing-session record. NOT an acceptance, NOT a review.**
> **The implementation worker has not reviewed or accepted its own work.**

A fresh independent review of PR #204's head `ef67c8035e7ee2f6cafd564fcbf2e12153a7e817`
returned **HOLD**: three P1 and two P2-material defects, none refuted under
adversarial verification. Two of the five sit directly in **TD-015**'s own
surface and are corrected here; none of the four evidence classes in the
table above changes as a result — this correction adds source-inspection
and local-test evidence only, same as every prior cycle.

| Defect | TD-015 surface affected | Correction |
| --- | --- | --- |
| **#4 (P2-material)** | `_remote_media_divergence` (`shopify_connector_export_reconnect.py`) | A truncated remote media read (`hasNextPage=true`) reached the acknowledgeable `checksum_unverifiable` reason whenever every connector-claimed File GID happened to appear on the one page that WAS read — the code checked "every claimed File found" before checking "was the read complete", reasoning (incorrectly) that the first implies the second. Truncation is now checked independently of, and before, that case. Regression: a fixture with `hasNextPage=True` and every claimed File present now asserts `media_read_truncated`, not `checksum_unverifiable`. |
| **#5 (P2-material)** | `_reassert_export_reconcile_acknowledgements` (same file) | The security-relevant re-validation pass that keeps an acknowledgement's invalidation load-bearing was capped at `EXPORT_RECONCILE_REVIEW_LIMIT` (200) — a constant borrowed from the store-form's UI display list — with no explicit order. A stale acknowledgement beyond the 200th (by id) was invisible to it forever, while the settlement path that first reaches `complete` scans unbounded. Replaced with deterministic keyset pagination (`RECONCILE_REVALIDATION_BATCH_SIZE`, a separate constant) that walks every matching row regardless of how many batches that takes. Regression: >200 acknowledged bindings, one stale beyond position 200, reached and re-blocked; a companion >200-valid-population test proves the walk is not a vacuous permanent block. |

A third defect, **#1 (P1, cross-company confidentiality bypass)**, sits in
the TD-015 acknowledgement wizard but is a SEC-3 company-isolation defect
rather than a PD-PX-7 reconciliation-pass defect; it and its correction are
recorded in
[`../03-architecture/sec3-company-isolation-audit.md`](../03-architecture/sec3-company-isolation-audit.md)
§8.9, not duplicated here. The remaining two defects (#2/#3, S1 connect-only
and Catalog-export-only activation) are an S1 readiness-check defect, not
TD-015; S1 has no row of its own in this register to correct.

**Not reopened by this correction:** every other row above, and every other
TD-015 claim not named in the table — the acknowledgement's binding to
connection generation/product identity/File-GID-set/local-claim digest, its
Administrator-only authority, its zero-Shopify-contact guarantee, and its
audit trail are all unchanged and unaffected.

---

_This register is active. TD-001 was its first real entry (logged via
AR-027's F1 acceptance patch, 2026-07-07) and is now **Resolved** (fixed by
PR #115, 2026-07-08 — see the TD-001 row above). TD-002 (readiness
scope-naming concern) remains **Open for code**; Wave 0 closed the exact-scope
research/routing question, pending DEC-033 acceptance and Wave 4 implementation. Research-only gaps and gated
architecture/MVP questions from earlier research and product sprints
(Sprints A–G, summarized below) remain tracked in their own research/product
docs, the handoffs, and `architecture-review-log.md` — they are **not**
technical debt unless a compromise is explicitly logged here as its own
row._

_**Research Sprint C note (2026-06-30):** none. Sprint C was research-only
(competitor deep dives, matrix, UX benchmark, patterns, gaps, avoid-list) — no
code, no module, no shortcuts. The blocked sources (R2 Teqstars docs 403, R5
Google Doc) are **research gaps**, tracked in
[`../01-research/resource-inventory.md`](../01-research/resource-inventory.md) and
the handoff, **not** technical debt._

_**Research/Product Sprint D note (2026-07-01):** none. Sprint D was
synthesis-only (canonical feature taxonomy + capability evidence map + product
handoff) — no code, no module, no implementation shortcuts. The weak/blocked
competitor evidence (Teqstars docs 403, EC/R5 setup guide, 17 unread VT Confluence
articles) and the open architecture/MVP questions are **research gaps / gated
decisions**, tracked in the taxonomy's "Open questions", the handoffs, and
`architecture-review-log.md` — **not** technical debt._

_**Product Sprint E note (2026-07-01): none.** Sprint E was product-strategy /
synthesis-only (product vision + setup/UX principles + handoffs) — no code, no module,
no implementation shortcuts. The open MVP/architecture questions (distribution, queue
framework, binding model, module boundaries, multi-company, error/retry taxonomy) and
the weak/blocked competitor evidence (Teqstars 403, EC/R5 setup guide, 17 unread VT
Confluence articles) are **gated decisions / research gaps**, tracked in the product
vision's "Open questions", the handoffs, and `architecture-review-log.md` — **not**
technical debt._

_**Product Sprint F note (2026-07-01): none.** Sprint F was an MVP-proposal /
synthesis-only sprint (MVP scope proposal + non-MVP boundaries + user stories + handoffs)
— **no code, no module, no implementation shortcuts.** The open MVP/architecture questions
(direction/export, Domain 9 minimum, refunds/cancellations, distribution AR-002, queue
framework AR-003, binding model AR-005, error/retry taxonomy AR-006, inventory/fulfilment
AR-007/008, module boundaries AR-004, bulk ops, multi-store/company) and the weak/blocked
competitor evidence (Teqstars 403, EC/R5 setup guide, 17 unread VT Confluence articles)
are **gated decisions / research gaps**, tracked in `mvp-scope.md` "Open questions", the
handoffs, and `architecture-review-log.md` — **not** technical debt._

_**Product Sprint G note (2026-07-01, incl. PR #55 revision): none.** Sprint G was a
decision-recording / documentation sprint (accepted MVP scope in
`../04-decisions/DEC-003-mvp-scope.md` + product doc alignment + handoffs; PR #55 corrected
**controlled product export/update into MVP**) — **no code, no module, no implementation
shortcuts.** Scope **deferred** from MVP (**unrestricted autonomous bidirectional catalog
ownership**, customer export, refunds/cancellations, full Domain 9 accounting automation,
payouts, multi-store/company, bulk-ops-as-a-feature, etc. — **note: controlled product
export/update is in MVP, not deferred**) is a **planned product-scope boundary with revisit
conditions**, **not** deferred hardening or a compromise — so it is **not** technical debt.
The still-open MVP/architecture questions (AR-002…AR-008, the destructive-apply mechanics +
product match/binding model, the Domain 9 draft-artifact exception, internal bulk-ops need)
are **gated decisions**, tracked in DEC-003, the handoffs, and `architecture-review-log.md` —
**not** technical debt._
